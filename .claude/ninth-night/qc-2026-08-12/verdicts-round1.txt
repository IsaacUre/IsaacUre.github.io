
=== [1] survived 2/2  severityVotes=['bug']
  --correct-- refuted=False conf=high
     Re-derived from source, not from the finding. Every claim holds.

1. setLamp is at comp/ninth.js:627-645 (line numbers as filed). Read the whole body: line 637 is `S.items.lamps[here] = 1; sSave();` and that is the only world-state write. It calls takeItem/giveItem/sSave/fillBag and returns a string. It never touches p.props, never pushes a light, never bakes a sprite.

2. lightsOf is at comp/ninth.js:7812-7829. Its entire input is `(p.props || [])` (emitters house/lamp/foot/mill/hearth) plus `(p.lights || [])` at 7828. `.lights` appears exactly once in the whole file (line 7828, the read), so p.lights is never populated by anything. S.items.lamps reaches it by no path.

3. Reachability of the renderer: `grep -n lightsOf` returns only the definition (7812) and one call (7837, inside drawLights). `grep -n drawLights` returns the definition (7830) and one call (draw() line 3892). So drawLights/lightsOf is the whole light pass and it cannot see S.items.lamps.

4. The six-site grep is correct. `S.items.lamps` occurs at 472/473 (sLoad default), 629/630/637 (setLamp), 650 (lampsElsewhere), 657/658 (takeLamp), 983 (dev Clear the bag), 4882 (bag panel). I also checked for indirect access: `grep -n "S\.items\["` returns nothing (no computed property route), and the only enumeration is `Object.keys(S.items.lamps)` at 650 inside lampsElsewhere, which feeds the bag text at 4887-4891, not a renderer.

5. The three prose lines are verbatim where claimed: 641 (the mark line), 643 (square), 644 ('It throws about a yard of light and the rest of it stays dark'), and comp/ninth.js:4884 ('You set this one down here. It is still burning.').

6. The mark claim checks out independently. PLACES.mark at comp/ninth.js:5216-5229 is `w:13, h:11, night:1, dark:1` with props markstone, fence, fence, tree. None of those four is in lightsOf's emitter list, so the place has zero prop lights. night:1 means drawLights does NOT return early at 7831 and does lay the rgba(6,5,14,0.5) veil, so the only light in the mark is the player's own `self` light pushed at 7838. Setting the lamp down there is therefore a no-op on the render, exactly as claimed.

7. Reachability in real play: ITEMS.lamp (302-306) has `cost: 30`, and fillShop's stock filter at 4933 is `ITEMS[id].cost && !ITEMS[id].writ`, so the lamp is real purchasable stock at the chandler. `use: function () { return setLamp(); }` at 305, and useItem at 605 says the returned string. This is not dead code.

Corroborating detail the finding missed: line 628 is `var here = RT.place, p = place();` and `p` is never referenced again anywhere in setLamp's body. The function fetches the place object and then does nothing with it, which is the shape of exactly the omission being reported.

I found no guard elsewhere. Nothing in draw()'s ents pass (3847-3932), no LIVE entry, no PROP entry, no reLook, no realisation check reads S.items.lamps. The NPC glow at 7985-7990 is NPC_PROP.widow (2083), unrelated.
     CORRECTION: Finding stands as written. One refinement to the proposed fix, not to the claim: lightsOf is only ever called from drawLights, which returns at comp/ninth.js:7831 when `!p.night`. So appending the player's lamp inside lightsOf lights it in the night places (including the mark, night:1) but still shows nothing in stage or arena, and in every place it produces a pool of light with no lamp object painted under it. If the gesture is meant to read visually, the position also needs something in the ents pass, not just an entry in the light list.
  --repro-- refuted=False conf=high
     Read the code, not the finding. comp/ninth.js:637 is the whole world effect of setLamp (`S.items.lamps[here] = 1; sSave();`). lightsOf at 7812 builds its list from p.props (house/lamp/foot/mill/hearth) and p.lights only and never touches S. draw()'s ents array at 3872-3891 is built from RT.foes, place().npcs, place().props and the actor, so there is no fourth source either. Grep for `items.lamps` returns exactly the sites the finding names (473, 629, 630, 637, 650, 657, 658, 983, 4882), none of them a renderer. The three prose lines are verbatim at 641, 644 and 4884. PLACES.mark (5216-5230) is night:1 dark:1 with props markstone/fence/fence/tree, none an emitter, so the finding's "zero prop lights" is right.

Reproduced with a positive control. Wrote .claude/nnqc/reach1-lamp.html: one page load at the mark, real `i` keydown to open the bag, real .click() on the bag's [data-use="lamp"] button, real Escape, with RT.t, shake, toasts, typo, parts, calls and every NPC's wait/bob/position pinned identically either side so the two frames differ only by world state. Served from a fresh python http.server rooted at this worktree, because port 8492 is currently serving a different 15512-line build (whose setLamp/lightsOf are identical in the relevant lines, but I did not rely on it). Result: lamp set, lamps={"mark":1}, canvas diff 0 changed pixels of 649600. Positive control with the player nudged 0.4 tiles so the self light pool moves: 41244 changed pixels, worst 303, box [389,191,637,432] (~250x242, the size an r-3.2 pool occupies). So the harness does catch a light-sized change; there is none. Screenshots .claude/nnqc/reach1-mark-lampSET.png vs reach1-mark-lampCTL.png (lamp vs coal, same spot, same frames) are indistinguishable except the HUD word card.

Reach: fully reachable, no save state, no gate. ITEMS.lamp has cost 30 and fillShop:4933 stocks every item with a cost and no writ, so it is ordinary chandler stock. The chandler exit (5107), lane->village (5168) and village->mark (5213) all have no `needs`. A player buys the lamp, walks four screens, clicks use, and reads a bespoke paragraph about it burning on the marker stone while nothing on screen changes. The item description ("You can put it down somewhere") advertises the gesture.

Severity: `bug` as filed. Nothing crashes and nothing is unplayable, so not `broken`; but a signposted player action prints narration the world contradicts, in the one place authored dark:1 with no light source, so not `polish` either.

=== [2] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     Mechanism confirmed line by line, then confirmed again by an A/B pixel diff.

CODE, re-read at the real lines:
- comp/ninth.js:2143-2147 (tail of drawActor, which starts at 2110): `sx = isoX(RT.px,RT.py)`, `sy = isoY(RT.px,RT.py)+TILE_H/2`; then `cx.save(); cx.globalCompositeOperation='lighter'; var lg = cx.createRadialGradient(sx, sy-10, 6, sx, sy-10, 120); lg.addColorStop(0,'rgba(255,190,90,.14)'); lg.addColorStop(1,'rgba(255,180,70,0)'); cx.fillStyle=lg; cx.beginPath(); cx.arc(sx, sy-10, 120, 0, TAU); cx.fill(); cx.restore();`. The three preceding cx.restore() calls have already unwound every actor transform, and draw() only ever applies a translate (shake) at 3852, so no scale is in effect. It is a true circle of radius 120.
- comp/ninth.js:7838-7848 drawLights: `if (!RT.dead) ls.push({x:RT.px, y:RT.py, r:3.4, c:'255,214,150', i:0.8, self:1})`; `rad = l.r*30` = 102; `cx.translate(sx, sy - (l.self?14:10)); cx.scale(1,0.62)` then `arc(0,0,102)`. Ellipse with semi-axes 102 x 63.24. Two pools, different radius, different y offset (-10 vs -14), different colour.
- Ordering confirmed at comp/ninth.js:3889 (`ents.push({k: RT.px+RT.py, fn: drawActor})`), 3890-3891 (sort + run), 3892 `drawLights(cx)`, 3893 `drawVignette(cx)`. So drawActor's pool is under the veil laid at 7834-7836 and the self light is over it.

MATHS re-derived from TILE_W=58, TILE_H=29 (line 46). A ground displacement (dx,dy) projects to (29(dx-dy), 14.5(dx+dy)). A circular ground pool must project 2:1. drawActor's reaches 120px vertically = 8.28 tiles of (x+y) half-reach; drawLights' reaches 63.24px = 4.36 tiles. So the circle is 1.90x too tall for the plane it claims to lie on.

MEASURED. I could not use the port-8492 server: it is rooted at the suspicious-driscoll-00fbd8 worktree, whose comp/ninth.js is 872028 bytes against this worktree's 475774 and was last written at 00:50 today. I served this worktree on 127.0.0.1:8571 and used .claude/nnqc/cor2-pool.html, a copy of canvas.html that fetches ninth.js as text and can delete exactly those five lines before eval (needle verified unique).
- mark (night:1, no emitter props, static NPC, camera fixed because placeBox is 824x505 < viewport): diff of cor2-mark-poolon.png vs cor2-mark-pooloff.png is a single circular blob, centroid (486.3, 319.0) canvas px against my predicted (487.5, 316.25), vertical span 195.0-434.5 = radius 120. Peak added colour summed 38; predicted 0.5*0.14*(255,190,90) summed = 37.4. Exact. Amplified diff saved as .claude/nnqc/cor2-mark-pooldiff.png: a perfect circle.
- stage (no `night`, so drawLights returns at 7831): sampled the diff along four cardinal rays from the derived pool centre (545.5, 287.25). Left, right and up all read (36,27,13) at r=0 and hit zero between r=110 and r=118; (36,27,13) is 0.14*(255,190,90) to the pixel. Full strength, no veil, circular. Confirms the claim that on the stage and in the arena (PLACES.stage at 5079-5081 and PLACES.arena at 5290-5293 both have no `night`) the wrong-shaped pool is the only player floor light.

NO GUARD EXISTS, and the file's own convention is against it: every other ground-plane circle is squashed. drawRings 2992 `cx.scale(1,0.5)`; the moveTo marker 3867 `cx.scale(1,0.5)`; the actor's own contact shadow 2117 `cx.ellipse(0,bob,10,4)`; contactShadow 6703-6705 follows the footprint quad. Only drawActor's 120px pool is unsquashed. It has been unchanged since the first commit (5f40ecc, `git log -L 2140,2150:comp/ninth.js`), i.e. it predates the drawLights pass that now also lights the player.

Two supporting facts the finding missed, both strengthening it: drawLights only pushes the self light `if (!RT.dead)` (7838) while drawActor draws its circle regardless, so on death in a night place the circle is the only pool left; and because the pool is inside the ents pass, any prop with a larger sort key paints over it, so a "floor" light is occluded by the sprite of a prop standing in front of the player.
     CORRECTION: The mechanism is exactly right and every load-bearing number checks out. Four corrections, all to the framing rather than the substance.

1. "4.1" is wrong: the drawLights ellipse reaches 63.24/14.5 = 4.36 tiles of (x+y), not 4.1. The ratio against drawActor's 8.28 is 1.90, not 2.02. (4.1 would come from rad 96, which is finding [6]'s r:3.2 street lamp, not the r:3.4 self light.)

2. "an ellipse 102 x 63.2" are semi-axes, not the bounding box; the pool is 204 x 126.5 across.

3. "loses 50% of itself (66% in the hollow)" is right outdoors but not indoors: drawLights:7834 uses rgba(14,8,10,.34) for `p.indoor`, so in bernhouse and chandler the pool keeps 66%. Three different attenuations for the same lamp, not two.

4. The perceptual claim is overstated. "the pool smears up behind the player and down in front, which is what makes the player look like they are standing in a vertical column rather than a puddle" does not survive the A/B. At 0.14 peak alpha ramping linearly to zero over 120px, the on/off pair (.claude/nnqc/cor2-mark-crop-on.png vs cor2-mark-crop-off.png) differs as a gentle broadening of a soft halo. It is wrong, and it is measurable, but nobody would read it as a column without the control frame beside it.

Severity: polish, not bug. Nothing malfunctions, no content is hidden, no input is lost; the whole symptom is a soft warm gradient with the wrong aspect ratio. Round one's own finding [3] is structurally the same defect (two overlapping overlays in different coordinate spaces, one quietly undoing the other's intent) and is graded polish; this should match it. The prescribed fix is still correct: wrap the gradient in `cx.translate(sx, sy-10); cx.scale(1,0.62);` and draw at (0,0).
  --repro-- refuted=False conf=high
     Every code fact checks out at the stated lines. comp/ninth.js:2143-2147 is verbatim as quoted: comment at 2143, cx.save()+lighter at 2144, createRadialGradient(sx, sy-10, 6, sx, sy-10, 120) at 2145, the two stops at 2146, and arc(sx, sy-10, 120) + fill at 2147, with no cx.scale anywhere in that block. comp/ninth.js:7831 is `var p = place(); if (!p.night) return;`. comp/ninth.js:7838 is `if (!RT.dead) ls.push({ x: RT.px, y: RT.py, r: 3.4, c: '255,214,150', i: 0.8, self: 1 });`, drawn at 7845 under `cx.translate(sx, sy - (l.self ? 14 : 10)); cx.scale(1, 0.62)`. draw() pushes drawActor into ents at 3889 and calls drawLights at 3892, so the under-veil/over-veil ordering is right.

I could not use port 8492: it is serving a DIFFERENT build (15512 lines, md5 31818d93e0d8cc1155d91bf39e460dd1, contains FAMS.ark, a `conceal` hood state, and this exact pool already refactored to `var pr = lerp(120, 44, hood)`) against this worktree's 8279 lines, md5 6868ebe295bd3edae01bff7afb8f61c0. I served this worktree myself on 127.0.0.1:8571 and md5-verified the served ninth.js before every capture.

Measured in situ rather than re-derived. Scratch probe .claude/nnqc/rr2-poolprobe.html patches createRadialGradient to tag drawActor's gradient (r0=6,r1=120) and drawLights' self light (r0=2,r1=102), then patches fill() to getImageData a 340x340 box immediately before and after each of those two fills and diff them, so the numbers are the pool's actual contribution to the frame:
  actorPool: peak red delta +36, reach at delta>=3 up 111 / down 111 / right 111 / left 112 px (at delta>=1: 117/117/117). Isotropic to within 1px: a circle.
  selfLight: peak +49, reach up 57 / down 57 / right 93 / left 93. 93/57 = 1.63 = 1/0.62. An ellipse.
Probing ndev=stage and ndev=arena records actorPool ONLY (no selfLight entry), confirming drawLights returns at 7831 there; square and hollow record both. So the "only player light in the prologue stage" claim is real and the prologue is reachable by every player.

Where it does not reproduce: the stated harm. Scratch page .claude/nnqc/rr2-poolab.html is canvas.html plus hkill=actor, which neuters that one gradient and changes nothing else. rr2-ab-stage-ON.png vs rr2-ab-stage-OFF.png and rr2-ab-hollow-ON/OFF.png, and a third pair with the player at the stage's north corner (rr2-ab-stageN-ON/OFF.png), are near-indistinguishable read side by side. The gradient is 0.14 alpha at the centre falling linearly to 0 with no edge anywhere, so it reads as a soft warm wash. Only at 6x gain (rr2-mkdiff.js -> rr2-diff-stage.png) does the circle become visibly a circle. Nobody looks at this and sees a vertical column. The layer is not negligible in energy (region-restricted diff over the world area: stage max channel delta 40, 42% of pixels shift >=16; hollow max 21, 0.01% >=16), it is just shapeless.

Two smaller overstatements: "reaches 120px up and down" is the nominal radius where alpha is exactly zero; visible reach is 111px, so "8.3 tiles along (x+y)" is really 7.7 (111/14.5). And "a correct pool" is not well defined here, since drawLights' own 0.62 is not the ground plane either (TILE_H/TILE_W = 29/58 = 0.5).

The double-draw defence that the circle is a lantern bloom rather than a floor pool does not hold: the bloom already exists separately as the radius-7 'lighter' arc inside the lantern-arm block at 2136-2139, and the 120px gradient is centred at sy-10, at the feet, with a comment that says "on the floor".

Artifacts under .claude/nnqc/: rr2-poolprobe.html, rr2-poolab.html, rr2-abdiff.js, rr2-mkdiff.js, rr2-ab-{stage,stageN,hollow}-{ON,OFF}.png, rr2-diff-{stage,hollow}.png, rr2-{stage,hollow,arena,square}-pool-z*.png.
     CORRECTION: The player's lantern is drawn as two overlapping pools with different radius, colour, y offset and vertical squash, and one of them sits under the night veil. Measured in situ: drawActor's (ninth.js:2145) contributes +36/255 red at its centre and reaches 111px equally in all four screen directions (a circle); drawLights' self light (7838) contributes +49 and reaches 93px horizontally against 57px vertically (an ellipse, ratio 1.63 = 1/0.62). Because drawActor's is in the ents pass (3889) and the veil is laid down inside drawLights (3892), the same lamp's pool comes out at a different strength in every place: 36 on the stage and in the arena, where drawLights returns at 7831 and the circle is the only player light; 18+49=67 outdoors at night 1; 24+49=73 indoors; 12+49=61 in the hollow. That inconsistency, not the shape, is the durable half of this. The shape error is not visible: A/B captures with the gradient neutered and nothing else changed are near-indistinguishable, and the "smears up behind the player and down in front... a vertical column rather than a puddle" claim does not reproduce at 1x. Drop "8.3 tiles" to 7.7 (111px visible reach, not the 120px nominal radius where alpha is already zero).

=== [3] survived 2/2  severityVotes=['polish']
  --repro-- refuted=False conf=high
     REPRODUCED EXACTLY, and it is on for every player on every frame.

Trap first: the dev server on port 8492 is NOT serving this worktree. It serves .claude/worktrees/suspicious-driscoll-00fbd8 (comp/ninth.js 15569 lines; ours is 8279, md5 6868ebe... vs 81e4f19...). /.claude/nnqc/lightqc-novig.html 404s there, so my first two captures were byte-identical 404 pages, which reads as "the CSS vignette does nothing". I served this worktree myself on 127.0.0.1:8571 and re-ran.

Code, checked at the lines: comp/ninth.js:805 emits '<div class="nn-vig"></div>' as the LAST child of .nn. comp/comp.css:2920 styles it 'position:absolute; inset:0; pointer-events:none; background: radial-gradient(ellipse at 50% 45%, transparent 52%, rgba(3,2,6,.72) 100%)'. grep for z-index across the whole .nn CSS block returns exactly two rules: .nn-panel/.nn-dev z-index 5 (comp.css:2983) and .nn-dlg z-index 6 (comp.css:3052). .nn is position:relative with z-index auto so it opens no stacking context; .nn-cv (comp.css:2919) is z-index auto and earlier in DOM order. So .nn-vig composites over every canvas pixel and over every DOM overlay except the panels and the dialogue box. Exactly as the finding says.

drawVignette at comp/ninth.js:7857-7868 is as quoted: createRadialGradient(VW/2, VH/2-30, ind?300:250, VW/2, VH/2, 680) with outdoor stops 0 -> rgba(4,3,8,0), .55 -> rgba(4,3,8,.3), 1 -> rgba(4,3,8,.8), and the comment about .92 at 620 against a 631 corner and "it just stops eating the town". git log -S confirms the timeline the finding implies: the softening landed in 527e3a7 (#119); the .nn-vig div and its CSS rule are untouched since the original NINTH NIGHT commit 5f40ecc (#95). Nobody revisited the CSS layer when the canvas layer was softened.

Repro, re-run fresh (.claude/nnqc/rr3b-sq-vig.png vs rr3b-sq-novig.png, via .claude/nnqc/lightqc-novig.html?dev=ninth&nwipe=1&ndev=square&nat=8,10&nfr=300&hz=1 with and without hnovig=1, mean luminance over 21x21 canvas-px boxes using lightqc-diff.js):
  centre (560,290)       28.6 / 28.6  kept 1.000
  left house (140,150)   22.6 / 25.5  kept 0.884
  right house (980,150)  18.7 / 21.3  kept 0.880
  lower-left (40,540)     2.8 /  4.9  kept 0.574
  lower-right (1080,540)  6.5 / 13.5  kept 0.482
  bottom-centre HUD       6.1 /  7.3  kept 0.836
The finding's figures land within a couple of percent. Reading the two PNGs side by side the difference is plainly visible: the rhyme and stanza buttons, the breath bar, the scene title and the B/K/I/V corner buttons are all noticeably more legible with the element removed.

Reachability, in the real game and not the harness: I loaded /comp/?dev=ninth in an iframe and read live rects (.claude/nnqc/rr3-measure.html, .claude/nnqc/rr3-measure.png). .nn box 1158.0 x 678.0, canvas painted at 1158.0 x 599.7 with object-fit letterbox y=39.2, .nn-vig present true, last child of .nn true, zIndex auto, display block, opacity 1; the child dump shows nn-cv/auto nn-scene/auto nn-coin/auto nn-toasts/auto nn-say/auto nn-dlg/6 nn-hud/auto nn-panel/5. So this is the shipped state inside the fake desktop: no dev flag, no save state, no harness. Every frame a player ever sees has both vignettes on it.

Arithmetic for the real window, since the finding measured on the 1120x580 QC page: for W=1158 H=678 the farthest-corner ellipse is centred (579, 305.1) with rx = 579*sqrt(2) = 818.8, ry = 372.9*sqrt(2) = 527.4. Canvas px map as px = cx*1.0339, py = 39.2 + cy*1.03397. The canvas corner (1119,579) lands at normalised 0.947 -> CSS alpha 0.640; the QC page gives 0.716 there. The canvas gradient is at about alpha 0.674 at the 631-radius corner, so combined transmission is (1-0.674)*(1-0.640) = 0.117 in the real window, against 0.326 for the canvas layer alone and 0.08 for the pre-softening version the comment was written to fix. "Largely undone" holds at the corners.
     CORRECTION: Real and reproducible as stated; two refinements. (1) The measured percentages come from the 1120x580 QC page. In the real 1158x678 window the canvas is letterboxed 39.2px top and bottom and the CSS ellipse is computed over the taller box, so it bites slightly less over canvas pixels: the house wall keeps about 0.92 rather than 0.884, and the extreme canvas corner takes CSS alpha 0.640 rather than 0.716. The conclusion is unchanged: combined corner transmission is 0.117, against 0.326 for the canvas vignette alone and 0.08 for the pre-softening version the drawVignette comment complains about. (2) The finding understates the reach. Because .nn-vig is the last child with z-index auto, and the only higher z-indexes in the entire .nn block are .nn-panel/.nn-dev at 5 (comp.css:2983) and .nn-dlg at 6 (comp.css:3052), the CSS layer also dims every DOM overlay: .nn-scene, .nn-coin, .nn-toasts, .nn-say, the whole .nn-hud and the .nn-btns corner row. drawVignette can never touch those, since it only paints canvas pixels. Measured, the bottom-centre HUD keeps 0.836 and the bottom-right corner where the B/K/I/V buttons sit keeps 0.482, so the second layer removes over half the light from a row of interactive buttons. Severity stays polish.
  --correct-- refuted=False conf=high
     Re-derived everything from source rather than trusting the finding.

CODE, verified line by line:
- comp/ninth.js:805 `'<div class="nn-vig"></div>' +`, and 806 is `'</div>'` closing the `.nn` root opened at 757. So it is the last child. grep for "nn-vig" across comp/ninth.js, comp/comp.css and comp/*.html returns exactly two hits: ninth.js:805 and comp.css:2920. Nothing hides, removes or reparents it at runtime.
- comp/comp.css:2918 `.nn { position: relative; ... overflow: hidden }`, 2919 `.nn-cv { position: absolute; inset: 0; ... object-fit: contain }`, 2920 `.nn-vig { position: absolute; inset: 0; pointer-events: none; background: radial-gradient(ellipse at 50% 45%, transparent 52%, rgba(3,2,6,.72) 100%) }`. Both z-index auto, both positioned, so they paint in tree order and `.nn-vig` is on top. There is no later override of `.nn-vig` anywhere in comp.css.
- comp/ninth.js:7857-7869 drawVignette is verbatim as quoted, including the comment at 7858-7862 ("used to reach .92 black at radius 620 against a canvas whose corner is at 631 ... it just stops eating the town to do it"). VW=1120, VH=580 at comp/ninth.js:45, so the corner distance from (VW/2, VH/2) is sqrt(560^2+290^2) = 630.6, which is the "631" the comment cites. Correct.

INDEPENDENT EMPIRICAL PROOF (my own capture, not round one's):
The dev server on 8492 serves the MAIN repo, not this worktree, so `.claude/nnqc/lightqc-novig.html` 404s and the round-one A/B page is not reachable from here. (comp/ninth.js and comp/comp.css are byte-identical between the two, checked with diff, so capturing against the served copy is valid.) Rather than rebuild the probe I used a test that needs no probe and that the canvas vignette provably cannot cause: the DOM HUD. `.nn-b` buttons are five identical opaque #14101c squares at fixed positions (comp.css:2978-2979, .nn-btns right:12 bottom:14, 26x26, gap 4 -> x 962..1108, y 540..566). Captured http://localhost:8492/.claude/ninth-night/tools/canvas.html?dev=ninth&nwipe=1&ndev=square&nfr=300&nat=8,10&hz=1 to .claude/nnqc/skep3-hudtest.png and sampled each button interior 5px in from its top-right corner (clear of the glyph) with .claude/nnqc/skep3-probe.js. Measured [12,10,18] [12,9,17] [11,8,16] [10,8,15] [9,7,14] against my from-first-principles CSS-gradient prediction [12,10,18] [12,9,17] [11,9,16] [10,8,15] [10,7,14]. A canvas-space gradient cannot dim a DOM element that sits above the canvas, so the only thing that can produce that ramp is `.nn-vig`. Reading the full capture, the four corners are near-solid and the bottom-right button row is barely visible.

CHECKED FOR A GUARD ELSEWHERE: none. No z-index on `.nn-cv`, no stacking context on `.nn` other than position:relative with z-index auto, no `.nn-vig` display/visibility toggle in ninth.js, no media query or later rule touching it.

CHECKED FOR "IT WAS TUNED THIS WAY ON PURPOSE": `git log -L` on comp.css:2920 shows `.nn-vig` landed in 5f40ecc (#95, the original proof of concept, Aug 5). `git show 527e3a7` (#119, Aug 10) is the commit that softened drawVignette from `radialGradient(..., 210, ..., 620)` / `.92` to `250/680` / `.8`, and its added comment reasons only about the canvas radius and the canvas corner. Neither the commit nor the comment mentions the CSS layer. So the softening was not compensated for a known second layer; the two are independent and the author of the softening did not account for the one already sitting on top.

The finding is correct, reproducible by a route independent of the one it used, and its arithmetic survives re-derivation. Severity "polish" is defensible (nothing is functionally broken) even though the HUD legibility consequence pushes it toward the top of that band.

Scratch evidence I left behind: .claude/nnqc/skep3-hudtest.png and .claude/nnqc/skep3-probe.js.
     CORRECTION: The finding stands and is if anything understated. Two corrections to the "what":

1. It says the CSS layer "composites on top of every canvas pixel". True, but incomplete: `.nn-vig` is the LAST child of `.nn` (comp/ninth.js:805, closing `</div>` at 806), so it also composites on top of every DOM HUD element, all of which are position:absolute with z-index auto and appear earlier in render() — `.nn-scene` (760), `.nn-coin` (761), `.nn-toasts` (762), `.nn-say` (765), `.nn-hud` (769), `.nn-btns` (797), `.nn-tip` (804). Only `.nn-panel`/`.nn-dev` (comp.css:2983, z-index 5) and `.nn-dlg` (comp.css:3052, z-index 6) escape it. This is the strongest available proof of the mechanism, because the canvas vignette cannot reach DOM: the five `.nn-b` buttons are authored `background: #14101c` = rgb(20,16,28) (comp.css:2979) and are pixel-identical to each other, yet in my own capture they render as a left-to-right ramp — B [12,10,18], K [12,9,17], I [11,8,16], V [10,8,15], ` [9,7,14] — matching the alpha I derived independently from the CSS gradient (0.457, 0.495, 0.533, 0.574, 0.615) to within 1/255 on every channel. The bottom-right dev/panel buttons lose 62% of their light and are near-invisible in the capture.

2. "It is only below .nn-panel at z-index 5 and .nn-dlg at 6" should also name `.nn-dev`, which shares the z-index:5 rule at comp.css:2983.

The claimed magnitudes are sound. My own derivation of the CSS layer (box 1120x580, centre 50%/45% = (560,261), farthest-corner ellipse rx=560*sqrt(2)=791.96, ry=319*sqrt(2)=451.13, transparent at t=0.52 ramping to alpha .72 at t=1) gives alpha 0.097 at the house walls at (140,150) and (980,150) — the finding measured 0.884/0.880 kept, I predict 0.913, same order — and alpha 0.72 at the lower canvas corners, predicting kept 0.61 against their measured 0.620.

The "largely undone" claim also checks out numerically. drawVignette (comp/ninth.js:7864-7867) at the canvas corner, distance sqrt(560^2+290^2)=630.6 from (560,290), sits at 0.745 of the way between the 0.55 stop (r=486.5, alpha .3) and the r=680 stop (alpha .8), i.e. alpha 0.672. Stacking the CSS .72 on top of that gives 1 - 0.328*0.28 = 0.908 total at the bottom corners, which is essentially the 0.92 the comment at 7858-7862 says was too much.

=== [4] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     Mechanism confirmed against source and reproduced. (1) comp/ninth.js:7820 pushes the house sill light at x = b[0]+b[2]+0.2 unconditionally. (2) Five house props are authored flush with their place's east boundary: comp/ninth.js:5092 [13,0,4,3.6] and 5093 [13.4,11,3.6,4] in square (w:17, line 5090), 5206 [11.4,0,3.6,3.4] in village (w:15, line 5204), 5283 [13.6,0,3.4,2.8] and 5284 [13.4,12.2,3.6,2.8] in a3sq (w:17, line 5281). All three places carry night:1, so drawLights (7830-7853) runs. (3) The floor really ends at world x = gw: buildFloor's tile loop (1325-1336) draws index (x,y) as the diamond world (x,y),(x+1,y),(x+1,y+1),(x,y+1), so tiles cover x in [0,gw]; everything outside is the '#07060a' fill at 1319, and draw() paints the canvas '#07060a' at 3856. drawLights is called at 3892, after every prop, with globalCompositeOperation 'lighter', so the glow lands additively on that background. (4) I re-derived the geometry rather than trusting the finding. Edge x=gw runs in screen direction (-29,+14.5); outward unit normal (0.4472,0.8944). The light's drawn centre is isoX(l.x,l.y), isoY(l.x,l.y)+TILE_H/2-10 (7841,7845). Perpendicular offset outside the edge = 0.2*(29*0.4472 + 14.5*0.8944) + 10*0.8944... computed exactly: 9.21 px, identical for all five. rad = l.r*30 = 108, drawn under scale(1,0.62), so the pool ellipse has semi-axes 108 x 66.96; its radius along the outward normal is 71.5 px and its support half-width is 76.9 px, giving 57.6% of the pool area beyond the edge line. (5) Camera: village placeBox is x 148..1059 (w 911 <= VW) so camX is pinned at 43.5, matching the finding; camY is 4.5 not 4.25 because camBounds reads the Math.ceil'd box height (549 from 548.5). Square is pinned at camX 29 with the east corner at canvas x 1024, so the void beyond it is on screen. (6) Reproduced: .claude/nnqc/sk4-village-sill-z2.png and .claude/nnqc/sk4-square-se-sill.png both show the cobbles stopping at the wall base with a warm pool continuing down-right onto black. Pixel probe of .claude/nnqc/sk4-village-z1.png along the outward normal at the lit house: lum 34.06 / 28.95 / 17.19 / 7.86 at d = 9 / 20 / 40 / 70 px, rgb(43,34,27) at d=9 (warm, R>G>B); the same edge at y=6 and y=10.5 reads 6.4 flat, rgb(6,6,12), which is exactly #07060a under the rgba(6,5,14,.5) night veil. So the lit pixels are provably the void, not dark floor, and the falloff is the ~70px the finding claims. No guard elsewhere: nothing clamps a light to the place, and drawVignette (7857) only attenuates it by about 15% at that screen position. Two numbers in the finding are wrong but neither is load-bearing: the game has 12 house props, not 14 (grep "t: 'house'" returns 5+3+4), and 'the pool is 102 x 63 screen px' is the player's own r:3.4 self-light (7838), not this one - the r:3.6 pool is 216 x 134. The 72px half-extent the maths actually uses is correct (71.5). Caveat on the proposed fix, not on the finding: Math.min(b[0]+b[2]+0.2, (p.w||GRID)-0.35) puts the square's lights at x=16.65, which is 0.35 inside the house's own footprint (13..17), sliding the pool centre behind the wall it is meant to shine through; 0.05 or a per-place nudge would keep it at the foot of the wall.
     CORRECTION: Five of the TWELVE house sill lights (not fourteen) are pinned outside their own place. The pool is 216 x 134 screen px, not 102 x 63 (rad = l.r*30 = 108, drawn under cx.scale(1,0.62); 102 x 63 is the player's own r:3.4 light at comp/ninth.js:7838). The rest holds exactly: centre 9.21 px outside the x = gw edge measured perpendicular, pool radius 71.5 px along that normal, 57.6% of its area on the #07060a void. Village camera is (43.5, 4.5), not (43.5, 4.25), because camBounds uses the ceil'd box height; the quoted canvas y 320.9 is the post-translate centre, the ground projection is 330.7.
  --repro-- refuted=False conf=high
     Reproduced in full, both by re-deriving the projection and by measuring pixels.

CODE. comp/ninth.js:7812-7829 is lightsOf; line 7820 is verbatim `out.push({ x: b[0] + b[2] + 0.2, y: b[1] + b[3] * (1 - u), r: 3.6, c: '255,196,110', i: 0.95 })`. The five flush houses are at comp/ninth.js:5092-5093 (square w:17, [13,0,4,3.6] and [13.4,11,3.6,4]), 5206 (village w:15, [11.4,0,3.6,3.4]) and 5283-5284 (a3sq w:17, [13.6,0,3.4,2.8] and [13.4,12.2,3.6,2.8]). All five put the light centre at x = w + 0.2.

THE VOID IS REAL. buildFloor fills the whole bitmap #07060a at comp/ninth.js:1319 and clips pass two (the cobbles) to the place diamond at comp/ninth.js:1338-1347, so there is provably no ground painted outside the diamond; draw() clears the canvas to the same #07060a at comp/ninth.js:3856. drawLights (comp/ninth.js:7830-7853) runs after the whole painter-sorted entity pass and applies no clip, so the pool falls on that background.

GEOMETRY, RE-DERIVED. buildFloor's shape[] is built from tiles 0..gw-1, so in the entity convention (isoY(x,y) + TILE_H/2, used by drawActor at comp/ninth.js:2111 and by drawLights at 7841) the painted floor covers world [-0.5, w-0.5] x [-0.5, h-0.5] - which is exactly the player's clamp range at moveActor, comp/ninth.js:6055 (0.5 .. W-0.5). For the square with cam (29,20) the diamond corners are canvas N(531,64) E(1024,310.5) S(589,528) W(96,281.5). The NE house's sill light (propVar('house',[13,0,4,3.6]) = 5, so houseSillU = 0.6, u = 0.67, y = 1.188) draws at canvas (995.3, 335.1). The east edge at that x is y = 324.85, so the centre is 10.25 px below it = 9.2 px perpendicular OUTSIDE. The finding's "about 9px" is exact. The 0.2-tile push is 18.2 px perpendicular; the -10 px translate at comp/ninth.js:7845 pulls 8.9 px of it back. Pool: rad = l.r*30 = 108 (7842), scale(1,0.62) so semi-axes 108 x 66.96; perpendicular half-extent along the edge normal (0.4472,0.8944) is 1/sqrt(nx^2/108^2 + ny^2/66.96^2) = 71.5 px, matching their 72. Area fraction on the void side = 0.5 + (1/pi)(asin(s) + s*sqrt(1-s^2)) with s = 9.2/71.5 = 0.129, giving 58%.

PIXELS. Fresh captures (.claude/nnqc/skep4-village-z1.png, skep4-village-z2.png, skep4-square-z1.png, skep4-square-ne.png) with a PNG luminance probe I wrote (.claude/nnqc/skep4-probe.js). Village light centre canvas (924.8,320.7) - identical to the finding's (924.8,320.9). Ray outward along the edge normal from the light vs a control ray 200 px further south along the same edge: light 30.6 / 18.1 / 10.5 / 5.3 at t=10/30/50/70 px, control 6.4 / 6.4 / 6.4 / 5.4. The control drops to background the moment it crosses the edge; the light ray carries ~70 px of warm glow onto the black. Round one's 26.74 / 11.88 / 4.78 at 9/40/70 px is the same curve. Same result in the square: 13.1 at 10 px out against a flat 5-6 control.

REACH. Grelling and the square are both walked into in normal play; the two a3sq lights are on screen too (canvas (1003,331) and (649,508)). skep4-village-z2.png and skep4-square-ne.png show the smear at zoom with no dev state beyond ndev/nat. It is not hidden by the vignette (drawVignette alpha there is about 0.2).
     CORRECTION: Real and reproduces exactly; severity polish is right. Three corrections. (1) There are TWELVE house props in the whole game, not fourteen - square 5 (comp/ninth.js:5092-5093), village 3 (5206), a3sq 4 (5283-5284). So five of twelve sill lights, not five of fourteen. (2) "The pool is 102 x 63 screen px" is wrong: rad = l.r*30 = 108 with scale(1,0.62), so the pool is 216 x 134. The derived 72 px perpendicular half-extent and 56% are correct and were computed from the right 108/66.96 ellipse, so this is a stray label rather than a propagated error. (3) The prescribed fix does NOT fix it. `Math.min(b[0]+b[2]+0.2, (p.w||GRID) - 0.35)` gives x = 16.65 for a w:17 place, but the painted floor stops at entity x = w-0.5 = 16.5 (buildFloor's shape[] runs over tiles 0..gw-1, comp/ninth.js:1338-1343), and the -10 px translate at 7845 already accounts for 8.9 px of the current offset. The clamped centre lands only 5.0 px INSIDE the painted edge, taking the void share from 58% to 46%. A clamp nearer `w - 1.2` would be needed to reach ~26%, and it cannot reach zero at all while the pool is 143 px across the normal and the lit wall is authored on the map boundary. If this is worth fixing it is a five-footprint move, not a one-line clamp - which is the opposite of what the fix line claims.

=== [5] survived 2/2  severityVotes=['risk', 'polish']
  --repro-- refuted=False conf=high
     Reproduced in full. grep over comp/ninth.js returns `dark` only at 5192 (loft), 5218 (mark), 5263 (hollow) as a place field, plus local vars in roofSpar (6784) and PAINT.curtain (7521) and prose. No `.dark`, `p.dark` or `['dark']` read exists anywhere in the game file. lightsOf is 7812, drawLights is 7830, and the only darkness reads are `p.night` at 7831 and `p.night >= 2` at 7835 — the finding's line numbers are all still correct after drift. `indoor: 1` appears only on bernhouse (5119) and chandler (5137), so loft and mark do receive the identical rgba(6,5,14,0.5) veil as square (5090) and lane (5154); the claim is exact.

Proved the field's sole effect empirically rather than by reading: I wrote a copy of ninth.js with `dark: 1` stripped and a copy of audit-geometry.js pointed at it (.claude/nnqc/rr5/). It printed exactly `3 problems: loft / mark / hollow: is night but has no lamp, house or light in it.` The unmodified audit prints `geometry clean`, exit 0. So audit-geometry.js:193 is the one and only consumer and the exemption is load-bearing for exactly those three places.

Confirmed PARALLEL.md:169-170 lists only `arena`, `calm`, `boss` as read-nowhere place fields, so `dark` is genuinely undocumented as dead.

Reach: no player can observe it. Screenshot .claude/nnqc/rr5-mark.png (ndev=mark, nat=6.5,7.5, nfr=300) shows the mark already renders black with only the lantern pool and vignette, so the inert flag costs nothing on screen. That is consistent with the finding's own `risk` classification — it never claimed a player symptom — so it is not refuted on reach grounds, but it cannot be promoted above `risk` either.
     CORRECTION: Two narrowings. (1) `dark` is not dead in the same sense as arena/calm/boss, which have zero readers anywhere; it has exactly one reader, audit-geometry.js:193, whose error text names the field and explains its purpose, so an author who trips over it is told what it does. The accurate statement is "a place field that lives in the game's data and reads as a render flag, but whose only consumer is a build-time linter." (2) The "silent in both directions" claim is half unexercisable: all three `dark: 1` places have zero light-emitting props today, so there is no last light to drop out of any of them and that direction cannot fire. The real residual risk is one-directional and narrow: adding a house/lamp/foot/mill/hearth prop to the loft, the mark or the hollow will light the place and `dark: 1` will not suppress it. Severity stays `risk`.
  --correct-- refuted=False conf=high
     Every factual claim checks out, verified line by line.

CONFIRMED: `dark: 1` is authored at comp/ninth.js:5192 (loft), 5218 (mark), 5263 (hollow) — exact lines. A grep for `\.dark`, `['dark']`, `["dark"]` and `dark:` across all of comp/ninth.js returns ONLY those three authoring sites. Every other `dark` hit in the file is a string literal in prose (129, 644, 724, 851, 2882, 3148, 5185, 5274, 5314, 6366), a local `var dark` in roofSpar (6781) and PAINT.curtain (7521), or a comment (1044, 1208, 3540, 6586, 6979, 7802, 7832).

CONFIRMED: drawLights is comp/ninth.js:7830-7853 and its only darkness reads are `var p = place(); if (!p.night) return;` at 7831 and `(p.night >= 2 ? 0.66 : 0.5)` at 7835. Both line numbers exact. I checked `indoor`: none of loft/mark/hollow has it (5192, 5218, 5263), so they take the else branch. loft and mark are night:1 -> rgba(6,5,14,0.5), identical to square (5090, night:1) and lane (5154, night:1). The veil claim is right.

CONFIRMED: the sole reader is audit-geometry.js:193, `if (!lit && !p.dark) bad(... 'Set `dark: 1` if the lantern is meant to be the only light.')`. Exact line. Nothing in comp/comp.css or comp/index.html reads it (the .bctx.dark CSS hits are browser context-menu styling, unrelated).

CONFIRMED: PARALLEL.md:169-170 lists exactly arena, calm and boss. `dark` is absent.

CONFIRMED the three places are genuinely emitter-free, so the flag describes reality: loft props are beam x4 + sack x2; mark is markstone + fence x2 + tree; hollow is cairn + stone x5 + tree x2. None are house/lamp/foot/mill/hearth and none have a `lights` array.

WHERE IT OVERSTATES. (1) The title's "reads as a render flag" implies an unwired or lost render feature. `git log -S"p.dark" -- comp/ninth.js` returns NO commits at all: the game has never read it in any revision. `git log -S"dark: 1" -- comp/ninth.js` and `git log -S"p.dark" -- .claude/ninth-night/tools/audit-geometry.js` both land on the same commit f0d9ba6 — the field and its only consumer were introduced together. It was born as a lint-suppression annotation, documented by its consumer's own error string, not as a render flag that lost its wire. (2) "The failure mode is silent in both directions" is half wrong. Direction one (add a lamp to a dark:1 place and nothing flags the contradiction) is a real, unchecked gap in the audit. Direction two ("drop the last light out of a dark:1 place and nothing warns, because the exemption is permanent") is not a failure at all: dark:1 asserts that lantern-only is intentional, so no warning is exactly correct behaviour.

WHAT IS LEFT. Real actionable residue: `dark` belongs alongside arena/calm/boss on PARALLEL.md:169-170 so a parallel author does not grep ninth.js, find nothing, and assume it is dead-and-deletable; and audit-geometry.js:193 could warn on a redundant dark:1. Zero player-visible symptom, no screenshot possible, nothing in the game misbehaves, and the audit does exactly what its author intended — which puts it below the other `risk` entries. Comparable dead-data findings in the same file ([12] dead save keys, [21] dead `ins`) all describe a feature that was authored and never wired; this one does not. Not a duplicate of [6], which is about the same four audit lines but attacks the emitter list and the presence-vs-reach test.
     CORRECTION: `dark: 1` (comp/ninth.js:5192 loft, 5218 mark, 5263 hollow) is read by nothing in comp/ninth.js — confirmed, drawLights at 7830-7853 reads only p.indoor, p.night and p.night >= 2 — and its only consumer is audit-geometry.js:193. But it is not an unwired render flag: git shows the field and the audit line arrived in the same commit (f0d9ba6) and `p.dark` has never appeared in comp/ninth.js in any revision, so it is a deliberate lint-suppression annotation whose meaning is documented by the audit's own error string. Only one of the two claimed silent directions is real: the audit never flags a `dark: 1` place that does have an emitter. The reverse case (a dark:1 place losing its last light with no warning) is the annotation working as designed, not a missed error. The defect that survives is documentation-sized: `dark` is missing from PARALLEL.md:169-170's list of place fields that are read nowhere in the game, which is what makes it read as live.

=== [6] survived 1/2  severityVotes=['risk', 'polish']
  --correct-- refuted=False conf=high
     I re-derived the whole thing from source rather than trusting the write-up.

PROJECTION, from scratch: comp/ninth.js:58-61 give isoX = ORX + (x-y)*29, isoY = ORY + (x+y)*14.5 (TILE_W 58, TILE_H 29, line 46). drawLights (comp/ninth.js:7830-7852) puts a light at sx = isoX(l.x,l.y), sy = isoY(l.x,l.y)+TILE_H/2, then translate(sx, sy-10), scale(1,0.62), arc(0,0,rad) with rad = l.r*30. So the reachable set of a world point (x,y) is ((dx-dy)*29/rad)^2 + (((dx+dy)*14.5+10)/(0.62*rad))^2 <= 1. The finding's version (rad 96 for r 3.2, semi-axes 96 and 59.52, |dx-dy| <= 3.310, |dx+dy| <= 4.105, min x = lx - sqrt(3.310^2+4.105^2)/2 = lx - 2.637 = 5.81) is arithmetically correct; it only drops the 10px lift, which moves things the same way it claims.

GAP (1) IS LITERALLY TRUE. audit-geometry.js:192 tests `house | lamp | foot | mill`. lightsOf at comp/ninth.js:7813-7826 emits for house, lamp, foot, mill AND hearth (line 7825). hearth appears once in the data, bernhouse comp/ninth.js:5126, and bernhouse does also carry a lamp at 5125, so the escape-by-accident claim is right too.

GAP (2) IS LITERALLY TRUE: line 192 is a `.some`, presence only, no reach.

REPRO REPRODUCED TO THE DIGIT. I wrote .claude/nnqc/skeptic6-light.js (grabs PLACES/PROP out of ninth.js the way the audit does, re-implements hash2/propVar/houseSillU/lightsOf verbatim). Output: square 49%, bernhouse 44%, chandler 40%, a3sq 40%, village 33%, lane 21/221 = 10%, mill 6%, road 21/374 = 6% with 1% in the bright core, loft/mark/hollow 0%. Lane south exit (6.2,16.3) against the lamp light at (7.85,14.65): dx-dy = -3.3 exactly, q = 95.7/96 = 0.99687. Every figure in the finding lands.

WHAT I COULD NOT USE TO REFUTE IT: drawExits does add a 30px additive glow per open exit, but at comp/ninth.js:3863 it runs BEFORE drawLights at 3892, so it goes under the night fill rather than over it; it does not make an exit "lit" in the sense the check is about, and the finding never claimed the exit was invisible. The player's own lantern (7838) covers the player wherever they stand, which is exactly the "deliberate choice" the audit comment at 190-191 already carves out.

So the mechanism is what the code does. Two things are overstated, and one consequence is smaller than stated - see correction.
     CORRECTION: Keep gap (1) as written; scope gap (2) down and fix two overstatements.

1. "the lamp's gradient does not reach a single point of the corridor the player actually walks" is too strong. It reaches 5% of the road's standable points and 21 of 374 tile centres, and that pool sits inside the fenced corridor (fences at comp/ninth.js:5239 run x 0.8-1.3 and 9.7-10.2 for y 23.6-32, so x 1.3..9.7 is walkable and the ellipse round (8.45,30.65) is inside it). A player hugging the east side at y about 29-32 does walk through it. What is true, and is the whole point: it reaches neither exit band. South exit (5.4,33.3) is at q = 1.725; the north exit is out of range entirely. Nothing on the x = 5.4 line the two exits define is lit.

2. The blast radius of the hearth omission is the audit's exit code, nothing more. `dark` is read nowhere in comp/ninth.js - the only three occurrences (5192, 5218, 5263) are writes in PLACES data. So a future hearth-only room costs you a red build and a data field that lies about the room, not anything a player sees.

3. Gap (2) is a scope decision more than a hole, and the proposed fix is wrong as specified. At the suggested 5%-of-standable threshold the check fires on road (5%) and mill (5%) on day one and lane sits at 11%, and all three are deliberate in the source: comp/ninth.js:5238 "the last lamp in the world, off the walking line" and 5157-5158 "The town lights the lane as far as it thinks the town goes, and then it stops". The author already made the human decision the finding wants the audit to ask for. The presence check is a floor for "night place with literally nothing to light it", and coverage is a different question that would need those three places whitelisted before it could ship.

Net: the actionable part is the one-word list divergence at audit-geometry.js:192. Add `hearth` so the audit's emitter list matches lightsOf. Severity risk (latent, no effect today) stands for that; the reach half is a note, not a defect.
  --repro-- refuted=True conf=high
     I ran both halves.

HALF 1 (hearth drift) — the code reads exactly as claimed. audit-geometry.js:192 is `o.t === 'house' || o.t === 'lamp' || o.t === 'foot' || o.t === 'mill'`; lightsOf at comp/ninth.js:7812-7830 has those four plus `hearth` at 7825. So the two lists really have drifted by one word. But it cannot fire on anything that exists: `grep -n hearth comp/ninth.js` returns exactly one prop instance in the whole game, comp/ninth.js:5126 in bernhouse, which also carries a lamp at 5125. To make the audit misbehave you need a night, non-dark room whose only emitter is a fire — a place that does not exist. And the consequence when it did exist is a nag telling the author to add `dark: 1`, i.e. a false positive in a dev tool, not a missed shipped bug. Note also that `lights:` appears nowhere in PLACES, so the `|| (p.lights || []).length` half of the same line is dead too, which the finding does not mention.

HALF 2 (presence, not reach) — this is where the maths is wrong and the conclusion does not survive. I mirrored lightsOf and drawLights exactly, including the two offsets the finding dropped: drawLights translates to `sy - 10` for non-self lights (comp/ninth.js:7842) and a world point's ground pixel is isoY + TILE_H/2 (placeBox, comp/ninth.js:91). Script: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/skep6-light-reach.js. Results:
- road: the westmost world x the gradient touches is x = 5.48, not the 5.81 the finding derives. Minimum q along the line x = 5.4 is 1.026, so the walking line misses the pool by 2.6% of the falloff radius, at a stop whose alpha is literally zero (`addColorStop(1, 'rgba(...,0)')`). That is not "does not reach", that is a rounding error on a zero-alpha edge.
- lane: the south exit band centre (6.2,16.3) is at q = 1.011, i.e. just outside, not the finding's 0.997 "on the last pixel of it". Same zero-alpha edge, other side.
- Coverage per night place, re-derived over standable points: square 41 / bernhouse 34 / chandler 48 / lane 11 / mill 4 / village 21 / road 6 / a3sq 22. The finding's table (square 49, chandler 40, village 33, a3sq 40, mill 6) is off by up to 18 points on four of nine rows. road 6 and lane 11 do match.

The reach premise itself does not hold. "The corridor the player actually walks" is an assumption about a free-roaming 11-wide place. The road's walkable band runs x 0.5..9.7 (fences at 0.8 and 9.7); nothing pins the player to x = 5.4 between the two exit centres. The 21 lit tiles sit around the lamp, exactly where the "The last lamp" look at (7.4,30.2) is, at q = 0.268.

Screenshots: .claude/nnqc/skep6-road-lamp.png (ndev=road, nat=5.4,31.5, nfr=300) and .claude/nnqc/skep6-road-lamp-z2.png (hz=2 on the lamp). The road is entirely legible — fence, trees, cairns, ground tiles all read — and the lamp's pool is a visible warm patch right beside the player. night:1 lays down rgba(6,5,14,0.5), and drawLights pushes the player's own lantern (r 3.4, i 0.8) every frame unless RT.dead. There is no player symptom to point at.

The proposed fix would break the thing it protects. audit-geometry.js ends in `process.exit(1)` on any problem and its header says "Exit code 1 if anything is wrong, so it can gate a commit". The two places the proposed <5% reach rule flags are the two whose source comments state the darkness is deliberate: comp/ninth.js:5238 `// the last lamp in the world, off the walking line` and comp/ninth.js:5159-5161 `// the last lamp of Wick. The town lights the lane as far as it thinks the town goes, and then it stops.` The check's own comment at audit-geometry.js:190-191 states its scope: "a place lit only by your own lantern is a deliberate choice; a lit place with nothing to light it is a mistake". Both places have something lighting them.

One more thing that guts the stakes of both halves: `p.dark` is read nowhere in comp/ninth.js. A grep for `.dark` returns only the three `dark: 1` literals at 5192, 5218, 5263. It is audit-only metadata with zero runtime effect, so nothing about this line, current or proposed, can change a pixel the player sees.
     CORRECTION: What survives is one word, not a two-part risk. audit-geometry.js:192 should include 'hearth' to match lightsOf (comp/ninth.js:7825) so a future room lit only by its fire does not fail the commit gate. That is a latent one-word tidy-up in a dev tool; no place in the game today can trigger it, since the only hearth (comp/ninth.js:5126, bernhouse) sits beside a lamp. The "presence, not reach" half should be dropped: the derivation omits drawLights's 10px lift (comp/ninth.js:7842), so the road's real cutoff is x >= 5.48 and the walking line misses by 2.6% of the falloff at zero alpha; the lane's south exit is at q = 1.011 rather than 0.997; and four of the nine coverage figures are off by up to 18 points. The road and the lane are dark on purpose and say so in the source (comp/ninth.js:5238, 5159-5161), the player carries their own lantern, and .claude/nnqc/skep6-road-lamp.png shows the road reading perfectly well. Adding the proposed <5% reach rule to a commit gate that exits 1 would fail the build on authored intent.

=== [7] survived 1/2  severityVotes=['bug', 'bug']
  --repro-- refuted=True conf=high
     I reproduced it with a with/without pixel diff rather than by eye: render the frame, park the child at (-40,-40), render the identical frame again, count changed pixels. Any changed pixel is a pixel of her that survives to the screen. Probes at .claude/nnqc/sk7-childroof.html, sk7-loop.html, sk7-shot.html; evidence image .claude/nnqc/sk7-ab2.png; patched audit at .claude/nnqc/sk7-audit.js.

DATA AND ARITHMETIC CONFIRMED. comp/ninth.js:5346 child x:10.6 y:9.8 small:1, path at 5348; comp/ninth.js:5093 house [13.4,11,3.6,4]; comp/ninth.js:7962 `var h = n.small ? 26 : 40`; RT.hide is player-only at 3882. My probe printed the house base centre as 623.8,492.9 and her foot as 583.2,394.3, which matches the finding's dx=-40.6 / dy=-113.1 exactly. Running the audit with only the height swapped: fh=40 clean, fh=26 fires with margin 12.9px. The 1.1-pixel miss is real.

THE CONCLUSION DRAWN FROM IT IS NOT. Measured, with the player at the finding's own nat=9.6,8.4 so no cutaway fires: 109 changed pixels against 532 on open cobbles. Rows -32..-20 relative to her feet, i.e. the top 13 rows of a ~36-row figure. That is her whole head, hat and shoulders standing clear above the roof ridge, in the baked CHILD_SPR sprite, not text. "Every pixel of her is behind the roof" is false, and so is "an empty roof". sk7-ab2.png panel 2 shows it side by side with the same frame minus her.

THE SUPPORTING SUB-CLAIMS ARE FALSE. Path point 1 (10.6,12.6), which the finding says "is covered too": 482/532 px, 91% visible, visually indistinguishable from the open-ground control. Along the first leg she measures 20% -> 25% -> 30% -> 50% -> 90%+, never invisible.

IT IS ALSO LARGELY UNREACHABLE. 120 s of the real stepNpcs, sampled 12x/s: 267/1440 samples at (7.5,10.0), but only 17 samples (1.4 s, the initial rnd(0.4,2.2) wait) at (10.5,10.0) and exactly 3 samples in each first-leg cell. She walks the first leg once per visit and never returns, because the leg from path[3] (7.4,10) back to path[0] (10.6,9.8) runs straight through the well footprint [8,9.4,1.6,1.6] (comp/ninth.js:5095), blocked() stops her and the give-up branch at comp/ninth.js:6207 skips her to path[1]. And the cutaway does rescue it from most approach angles: with the player at (9.8,10.4), still inside the 1.9 interact radius, coversSomeone fires and the roof eases to T_CUT, giving 508/532 px.

WHAT IS ACTUALLY THERE. Path point 3 (7.4,10.0): 18/532 px at the exact point, 0% at the half-tile bucket she really occupies, entirely hidden by the well (h:26 over:54, comp/ninth.js:6394). That is 18.5% of her wall-clock time invisible. The audit at today's fh=40 already flags it with 16.8px to spare once path points are tested, so only half the proposed fix is load-bearing: adding path points is right, and 40 -> 26 is wrong, since it fires on her stand (20% visible) and on path point 1 (91% visible), both false positives against the rule's own stated intent at audit-geometry.js:181 ("only flag somebody with nothing showing").
     CORRECTION: Not "painted out by a house roof". The SE house leaves her head, hat and shoulders above the ridge (109 of 532 pixels, the top 13 rows of the figure), she is only there for the 0.4-2.2 s start wait on each arrival, she never returns to that spot, and approaching her from the south thins the roof to T_CUT anyway. The real burial is a different prop: the well [8,9.4,1.6,1.6] hides 100% of her at path point 3 (7.4,10.0), which is where the real loop parks her 18.5% of the time, because the leg from path[3] back to path[0] drives through the well footprint and the give-up branch at comp/ninth.js:6207 sends her round again. The fix is the path-point half only: keep fh=40 and test n.path, which already flags the well with 16.8px margin. Changing 40 to 26 manufactures two false positives (her stand at 20% visible, path point 1 at 91% visible). The data fix is the well leg, not moving her home off the roof.
  --correct-- refuted=False conf=high
     I re-derived every number from source and then reproduced it on screen against THIS worktree (note: the dev server on 8492 is serving a different checkout — .claude/worktrees/suspicious-driscoll-00fbd8, a 15587-line ninth.js. I stood my own server up on 8937 over this worktree and used a probe page at .claude/nnqc/sk7-child.html that freezes the child).

WHAT CHECKS OUT EXACTLY:
- comp/ninth.js:5346 child { x: 10.6, y: 9.8, small: 1, skip: 1, speed: 1.9, path: [[10.6,9.8],[10.6,12.6],[7.4,12.6],[7.4,10.0]] }. `small:` occurs exactly once in the file, so "the only small NPC" is right.
- comp/ninth.js:5094 { t:'house', b:[13.4,11,3.6,4] }; comp/ninth.js:6389 house { h: 78, over: 74 }.
- draw() sort key (comp/ninth.js:3884-3888): house k = 28.2, child k = 20.4, so the house paints after her. Confirmed.
- comp/ninth.js:3882 `RT.hide = [{ x: RT.px, y: RT.py, k: RT.px + RT.py, h: 44 }]` — only the player, so coversSomeone (6669) never thins a roof for an NPC. Confirmed.
- The audit arithmetic is exact. rrx=110.2, rry=55.1, dx=-40.6, dy=-113.1, top=-152, and `dy - 40 >= top` is -153.1 >= -152 = FALSE, missing by 1.1000000000000796 px. `node .claude/ninth-night/tools/audit-geometry.js` still says "geometry clean". The check at audit-geometry.js:168-187 does only read n.x/n.y, never n.path. Both holes are real.
- She is one of three S.heard sources for Fragment I (comp/ninth.js:5939 `S.heard.child || S.heard.busker || S.heard.shepherd`).

WHAT DOES NOT CHECK OUT (three overstatements):
1. "every pixel of her is behind the roof" is false. propVar('house',[13.4,11]) = 1, so PAINT.house takes the flip branch with rh=32; the roof's upper silhouette at local x is the eaveW->eaveN line, y = -143.22 - 0.5x, which at x=-40.6 is -122.92. Her real drawn top is -130.6, so 7.68px of her stands above the roof. Verified on screen: .claude/nnqc/sk7-head-z8.png (8x, player parked far away) shows hair-top, hair, brow and both eyes above the thatch. Because a +y step moves -29,+14.5 on screen and the eave has slope exactly -0.5, that 7.68px gap is CONSTANT for the whole first leg; add the skip bob (drawNpc:7960, stride 5.5 while moving) and it is 8-13px. The finding's own repro line already says "two or three pixels of hat", contradicting its own "what".
2. The 26 is not the drawn height. `var h = n.small ? 26 : 40` (comp/ninth.js:7963) is used only by the GESTURE hook (7999) and the '·' talk marker (8004). The figure is bake('npc.child', CHILD_SPR) blitted at y - spr.height; CHILD_SPR (comp/ninth.js:2020) is 20 rows at PXS 2 with content in rows 4..18, so 32px above the feet, not 26. The suggested fix's `fh = n.small ? 26 : 40` is still wrong. It does not change the outcome here (-113.1-32 = -145.1 >= -152 still fires) but it is the wrong constant.
3. "Nothing rescues it either" is only two-thirds true. coversSomeone/T_CUT does fire — for the player — and drawProp fades the WHOLE roof sprite, which reveals the child too. The player is inside the same box when px+py >= 19.75 and -2.17 < px-py < 6.57; the talk radius is 1.9 (nearestInteract:6237), so most of the ring you can read her prompt from does thin the roof. .claude/nnqc/sk7-near-z4.png (player at 10.6,11.4) shows her head and shoulders straight through the thinned thatch. The symptom does still reproduce from the north-west: .claude/nnqc/sk7-nw-z1.png and sk7-nw-z4.png (player at 9.2,8.8, 1.72 tiles away) are the "E — talk to A child with a skipping rope" label floating over a solid roof, which is the bug as advertised — but from one approach quadrant, not always.

Also, the "second hole" example is a false positive under the finding's own fix. Path point 1 is (10.6,12.6): dx = -121.8, and the west eave tip is at -1.1*rrx = -121.22, so the house paints nothing at all at her centre there — she is in the open. Path point 3 (7.4,10.0) against the well (h:26, over:54, comp/ninth.js:6394) would also be flagged (dy=-23.2, dy-26 = -49.2 >= -80) and is almost certainly another false positive, since the well is a low cylinder. Testing path points is the right idea; those two spots are not evidence for it.

Duration: RT.world.npc = {} on every gotoPlace (comp/ninth.js:6144), so she does restart at 10.6,9.8 each time you enter the square, waits rnd(0.4,2.2) then rnd(0.2,0.8) (npcRT:6177, stepNpcs:6198), then walks the 2.8-tile leg in ~1.47s. That is roughly 1.5-3.5s of an ~8.2s loop; legs 2, 3 and most of 4 are on open cobbles.
     CORRECTION: The child (comp/ninth.js:5346, x:10.6 y:9.8, the game's only small:1 NPC) is drawn behind the south-east house's roof (comp/ninth.js:5094, [13.4,11,3.6,4], h:78 over:74), and the geometry audit's burial check (audit-geometry.js:182) misses it by exactly 1.1px because it hard-codes a 40px figure. All of that is right. Three corrections: (a) she is not fully hidden — the house variant is v=1, whose north-west eave line sits at local y = -143.22 - 0.5x, leaving exactly 7.68px of her (hair, brow, eyes) above the thatch, constant along the whole first leg because the walk direction is parallel to the eave, and 8-13px once the skip bob is added; she reads as a pair of eyes on a roof, not as nothing. (b) The drawn height of a small NPC is 32px (CHILD_SPR, comp/ninth.js:2020, rows 4..18 at PXS 2 blitted from the feet up), not the 26 in `var h = n.small ? 26 : 40` at comp/ninth.js:7963 — that 26 only positions the gesture hook and the talk dot, so the proposed fix's `fh` constant is wrong too. (c) Something does rescue it some of the time: coversSomeone fires for the PLAYER whenever px+py >= 19.75 and -2.17 < px-py < 6.57, and the T_CUT fade applies to the entire roof sprite, so from most of the 1.9-tile talk ring the roof thins to 0.56 and she shows through. The "prompt over an empty roof" symptom reproduces from the north-west approach only. Both audit holes (fixed 40px height; n.path never tested) are still real and worth closing, but the two path points cited as evidence are false positives: (10.6,12.6) projects to dx=-121.8, which is 0.58px past the west eave tip at -121.22, so no house paint touches her there, and (7.4,10.0) against the 26px-tall well is almost certainly the same. Severity: bug, not broken — she is reachable, talkable, visible for most of her loop, and Fragment I has two other sources.

=== [9] survived 2/2  severityVotes=['polish']
  --correct-- refuted=False conf=high
     Tried to refute and could not. comp/ninth.js:6225 is verbatim `label: open ? 'go to ' + e.n : (e.over && S.a3.ending ? 'over' : 'not yet')` and comp/ninth.js:8009 is verbatim `var txt = (o.shut ? '' : 'E — ') + o.label;` — both line numbers exact. Grep shows `'go to '` occurs exactly once in the file and `.label` is consumed at only two places, 8009 (the render) and 1606 (the dev-handle mirror), so there is no alternate, better-worded render and no post-processing that could rescue the string. drawExits (7890-7908) draws only a pulsing ring and a radial lamp, no text. shutText (6115) does not touch e.n. RT.prompt is recomputed every step at 1750 via nearestInteract() with a 1.9-tile radius (6229), and stepTravel() runs one line earlier at 1749 and only fires inside the band with RT.armed, so the prompt is genuinely on screen during the approach rather than being pre-empted. I re-derived the exit table by evaluating the PLACES literal out of the source: 19 exit objects, but 'out to the square' is duplicated (bernhouse:5133, chandler:5150), giving 18 distinct name strings, 11 of which collide with the prefix and 7 of which read cleanly ('the lane, north', "Bern's door", "the chandler's shop", 'the mill', 'the next village, east', 'east, past the fence', 'the hollow, north'). 11 + 7 = 18, so the finding's arithmetic is right. All twelve cited data line numbers check out: 5108, 5133, 5150, 5166, 5183, 5184, 5198, 5212, 5227, 5228, 5254, 5276. Captured independently: .claude/nnqc/skeptic9-bernhouse-zoom.png (ndev=bernhouse&nat=5.5,7.3&nfr=300&hz=4&hw=5.5,7.3) renders '— go to out to the square' at 4x; full frame in skeptic9-bernhouse-prompt.png. The three gated exits show 'not yet' while shut so the bad string only appears once open, but it does appear. No guard elsewhere; the static audit has no prose check. Severity polish is right: it is player-facing prose with no functional effect. The only overstatement is rhetorical — 'the single most-read line in the world layer' is arguable, since 'talk to X' and 'look at X' share the same slot and read fine — which does not change the defect.
  --repro-- refuted=False conf=high
     Read the code, not the finding. comp/ninth.js:6225 is verbatim `label: open ? 'go to ' + e.n : (e.over && S.a3.ending ? 'over' : 'not yet')` inside interactables(), and comp/ninth.js:8009 in drawPrompt is verbatim `var txt = (o.shut ? '' : 'E — ') + o.label;`. Grep for 'go to ' across the whole file returns exactly one hit (6225), and grep for `.label` returns only 1606 (dev handle), 8009 (drawPrompt) and the constructors at 6217/6220/6225, so nothing rewrites the string between construction and paint. Ran the repro URL verbatim: .claude/nnqc/sk9-road-south.png renders 'E — go to back south, to the mark' mid screen. Captured two more the finding did not: .claude/nnqc/sk9-bern-out.png shows 'E — go to out to the square' and .claude/nnqc/sk9-mill-lane.png shows 'E — go to back down the lane'. All three at nfr=300 so sprites are built. Enumerated every exit with a script (.claude/nnqc/_sk9_exitlist.js) rather than trusting the list: 19 hits at lines 5105, 5106, 5107, 5108, 5133, 5150, 5166, 5167, 5168, 5183, 5184, 5198, 5212, 5213, 5227, 5228, 5254, 5255, 5276 - hence the count correction. Checked reachability of the two gated exits, since a label nobody can see would refute those two: exitOpen at 6099 returns testNeed(e.needs), and both flags are set on the normal path - S.seen.a3ready at 5318 (fragCount()===3 after Bern, not just the dev-panel button at 853) and S.seen.chorusDown at 3444 after the Chorus fight. Every other bad exit is plain traversal, and bernhouse/chandler to square is mandatory, so a first-time player reads several of these before leaving the first interior. Not covered by the static audit, which checks exit geometry and targets, never label prose.
     CORRECTION: The mechanism is exactly as described, but the counts are off. There are 19 named exits, not 18. Twelve of them (not eleven) read badly once the fixed prefix is applied: the finding's list of eleven strings includes 'out to the square', which is two separate exits (comp/ninth.js:5133 bernhouse and comp/ninth.js:5150 chandler). Six read cleanly ('the lane, north', "Bern's door", "the chandler's shop", 'the mill', 'the next village, east', 'the hollow, north'). The seventh survivor the finding credits, 'east, past the fence' (comp/ninth.js:5213), actually reads as 'go to east, past the fence' and belongs with the broken ones, which would make it 13 of 19. So the correct statement is "twelve of the nineteen exit prompts read like 'go to back down the lane', and a thirteenth is borderline". Severity polish is right: no functional effect, purely the wording of the most-read line in the world layer.

=== [10] survived 2/2  severityVotes=[]
  --correct-- refuted=False conf=high
     Verified exactly as written. comp/ninth.js:5382 is ['The shepherd', '"...not for the man who came back down — but for the girl who never will."'] with a real U+2014 (literal em-dash grep matched; no en dashes and no —/&mdash; escapes exist anywhere in the file). It is spoken prose, not narration: comp/ninth.js:5375-5390 is NPCS.shepherd.talk, the speaker slot is 'The shepherd' (an empty first element is the narrator, cf. 5381 and 5387), and it sits in the !S.seen.shep1 first-conversation branch, so every player who talks to the shepherd once sees it. Reachable on the normal route: comp/ninth.js:5163 gives the lane npcs: ['shepherd'].

No guard strips it. showDialog at comp/ninth.js:6303 does esc(ln[1]).replace(/\n/g,'<br>'), and esc at comp/ninth.js:35 only replaces & < > " — the em dash reaches the DOM verbatim. The only em-dash-aware code is the map label split at comp/ninth.js:8113, which operates on PLACES[id].n, not dialogue.

The comparisons are quoted accurately. comp/ninth.js:135 carries the couplet as two separate BALLAD strings ('not for the man who came back down' / 'but for the girl who never will.') with no joining punctuation, and comp/ninth.js:2884 repeats that split. comp/ninth.js:5405 is the busker's 'Word. "And we would not say a word." Because nobody answered her.' — inline ballad quotation with a full stop, no dash.

The "only one" claim survives an exhaustive sweep. Of the 46 em-dash lines, the player-facing ones are place titles (5089, 5203, 5280, 5291), HUD affixes (6301, 6306, 8009), the pulse warning at 3968, a dev-menu label at 985, the map splitter at 8113, and the milestone sign inscription 'WICK — 1/4 MILE.' at 5164. All of those are title/label separators matching sanctioned house style; 5164 is a signpost inscription, not spoken and not sentence punctuation. 5382 is the only em dash inside a dialogue line. I also checked the other four inline ballad quotations (5354, 5361, 5389, 5419): none spans a ballad line break, so there is no established convention that would justify the dash as a sung line-break marker, and the proposed comma fix is consistent with 135's own 'and set one on the sill,'.

Screenshot confirms it renders on screen: .claude/nnqc/skeptic10-shepherd-line2.png shows the dialogue box reading "...not for the man who came back down — but for the girl who never will."

Only correction is to the repro, not the claim: the URL as given lands on the narration line 'He is singing the last verse to nobody...'; append &nkey=e to page to the em-dash line. Severity polish is correct — a one-character prose-style violation with no mechanical effect.
     CORRECTION: Finding stands as written. Repro only: the given URL opens on the narration line; append &nkey=e (i.e. ...&ndev=lane&nfr=240&ndlg=shepherd&nkey=e&hz=1) to page to the line containing the em dash.
  --repro-- refuted=False conf=high
     CODE: comp/ninth.js:5382 is exactly as quoted, inside NPCS.shepherd.talk (block at 5375-5390): ['The shepherd', '"...not for the man who came back down — but for the girl who never will."']. I dumped the code points on that line with node: the only non-ASCII character is at index 81, U+2014 EM DASH. There are zero U+2013 en dashes in the whole file. So the character and the location are as claimed.

REACH: the shepherd is unconditional. PLACES.lane (comp/ninth.js:5152) has npcs: ['shepherd'] with no gate, and the square's exit to it (comp/ninth.js:5105, { x: 8.5, y: 14.3, w: 3, to: 'lane' }) has no `needs` and no `shut`, unlike the a3sq exit two lines below. interactables() (6212) pushes every id in p.npcs unconditionally, and doInteract() (6240) calls openDialog(o.n.talk(), ...). The talk function's first branch is guarded only by !S.seen.shep1, i.e. the first conversation. So this line is on the critical path of a cold save: one ungated exit from the hub, one of seven NPCs, first thing he says after the narration line.

REPRO, two ways, both captured:
1. .claude/nnqc/r2-f10-shep-line2.png (ndlg=shepherd&nkey=e) - the finding's own repro.
2. .claude/nnqc/r2-f10-shep-realE.png - the honest one, no ndlg at all: ?nwipe=1&ndev=lane&nat=9.2,5.6&nfr=300&nkey=e,e&hz=1, i.e. stand next to him and press E twice through the real keydown chain. Same result. Dialogue box reads "The shepherd / "...not for the man who came back down — but for the girl who never will."" with a visible long dash. Text reaches the DOM via showDialog() (6283) as esc(ln[1]) into innerHTML; esc() escapes only &<>"' so U+2014 passes through untouched.

THE "ONLY ONE" CLAIM: I classified all 34 U+2014 hits. Comments account for most. Player-facing string hits are 985 (dev-menu 'Wipe save — click again'), 3968 (HUD 'PULSE INCOMING — SPEND YOUR STACKS'), 5089/5203/5280/5291 (place titles), 5164 (the lane milestone look, 'WICK — 1/4 MILE.'), 6301/6306/8009 (prompt affixes), 8113 (a .split('—') on titles). None of those is sentence punctuation in spoken prose. 5382 is the only one. Claim holds.

PRECEDENT: the file's own convention for quoting ballad inside speech does not use a dash. BALLAD stanza 7 (135) holds 'not for the man who came back down' and 'but for the girl who never will.' as two separate array entries, as does the Fragment III reveal at 2884. The shepherd's own repeat line at 5389 quotes the half-couplet with a full stop after it. Strongest of all, the busker at 5402 runs three consecutive ballad lines into one spoken sentence joined by commas: 'he stood up, he spoke, we all heard, and he went alone.' The suggested comma fix is what the file already does everywhere else.
     CORRECTION: The finding is real, reachable and correctly rated polish. Two small slips in its rationale, neither of which changes the verdict, but which should not be copied into a fix commit:

1. "the busker at comp/ninth.js:5405 quotes across the break with a full stop" - 5405 is 'Word. "And we would not say a word." Because nobody answered her.', which is stanza 3's heard/word break, not the sill couplet. It is an analogous case, not the same couplet. The genuinely on-point precedent is comp/ninth.js:5402, where the busker runs three consecutive ballad lines into one spoken sentence with commas: 'he stood up, he spoke, we all heard, and he went alone.'

2. "every other hit is a place title, a HUD separator or a code comment" - there are two hits in neither of those three buckets: comp/ninth.js:5164, the lane milestone look text 'WICK — 1/4 MILE.', and comp/ninth.js:985, the dev-menu string 'Wipe save — click again to confirm'. Both are separators rather than sentence punctuation, and neither is spoken, so neither is a defect under the house rule, but the sweep as characterised is not exhaustive.

=== [11] survived 1/2  severityVotes=['polish']
  --correct-- refuted=False conf=high
     Core geometry re-derived and confirmed. isoXB(x,y)=560+(x-y)*29, isoYB(x,y)=84+(x+y)*14.5. placeBox(11,34) (ninth.js:70-74): x0=isoXB(0,34)-64=-490, x1=isoXB(11,0)+64=943, w=1433; y0=20, y1=isoYB(11,34)+29+64=829.5, h=810. camBounds (ninth.js:77-85): both axes exceed the viewport so x in [-490,-177] (313px travel), y in [20,250] (230px). All matches the finding.

Dynamics verified against camTarget (ninth.js:90-97), l=410 r=710 t=215 bm=365. Arrival from mark via gotoPlace (6136-6144) is (5.4,31.8) [not the finding's implied path detail; from hollow it is (5.4,2.2), not 1.9]. At (5.4,31.8) base x=-205.6, camera clamps to -490, player canvas x=284.4, y=387.9. Measured in my capture .claude/nnqc/sk11-road-south-arrive.png: (286,386). At the walkable NE corner (10.5,0.5) - reachable, moveActor clamps to 0.5..10.5 / 0.5..33.5 and no prop is near - camera pins at -177 and the player sits at canvas x=1027, 93px from the right edge. Measured exactly in .claude/nnqc/sk11-road-ne-zoom.png (hx=520 region, player at region x 507 = canvas 1027). At (5.4,17) the model predicts screen x 710; measured 712 in sk11-road-mid17.png. So the mechanism is real and the bounds maths is right.

Four supporting claims are wrong. (1) "pinned for the whole length": solving camTarget along the walk, c.x = isoXB - 710 while walking north, which is inside [-490,-177] for base x in [-80,233], i.e. world y 27.47 down to 6.33; the camera actually scrolls its entire 313px over y 17.1 to 6.33 and is pinned only at the two ends. (2) "the 300px dead zone never engages once": false. From y 27.47 to 17.1 walking north, camTarget returns c.x unchanged (-490) with no clamp active, so the player crosses the full dead box 410->710 over about ten tiles with the camera perfectly still - the dead zone doing exactly its job. (3) The base-x span between the cited points is 661.5-(-205.6)=867px, not 899, so the slide is 867-313=554px, not 586. (4) "inside the part of drawVignette that is 0.8 black": drawVignette (7864-7867) puts the .8 stop at radius 680 from (560,290); the player at (1027,215) is ~469 out, t~0.51, alpha ~0.28. Even the canvas corner is only ~0.67. The capture shows him and the ground under him clearly lit.

Also "screen corner" is a screen edge at mid height. Nothing is ever clipped or off screen; on the walking line x~5.4 the player stays between canvas x 284 and 873, never closer than 247px to an edge. Nothing malfunctions - it is a framing/composition regression on one place, worst at the two diagonal corners nobody has to visit, so polish rather than bug.
     CORRECTION: Real but overstated. Correct statement: camBounds clamps the eye to placeBox's bounding box, which for the 11x34 road is mostly empty triangle, leaving only 313px of horizontal camera travel against ~1300px of walkable base-x span. The camera does follow you for part of the road - it scrolls its whole 313px between world y 17.1 and 6.33, and the 300px dead zone genuinely engages for about ten tiles (y 27.5 to 17.1) with the camera still and unclamped. It is pinned at both ends: at the south arrival (5.4,31.8) the player is at canvas (284,388), and at the north end (5.4,2.2) at canvas x 830. The worst reachable case is the NE corner (10.5,0.5) at canvas (1027,215), 93px from the right screen edge - an edge, not a corner, and the vignette there is ~0.28 alpha, not 0.8. The player is never clipped or off screen. The real cost is that on the game's one long place, walking north (up-right in iso) pushes you toward the top-right so you see less ahead of you than behind, and the total slide across the screen over the walk is 554px.
  --repro-- refuted=True conf=high
     Served build check first: port 8492 is rooted at a DIFFERENT worktree (15587 lines vs this one's 8279), so I stood up my own static server on 8493 rooted at this worktree (scratch: .claude/nnqc/rr11-serve.js) and byte-verified /comp/ninth.js matched (md5 6868ebe2...). camBounds/camTarget/placeBox/DEAD_W and PLACES.road are byte-identical in both builds anyway.

ARITHMETIC IS CORRECT. comp/ninth.js:70-74 placeBox(11,34) = x -490 w 1433, y 20 h 810; comp/ninth.js:77-85 camBounds gives x0 -490, x1 -177 (313px of travel) and y0 20, y1 250 (230px). Re-derived independently in .claude/nnqc/rr11-cols.js. The endpoint screen positions the finding quotes are right too.

I THEN RAN THE WALK FOR REAL. .claude/nnqc/rr11-walk.html loads the game, does the devDemo arrival at (5.4,31.8) (which gotoPlace comp/ninth.js:6136-6143 does produce from the mark), holds W+D through the real root keydown chain with no keyup, and steps step()/draw() one 1/60 frame at a time recording RT.world.cam and the player's screen point. 525 frames, arrival to the north exit band. Result:
  screenX 286.1 .. 859.5, screenY 173.8 .. 387.1, camMovedFrames=313, insideDeadBoxFrames=130.
Southbound (hold S+A from (5.4,2.2)): screenX 828.1 .. 254.7, camMovedFrames=314, insideDeadBoxFrames=139.

So four load-bearing claims do not reproduce:

1. "pinned against its own bounds for the whole length". The camera moves on 313 of 525 frames (60%). Trace: pinned at x0=-490 for frames 1-215 (py 31.8->19.4), tracking continuously frames 216-460 (py 19.4->5.4, camx -490 -> -177), pinned at x1 for the last 65. Not "the whole length", and not "the first third" either.

2. "The 300px dead zone never engages once". The player is strictly inside the 300x150 box for 130 of 525 frames (25%) northbound, 139 southbound. During the tracking segment the camera follows because the player is pressing the box edge (sx holds at 724.9, the ~15px steady-state lag of the k=1-0.0016^dt ease at comp/ninth.js:104). That is the dead zone doing exactly its job.

3. "you walk into a screen corner". Vertically the player never leaves screenY 174..387 of 580. It is a screen side, not a corner. At the end of the northbound walk the player is at sx 859.5, 260px from the right edge; the "E — go to the hollow, north" prompt is drawn to their right and fits entirely on canvas (.claude/nnqc/rr11-road-N.png).

4. "inside the part of drawVignette that is 0.8 black". comp/ninth.js:7864-7867 is a two-circle gradient, inner (560,250) r250, outer (560,290) r680. At the NE corner point (1027,215) the stop parameter is ~0.50, i.e. alpha ~0.28. The 0.8 stop sits at radius 680; the canvas's own true corner is at 631, stop 0.886, alpha ~0.67. No canvas pixel is ever 0.8. .claude/nnqc/rr11-road-NE-z2.png (hz=2 on the player at 10.4,0.6) shows the figure fully drawn and legible in its own lantern pool.

THE CENTRAL ARGUMENT IS BACKWARDS. The finding says "the clamp is not even buying anything there: at the south end the ground occupies screen x 122..441 and the 679px to the right of it is outside the iso diamond and painted black regardless". I swept every canvas column for floor coverage at both clamped cameras (.claude/nnqc/rr11-cols.js): at cam (-490,250) the road floor occupies columns 64..1119 - it reaches the right edge, 0px of black to its right. Visible in .claude/nnqc/rr11-road-S.png, where the road's north half runs up into the top-right of the frame. At cam (-177,20) the floor occupies 0..1055 and the only black on the right is the 64px of FLOOR_PAD (the diamond's east corner (11,0) lands at canvas 1056). The clamp is buying almost the whole screen at both ends.

THE PROPOSED FIX IS STRICTLY WORSE. Evaluating the finding's own camBounds replacement on the road: wx = [isoXB(0.5,33.5), isoXB(10.5,0.5)] = [-397, 850], giving x0 -1107 and x1 440. At cam.x 440 the floor occupies columns 0..438 and 681px of the screen is void past the floor bitmap. It trades a 64px sliver of black for 681px, which is precisely what the camBounds comment at comp/ninth.js:75-76 exists to prevent.

REACH: the road is normal-play reachable (square -> lane -> village -> mark -> road) and every player walks it, so the 573px screen sweep is genuinely seen. But it causes no clipping, no illegibility and nothing off-screen. The extreme case the finding leans on, (10.5,0.5), is walkable (moveActor clamps to [0.5,10.5]x[0.5,33.5], blocked() clear) but has no prop, look, exit or NPC on it, and holding W drifts you west not east, so nothing routes a player there.

Scratch, all under .claude/nnqc/ and uncommitted: rr11-serve.js, rr11-walk.html, rr11-cols.js, rr11-road-S/M/N/NE.png, rr11-road-NE-z2.png. comp/* untouched.
     CORRECTION: What actually happens on the road, measured by holding W+D through the real keydown chain for the whole 30-tile walk: the camera is pinned at x0 for the first 215 frames, tracks the player for the next 245, and is pinned at x1 for the last 65. The player's screen x travels 286 -> 860 while the camera contributes 313 of the 886px of base-projection travel, so about 573px of the walk is the player crossing a static frame rather than the frame following. Screen y stays inside 174..387 throughout; the player never approaches a corner, is never clipped, and the exit prompt at the north end fits on canvas. This is the unavoidable consequence of a place whose walkable span is wider than its bitmap allows the eye to travel, and the clamp that causes it is keeping real floor on screen at both ends (floor columns 64..1119 at the south camera, 0..1055 at the north). Not a bug; at most a note that the road is the one place where the dead zone spends half the walk against a bound. The finding's fix must not be applied: it would put 681px of black void on screen at the road's north-east end.

=== [12] survived 2/2  severityVotes=['risk', 'risk']
  --correct-- refuted=False conf=high
     Every line cited checks out verbatim. endDialog (comp/ninth.js:6311-6317) does `var key = d.key; ... if (key) S.seen[key] = 1;` and d.key arrives only from openDialog's third arg (6274-6275), which doInteract supplies as o.l.key at 6247. A look's `key` field therefore has exactly one effect. Dimming is genuinely independent: doInteract sets S.looked[lookKey(o.l)] at 6245-6246, lookKey is `RT.place + ':' + l.n` (6092), and drawLooks reads it at 7877 -- so deleting a key changes nothing visible.

The five look keys are bernscript (5130), ledger (5148), markstone (5224), fenceend (5249), hollowground (5272). Only markstone has readers: checkMark 5964 (`if (!S.seen.markstone || !S.heard.hal) return;`) and checkSill 5981. askedTwo (5440) and askedLastLamp (5453) are set inside `do:` closures on chandler dialogue options that live in the one-shot `if (!S.seen.chandler1)` branch (5431), so they are one-shot writes with no reader.

I hunted for a guard elsewhere and found none. The only dynamic reads of S.seen in the whole file are testNeed (6111, `return !!S.seen[n]`), S.seen['been_'+id] (6119 write; 5452, 8077, 8090, 8094, 8106 read) and `delete S.seen[p.script + 'Intro']` (887, 6159). There are exactly three `needs:` values in PLACES -- a3ready 5108, rehearsed 5184, chorusDown 5198 -- all strings, all paired, none of them one of the six. No `needs:` function form is used in PLACES, so testNeed cannot be a hidden reader. No Object.keys/for-in over S.seen anywhere. ACH (236) and every ach() call site were checked: no achievement reads looks. Repo-wide grep over js/html/css confirms each of the six appears exactly once outside comments (ledger's other hits are a comment at 5305 and an unrelated local `var lg2` at 7642).

The intent claim is supported by the code shape, not just by vibes: README.md:77 says each fragment is "two facts you were told separately", and the three implemented pairs are heard.refrain + a true-line carrier (5938-5939), seen.markstone + heard.hal (5964), heard.widow + seen.markstone (5981). markstone is the only look plugged into that machine. Nothing in PARALLEL.md or README.md pre-accepts these keys the way arena/calm/boss are accepted, so this is not in the already-known set.

Severity "risk" is correct: no player-visible break, but a save flag written for a gate that was never wired.
     CORRECTION: Two factual corrections, both of which widen the finding rather than shrink it.

(1) "these six are the whole set of one-sided flags" is wrong. comp/ninth.js:5430 -- `S.heard.chandler = 1; sSave();` in the chandler's talk() -- is a seventh one-sided story flag. The only readers of S.heard are refrain (5938), child/busker/shepherd (5939), hal (5964) and widow (5981), plus the dev-menu toggles at 897 and 901. `chandler` has no reader. So it is seven flags, not six: four look keys, two dialogue-option flags, and one heard flag.

(2) The audit check offered in the fix does not close the hole it claims to close. It walks PLACES[id].looks[].key and exits[].needs, so it catches bernscript/ledger/fenceend/hollowground but is structurally blind to askedTwo and askedLastLamp (written inside `do:` closures on dialogue options, not in any data field the walk visits) and to S.heard.chandler. Since the finding itself lists those two dialogue flags as part of the defect, the proposed guard would let two of its own six through. A guard that actually works has to scan the raw source for `S.seen.<ident> =` and `S.heard.<ident> =` write sites and pair each against a read site, rather than walking the PLACES data structure.
  --repro-- refuted=False conf=high
     Ran the finding's own repro and then a stricter one, and it reproduces exactly.

1) The stated grep, `grep -n "bernscript\|ledger\|fenceend\|hollowground\|askedTwo\|askedLastLamp" comp/ninth.js`, returns exactly one live site per flag: comp/ninth.js:5130 (look 'His script', key 'bernscript'), :5148 ('The ledger', key 'ledger'), :5249 ('Where the fence ends', key 'fenceend'), :5272 ('The ground', key 'hollowground'), :5440 (`do: function (S) { S.seen.askedTwo = 1; }`), :5453 (`do: function (S) { S.seen.askedLastLamp = 1; }`). The only other hits are a prose comment at :5305 ("ledger four hundred years long") and an unrelated local `var lg2` at :7642. Neither is a reader.

2) Grep can miss dynamic access, so I enumerated every `S.seen.<ident>` with write/read classification plus every dynamic `S.seen[...]` site (script at /tmp/sk12.js). Result: the only dynamic sites in the whole file are :887 and :6159 `S.seen[p.script + 'Intro']` (write/delete), :6111 `return !!S.seen[n]` inside testNeed, :6119 `S.seen['been_' + id] = 1`, :6315 `S.seen[key] = 1` in endDialog, and :8077/:8090/:8094/:8106 `S.seen['been_' + ...]` in drawMap. testNeed at :6111 is the only dynamic *read* that could reach a look key, and the complete set of `needs:` strings in PLACES is three — 'a3ready' (:5108), 'rehearsed' (:5184), 'chorusDown' (:5198). So no dynamic path can ever read bernscript/ledger/fenceend/hollowground/askedTwo/askedLastLamp. The negative is proven, not asserted.

3) The comparison claims check out too. markstone is written only through the same dynamic :6315 and is read at :5964 (checkMark) and :5981 (checkSill). Every other named flag pairs: a3ready W[853,5318] R[5310], chorusDown W[3444] R[470,5497], openAll W[888] R[6104], rehearsed W[888,6376] R[6375], and bern1/child1/shep1/busk1/chandler1/millIntro/bernA3 are self-guards (read one line above their write). Six one-sided flags, exactly as claimed. The dimming of a read look is indeed a separate key: S.looked[lookKey(l)] written at :6246, read at :7877, and lookKey at :6092 is `RT.place + ':' + l.n` — no dependence on `key`.

4) "Written to the save" is literally true: sSave at :478 is `localStorage.setItem('comp_ninth', JSON.stringify(S))`, whole-object, and endDialog at :6316 calls sSave immediately after setting the flag.

REACH: the writes are ordinary play, not a save-state artifact. Captured .claude/nnqc/sk12-ledger-prompt.png (chandler, nat=6,7.1, nfr=300): the prompt reads "E — look at The ledger" with the player standing in front of it, so one keypress runs endDialog and sets S.seen.ledger. The chandler option 'And if somebody wanted two?' has no `if` guard at all, and 'Who lights the one out past the fence?' guards on `S.seen.been_road` (:5452), reachable as soon as you have been up the road. So a normal player sets all six.

Not covered by the accepted-dead-code carve-out either: PARALLEL.md:169 names only the three place fields arena/calm/boss. Look keys and dialogue `do:` flags are not listed anywhere in .claude/ninth-night/.

Severity: (risk) is the right level and should not be inflated. There is no player-visible symptom to find — the looks still print their prose, still dim, and no fragment, achievement or ending is gated on these flags (checkRealisation :5937 runs off S.heard.refrain plus child/busker/shepherd, checkMark :5962 off markstone plus S.heard.hal, checkSill :5979 off S.heard.widow plus markstone). Nothing is unreachable because of this.
     CORRECTION: Two corrections, neither of which refutes it.

(a) The finding's rhetoric overshoots slightly. "The two most loaded facts in the game do nothing mechanically" is true only of the flag: reading Bern's script and the ledger still prints the full text, still marks the look read through the separate S.looked key (:6246 / :7877), and the three fragment chains are fully wired through other flags. No content, fragment, achievement or ending is unreachable. The defect is dead persisted state and a `key:` field that implies wiring that does not exist, not a broken story gate — which is what (risk) already says.

(b) The proposed audit check does not catch its own finding. The snippet walks `PLACES[id].looks` and `PLACES[id].exits` only, so it builds `writes` from the four look keys and the three exit `needs` and never sees askedTwo/askedLastLamp, which live in NPCS.chandler.talk at comp/ninth.js:5440 and :5453 inside option `do:` functions. As written it would flag 4 of the 6 and let the dialogue half recur. It needs a second pass over the raw source for `S.seen.<name> = ` assignments (or over NPCS talk bodies), classifying each name as written-and-never-read, which is exactly what my /tmp/sk12.js enumeration does and which also correctly clears markstone.

=== [13] survived 2/2  severityVotes=['risk']
  --correct-- refuted=False conf=high
     Re-derived from source, not from the finding. comp/ninth.js:5108 is { x: 8.7, y: 6.6, w: 2.4, to: 'a3sq', needs: 'a3ready', over: 1 } with no h; exitAt (6061-6065) defaults w/h to 0.9 and tests x > e.x - w/2 && x < e.x + w/2 && y > e.y - h/2 && y < e.y + h/2, giving x in (7.5, 9.9), y in (6.15, 7.05). Band maths confirmed. comp/ninth.js:5102 puts the playbill at (7.2, 6.6): inside the band's y range, 0.30 west of its x edge.

Standability: blocked (6045) with r=0.3 against stagewip b:[6.4,3.6,4.6,2.6] (y ends 6.2) frees any y >= 6.5, so (7.2, 6.6) is legal and so is the whole strip east of it at y in [6.5, 7.05). No prop, no clamp, nothing stops the walk east.

Trigger path: stepTravel (6073-6081) is called from stepPlayer (1749), and step (3814-3828) calls stepPlayer unconditionally whenever not dead, so it runs in the square's calm phase. It calls gotoPlace(e.to) the instant exitAt returns an open exit with RT.armed true. No prompt, no key, no confirmation; I grepped the file for confirm/Are you sure and the only hit is the save-wipe button at 985.

Point of no return: exitOpen (6099-6102) has `if (e.over && S.a3.ending) return false;` before the S.seen.openAll bypass, so openAll does not lift it. a3sq (5279-5288) has exits: [] and oneway: 1. `stage` also has exits: [] but no place has an exit to it, so a3sq is the only walk-in place with no way back. S.seen.a3ready is set at 5318 in Bern's talk branch whose last line is 'The steps up to the stage are open.'

Prompt shadowing: nearestInteract (6229-6237) takes the nearest interactable within bd = 1.9. From (7.2, 6.6) the playbill is 0 away and the exit centre 1.5, so the playbill wins. Verified in capture .claude/nnqc/skep13-playbill-band.png (nat=7.2,6.6, nfrag=3, nfr=300): the prompt reads 'E — look at The playbill'. Note stepPlayer recomputes RT.prompt AFTER stepTravel, so on the triggering frame the prompt is already null in a3sq — the previous frame's prompt was the playbill, which is what the finding claims.

Audit hole: .claude/ninth-night/tools/audit-geometry.js lines 196-234 check standable point, buried %, unknown `to`, needs-without-shut, and look reachability. There is no look-vs-exit-band proximity check. Real hole.

Proposed fix arithmetic also checks: { x: 9.0, w: 1.6 } gives x 8.2..9.8, exactly 1.0 clearance from the playbill at 7.2, and y 6.5..7.05 remains standable past the stagewip.

Every line number cited in the finding resolves correctly (5102 playbill, 5108 exit, 5281 oneway, 5318 a3ready, 6065 band test, 6073 stepTravel, 6102 over/ending, 6231 bd=1.9).
     CORRECTION: The distance is 0.30 tiles, not 0.31 (hypot(max(0,|7.2-8.7|-1.2), max(0,|6.6-6.6|-0.45)) = 0.30). Everything else in the finding is correct, and three details make it sharper than written: (1) moveSpd is 3.5 (comp/ninth.js:166) and the WASD map (1732-1737) makes D world (+x,-y) at 0.707 each; moveActor's axis separation rejects the y component once ny < 6.5 (stagewip's south face at y 6.2 plus r 0.3), so you slide east along the wall while x climbs 0.0412/frame and cross x=7.5 on frame 8 — 0.133 s of holding D, or 17.4 screen px. (2) Bern lives at (7.4, 7.2) (comp/ninth.js:5308, no path/wander), 0.63 from the playbill, so the perpendicular bisector x + 3y < 28 confines the playbill's prompt to the north-west of him; at y ~ 6.6 the readable pocket is x 5.3..7.95 and its east edge IS the trigger edge — and Bern is the person you just talked to to set a3ready. (3) drawExits (7890-7907) draws only a 22px ring (scaled 1,0.5) at the exit CENTRE; the trigger edge is 1.2 tiles = 34.8 screen px west of it, so the only visual cue understates where the door starts. Severity 'risk' as filed is right: it needs the player to be standing on the look glyph in a fully-flagged endgame state, so it is a hazard rather than a guaranteed loss.
  --repro-- refuted=False conf=high
     REPRODUCED end to end with real key events, not the dev handle.

GEOMETRY (read, not taken on trust). comp/ninth.js:5108 is `{ x: 8.7, y: 6.6, w: 2.4, to: 'a3sq', ... needs: 'a3ready', over: 1 }` with no `h`. comp/ninth.js:6064 defaults `var w = e.w || 0.9, h = e.h || 0.9`, so exitAt's band is x in (7.5, 9.9), y in (6.15, 7.05). The playbill look is comp/ninth.js:5102, `{ x: 7.2, y: 6.6, n: 'The playbill' }`. Gap to the band's west edge = 0.30 tiles.

RAN IT. Probe .claude/nnqc/r13-reach.html loads the real game, dispatches real keydown/keyup on RT.root, and steps 1/60 between presses. Output in .claude/nnqc/r13-reach.png:
- 6 real 'e' presses beside Bern at (7.4, 8.0) set S.seen.a3ready = true (comp/ninth.js:5318).
- Placed at exactly (7.20, 6.60): RT.prompt = "look:look at The playbill", RT.armed = true.
- Held 'd' (screen east). Frame trace x = 7.241, 7.282, 7.324, 7.365, 7.406, 7.447, 7.489, then frame 8 fired gotoPlace. Log line: "*** TRAVELLED after 8 frames (0.133s of holding D) ... last prompt before it was look:look at The playbill". RT.place = 'a3sq', exits 0.
So: 0.133 seconds of one movement key, no prompt, no key press, no confirmation, and the on-screen prompt named the playbill on the last frame before the endgame. Exactly as claimed.

IRREVERSIBLE, verified rather than asserted. `over: 1` appears on exactly one exit in all 8279 lines (5108; the other `over:` hits are the PROP table's draw-order field). a3sq has `exits: []` (5288); `oneway: 1` is dead data, grep finds it only at 5281 and it is never read. drawMap (8067-8118) is display only, no fast travel. gotoPlace writes `S.place = id` and `sSave()` on the same frame (6118-6119) and init() does `gotoPlace(S.place, false)` (1576), so a page reload lands you back in a3sq: refreshing does not undo it. The author's own comment at 5626-5630 calls a3sq's exitlessness "a permanent softlock that survives a reload".

THE FINDING'S OWN REPRO URL ALSO REPRODUCES. canvas.html?...&nfrag=3&ndev=square&nat=7.2,6.6&nfr=300 renders "E - look at The playbill" with the player 0.30 tiles off the band (.claude/nnqc/r13-orig-repro.png). That repro only shows the prompt though: nfrag=3 does not set a3ready, so the exit is shut in it and no travel can occur. My .claude/nnqc/r13-shot.html sets a3ready through Bern first (r13-playbill-open.png, r13-playbill-zoom.png) - the pulsing gold ring drawExits paints at 7890-7907 is visible 1.5 tiles east, so there is a visual cue, but no confirmation step.

SWEEP. .claude/nnqc/r13-sweep.html sets RT.px/py and calls step(0) so the real nearestInteract computes the prompt, with blocked()'s rect test replicated from the square's prop list. 57 standable cells on a 0.2 grid read "look at The playbill". Of those: 2 within 0.25 tiles of the band, 6 within 0.50, 9 within 0.75, 12 within 1.00. Roughly 10% of the reading positions are in the hazard zone, which is the one thing that moderates the finding, but the playbill-prompt region runs continuously east until the band starts, so the true minimum separation is 0, not 0.30.

REACH IN NORMAL PLAY is easy, and easier than the finding argues. The gate is fragCount() === 3 plus Bern's line, and Bern's home is (7.4, 7.2), 0.63 tiles from the playbill and 0.15 tiles south of the band's own south edge, so the conversation that opens the door happens standing next to the door. More importantly, blocked() (6044-6051) treats every prop as solid with no solid flag, so the stagewip at [6.4, 3.6, 4.6, 2.6] stops the player at y >= 6.5 across x 6.1..11.3. Screen-east 'd' is (mx +1, my -1) at 1734-1737, so pressing east pins you to that wall and slides you along it: my trace shows y snapping 6.600 -> 6.518 and then running east. For x 7.5..9.9 the entire walkable strip between the stage wall (y 6.5) and the band's south edge (y 7.05) IS the band.

OPERATIONAL NOTE for whoever runs this next: the dev server on port 8492 serves the MAIN repo root, not this worktree. /.claude/nnqc/*.html 404s there. I served the worktree root on 8493 for the probes and shut it down afterwards.

Probes and shots left behind, nothing under comp/ touched:
C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/r13-reach.html (travel repro; r13-reach.png)
C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/r13-shot.html (captures with a3ready set; r13-playbill-open.png, r13-playbill-zoom.png)
C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/r13-sweep.html (prompt/band proximity sweep; r13-sweep.png)
C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/r13-orig-repro.png (the finding's own URL)
     CORRECTION: Real, and if anything understated. Two corrections. (1) The gap is 0.30 tiles, not 0.31: exitAt's west edge is 8.7 - 2.4/2 = 7.5 against the look's 7.2, and the y offset is zero. (2) "The playbill sits 0.3 tiles from the band" undersells it. The playbill-prompt region is continuous right up to the band, so the minimum separation is 0, and the real trap is the corridor: the stagewip prop is solid (blocked() has no solid flag, every prop blocks) and stops the player at y >= 6.5, so between the stage's south wall and the band's south edge at y 7.05 there is a 0.55-tile-wide strip which, for x 7.5..9.9, is entirely inside the exit band. Screen-east 'd' is (mx +1, my -1), which pins the player to that wall and slides them along it, so any eastward walk across the top of the hub square goes through the endgame door; the playbill is what draws you into that corridor, it is not the whole of the hazard. Measured: 8 frames, 0.133s of holding D from the playbill, prompt still reading "E - look at The playbill" on the frame before. Two things worth adding to the finding: gotoPlace sSave()s S.place on the same frame and init() reloads into S.place, so refreshing the page does not undo the accident; and the finding's stated repro only demonstrates the prompt, not the travel, because nfrag=3 alone leaves a3ready unset and the exit shut.

=== [14] survived 2/2  severityVotes=['bug', 'bug']
  --correct-- refuted=False conf=high
     The code is exactly as quoted. comp/ninth.js:1317 reads `var seed = 9; function fr() { seed = (seed * 1103515245 + 12345) >>> 0; return (seed >>> 8) / 16777216; }`. seed is a uint32 so seed*1103515245 reaches ~2^62, past the 53-bit mantissa, and the low bits are rounded away before `>>> 0` sees them. Reproduced the exact repro: `repeat at 5078 prev 4659 cycle 419`. The `Math.imul` fix: no repeated state in 300,000 draws. Confirmed.

No guard elsewhere. Every other PRNG in the file is correct 32-bit: hash2 at 6436-6442 uses Math.imul three times, seedRng at 6447-6450 is xorshift with shifts only. Line 1317 is the only raw `*` on a 32-bit LCG multiplier in the file, which makes it an oversight, not a choice.

I re-simulated fr() consumption from the source myself (tile pass at 1325 = gw*gh draws, then GROUND[kind]), counting fr() calls inside argument lists (blob's rx/ry/col args each consume one). Two independent numbers came out identical to the report: GROUND.town on 17x15 places 2681 cobbles from 929 distinct (rx, k) pairs, and the lattice is 60 cols x 54 rows = 3240 cells. The projection also checks out from source: TILE_W=58, TILE_H=29 at line 46, isoXB/isoYB at 58-59, so a lattice offset of (du,dv)=(-0.6,+0.9) is dx=(du-dv)*29=-43.5, dy=(du+dv)*14.5=+4.35, exactly the shift the report's bitmap sweep found a minimum at.

Two numbers in the report are wrong, both in the direction of understating it:

1. The report undercounts fr() draws because it misses the fr() calls inside blob()'s radius and colour arguments. GROUND.town's 90 mud blobs are 4 draws each, not 2, so town17x15 is 16368 draws not 16188 and town15x12 is 12311 not 12131 (both deltas are exactly 180 = 90*2, which is the signature of that miscount). Percentages barely move (72% and 62%).

2. The same miscount makes the road claim flatly wrong. GROUND.road's grit loop at 1134-1137 is `u=fr(), v=fr()` plus three more fr() inside the blob call (`1+fr()*2.2`, `0.7+fr()*1.2`, `fr()<0.5?...`) = 5 draws per blob. 900*5 = 4500, plus the 374-draw tile pass = 4874, already past the cycle entry at draw 4660 before the ruts, weeds or stones run. Full simulation: road11x34 consumes 6546 draws, 1887 of them (29%) from the 419-loop. The road is the longest walk in the game and it is affected too. Every other floor I re-simulated does come out clean and matches the report's range (mill17x17 3749, mill13x17 3681, mill15x13 3655, mill13x11 3603, hollow15x13 2195, loft13x13 1364, stage13x11 965, room12x9 598, room11x9 587).

One claim is overstated: "the cell stream repeats exactly every 178 cells from cell 2191 of 3240". It cannot repeat exactly, because the lattice cull test `if (fr() < 0.1 + d * 0.012) return` at 1106 depends on distance from the place centre, so per-cell consumption is not purely state-driven. Measured on my own replay: the PRNG state at lag 178 matches for 30.9% of cells, against 0.0% at lags 59, 60, 118, 119, 120, 177, 179, 236, 237, 238 (11.2% at 180). The matches come in long contiguous runs (218 cells from 1264, 173 from 1483, 125 from 2217, 214 from 2369). So the lag-178 structure is real and strongly isolated, but it is intermittent runs, not an exact repeat from a fixed cell.

Severity stays bug. The generator is genuinely broken, the fix is one line and exact, and it degrades the two town squares plus the road.
     CORRECTION: The PRNG defect and the fix are correct as written. Three numbers need fixing, and the road claim is backwards. (a) The report undercounts fr() consumption by missing the fr() calls inside blob()'s radius/colour argument lists. town17x15 is 16368 draws (not 16188) and town15x12 is 12311 (not 12131). (b) "road 11x34: 4583 draws, 76 short of the tail, clean by luck" is wrong. GROUND.road's grit loop (comp/ninth.js:1134-1137) burns 5 fr() per blob, so 900 grit = 4500 draws, and with the 374-draw tile pass the road is already at 4874 before the ruts even start, past the cycle entry at draw 4660. road11x34 consumes 6546 draws, 1887 of them (29%) from the 419-loop. So it is both town squares AND the road, which is the longest walk in the game. (c) "the cell stream repeats exactly every 178 cells from cell 2191 of 3240" should read: the PRNG state realigns at lag 178 for 30.9% of lattice cells, in long contiguous runs starting around cell 1264, against 0.0% at every neighbouring lag. It cannot be an exact repeat because the cull test at line 1106 (`fr() < 0.1 + d * 0.012`) makes per-cell consumption depend on position as well as state. The bitmap evidence and the (-43.5, +4.35) px shift derivation are unaffected and I re-derived both from TILE_W=58/TILE_H=29.
  --repro-- refuted=False conf=high
     Reproduced end to end, three independent ways.

1. CODE. comp/ninth.js:1317 is verbatim as quoted: `var seed = 9; function fr() { seed = (seed * 1103515245 + 12345) >>> 0; return (seed >>> 8) / 16777216; }`. seed is uint32 and the multiplier is ~2^30.04, so any seed above 2^53/1103515245 = ~8.16e6 (99.81% of the uint32 range) overflows the double mantissa and loses low bits before `>>> 0` runs. The finding's own repro command reproduces exactly: `repeat at 5078 prev 4659 cycle 419`.

2. REACH. I replayed each GROUND function's exact fr() consumption in node. town 17x15 = 16368 draws, of which 11709 (72%) come after the 4659-draw tail, i.e. out of the 419-value loop. square (ninth.js:5090) and a3sq (ninth.js:5281) are both `floor: 'town', w: 17, h: 15`, sharing key town17x15. village/Grelling (5204, town 15x12) = 12311 draws, 7652 (62%) in the loop. This is the hub, unavoidable, and the ground is plainly visible in a normal capture (.claude/nnqc/sk14-square-band.png).

3. VISUAL MECHANISM, derived independently. Replaying lattice(f,0.3,0.2) cell by cell for town 17x15: 3240 cells, 60 columns per row. Comparing each cell's (jitter_u, jitter_v, skip, rx, k) against the cell 178 later gives 938/2101 = 44.6% bit-identical duplicates, with whole 60-cell rows at 100% (v = 6.1, 6.4, 7.0, 7.3, 8.5, 10.6, 10.9, 11.5, 11.8, 12.1). 178 = 3*60 - 2, so the copy sits at world (du,dv) = (-0.6, +0.9) = screen (-43.5, +4.35) px, which is not a tile-lattice vector. I also measured 2681 cobbles drawn from 929 distinct (rx,k) pairs, matching the finding digit for digit.

4. BITMAP, my own probe with a proper control (.claude/nnqc/sk14-probe.html, output in .claude/nnqc/_sk14dom.txt). Sampling constant-v world rows rather than a screen rectangle, mean absolute luminance difference under shift, dx -60..-20, dy 0..8:
  v 6.1..7.3 (predicted duplicate rows): sharp isolated minimum 9.63 at (-43,4), 9.68 at (-44,4), against 14.3..15.2 across the rest of the sweep.
  v 10.6..12.1 (predicted duplicate rows): 11.05 at (-43,4) and (-44,4), against 14.5..15.6.
  v 1.0..3.5 (control, drawn before the state enters the cycle): 14.68 at (-43,4), no minimum there at all; that sweep's own min is 14.30 at (-53,0), i.e. noise.
That is a 35% contrast spike at exactly the offset the arithmetic predicts, absent from the control.

Nothing here needs a save state, a debug flag or an input that does not exist. Every player who walks into Wick's square is looking at ground made from 419 numbers.
     CORRECTION: Three numbers in the finding are wrong, and all three understate it.

(a) Draw counts are 180 low for both town keys. The finding counted the 90-iteration mud loop in GROUND.town as 2 draws per iteration, but `blob(g, f.px(u2,v2), f.py(u2,v2), 6 + fr()*12, 3 + fr()*6, ...)` is 4. Correct: town 17x15 = 16368 draws, 11709 (72%) in the loop; town 15x12 = 12311 draws, 7652 (62%). Conclusion unchanged.

(b) "road 11x34: 4583 draws, 76 short of the tail, clean by luck" is false. GROUND.road's grit loop (comp/ninth.js:1134) is 900 iterations of 5 draws each (u, v, `1+fr()*2.2`, `0.7+fr()*1.2`, `fr()<0.5?...`). 374 draws from pass one + 4500 = 4874, already 215 past the 4659 tail before the fence, weeds or frost stones start. Full count 6546 draws, 1887 (29%) from the 419-loop. The road, the longest walk in the game, is affected too.

(c) The duplication is not the uniform "repeats exactly every 178 cells from cell 2191 of 3240" the finding describes. Because the skip test `fr() < 0.1 + d*0.012` depends on the cell's position, alignment breaks and resyncs, so it bands: 0% for v <= 5.2, then 93-100% for v 5.8..7.6, 0% at v 7.9, 87-100% for v 8.5..9.1, 0% for v 9.4..10.3, 100% for v 10.6..12.1, then 0% from v 12.4 south. The first cell drawing from inside the cycle is 961, not 2191. So the affected area starts further north and is larger than claimed, and the finding's stated band "v about 12..14" is mislabelled: v 12.7..15.4 is clean, the duplicated rows are the walkable middle of the square. The finding's control region (a screen rectangle anchored at px(4,3)) actually overlaps duplicated rows v 5.8..9.1, so its control was weaker than it read; my constant-v control at v 1..3.5 confirms the conclusion anyway.

Severity stays at bug. The LCG objectively does not have the period it is written to have, the texture consequence is measurable at 35% contrast against a clean control, and the fix is the one line the finding gives (`Math.imul`). It is a faint artifact rather than obvious tiling, which the finding itself says, so it is not "broken" - but it is a real defect, not taste.

=== [15] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     OUTCOME CONFIRMED, MECHANISM WRONG.

1) The tile pass, re-derived. comp/ninth.js:1325-1331: sx=isoXB(x,y)-box.x, sy=isoYB(x,y)-box.y, then moveTo(sx,sy) -> (sx+29, sy+14.5) -> (sx, sy+29) -> (sx-29, sy+14.5). isoXB(x+1,y)=isoXB(x,y)+29 and isoYB(x+1,y)=isoYB(x,y)+14.5; isoXB(x+1,y+1)=isoXB(x,y) and isoYB(x+1,y+1)=isoYB(x,y)+29. So the diamond corners are world (x,y),(x+1,y),(x+1,y+1),(x,y+1): tile (x,y) covers [x,x+1]x[y,y+1] and the TILE EDGES ARE AT INTEGER world coordinates. The finding's premise ("the diamond covers world [x-0.5,x+0.5] x [y-0.5,y+0.5], so the tile edges are the lines u=k+0.5 and v=k+0.5") is exactly backwards, and contradicts finding [19] in the same file, which gets the projection right.

2) The real cause is one term, comp/ninth.js:1353: `py: function (u, v) { return isoYB(u, v) - box.y + TILE_H / 2; }` against `px` at :1352 which has no matching term. A pure screen shift of (0,+14.5) inverts to world (+0.5,+0.5) (isoX unchanged forces dx=dy; isoY += (dx+dy)*14.5 = 14.5 forces dx+dy=1). So f.px/f.py(u,v) is the projection of the CENTRE of tile (u,v), and GROUND.hollow's integer loops at :1230 and :1234 rule the centres. This is the same TILE_H/2 that finding [19] is about; [19] already lists :1353 as one of its sites.

3) Measured independently (my probe C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk15-probe.html, output in .claude/nnqc/sk15-dom.html; hollow bitmap 940x563, box.x=119 box.y=20, u==v column at bitmap x=441). Down that column, the darkest pixel is within 1px of the predicted tile corner 64+29k for every k=1..11 (92.14, 121.10, 150.37, 179.35, 208.56, 237.47, 266.25, 295.50, 324.33, 353.31, 382.50) and the brightest is within 1px of the predicted tile-centre 78.5+29k (107, 136, 165, 193.99, 223, 252, 281, 310.01, 339, 367.99, 397). 14.5px, half a tile, exactly as claimed. Their arithmetic reproduces.

4) Their clip claim is wrong. The shape[] polygon at :1339-1344 has corners at world (0,0),(gw,0),(gw,gh),(0,gh): isoXB(gw-1,0)+29 = isoXB(gw,0), isoYB(gw-1,0)+14.5 = isoYB(gw,0), isoXB(gw-1,gh-1) = isoXB(gw,gh), isoYB(gw-1,gh-1)+29 = isoYB(gw,gh). Verified numerically (shape[1]=876,281.5 = BX/BY(15,0); shape[2]=499,470 = BX/BY(15,13)). The clip ends at world y = gh = 13, not gh-0.5 = 12.5. The v=13 line is nevertheless at world y = 13.5, still outside: sampled along it at world (2..13, 13.5) the bitmap reads lum 6.8-8.1, i.e. the bare '#07060a' fill (lum 6.75), while the v=12 line at world y=12.5 reads 20.6-22.7. So "the last line never lands" survives, with different numbers.

5) North strip confirmed: at world (6, 0.0) lum 10.8 (the tile seam), at (6, 0.5) lum 23.1 (the first frost line), flat 14.0-14.9 between. But the finding is incomplete here: the last landing line is world y=12.5 / x=14.5 against a floor that runs to 13 / 15, so the unruled half-tile strip is on ALL FOUR sides, not just north and west.

6) Their fix is correct despite the wrong derivation: v = -0.5 .. gh-0.5 maps through py to world y = 0 .. gh, exactly the tile edges, because the -0.5 in the index cancels the +0.5 that py adds.

7) Severity. Purely cosmetic, one ground painter, the darkest place in the game, no gameplay or state effect. Measured (.claude/nnqc/sk15-probe2.html): parity checkerboard amplitude 3.43 lum at tile quarter points; averaged across world x=3..11, the tile seams at world y=4 and y=5 read 21.37 and 20.28 against a ~25 local mean, and the frost line at y=4.5 reads 35.94. So both lattices are measurable and the doubling is real, but the black one is about half the amplitude of the white one. At hz=1 in play (.claude/nnqc/sk15-hollow-z1.png) the hollow just reads as a faint ruled diamond field; you need hz=4 (.claude/nnqc/sk15-hollow-z4.png) or the brightness boost round one used to see the offset. That is polish, not bug.
     CORRECTION: Real, and the measurements are right, but the mechanism as written is backwards and the clip figure is wrong. buildFloor's tile loop (comp/ninth.js:1325-1331) draws index (x,y) with corners at world (x,y),(x+1,y),(x+1,y+1),(x,y+1), so a tile covers [x,x+1]x[y,y+1] and the tile edges are at INTEGER world coordinates. GROUND.hollow's integer loops would therefore be exactly on the edges. What moves them is one term in buildFloor's own helper at comp/ninth.js:1353, `py: isoYB(u,v) - box.y + TILE_H / 2`, with no matching term in px at :1352: a pure +14.5px screen-y shift is world (+0.5,+0.5), so f.px/f.py(u,v) is the projection of the CENTRE of tile (u,v). That is the same TILE_H/2 constant finding [19] is about (and [19] already lists :1353), so this finding as written contradicts [19]'s correct derivation. The clip polygon at :1339-1344 is the full world quad (0,0)-(gw,0)-(gw,gh)-(0,gh), ending at world y = gh = 13, not gh-0.5 = 12.5; the v=gh line still misses because it sits at world y = gh+0.5. The unruled half-tile strip is on all four sides, not just north and west. The proposed fix is correct as written. Severity is polish, not bug: the tile-seam dip measures about 5 lum against a 25 lum local mean and the frost peak about +10, so the doubling is real but only legible at 4x zoom or with brightness boosted; at 1x the hollow reads as intended.
  --repro-- refuted=False conf=high
     Reproduced independently. Note: the shared dev server on :8492 serves a DIFFERENT worktree (its comp/ninth.js is 872028 bytes / 15587 lines; this worktree's is 475774 / 8279), so I ran my own static server on :8593 and confirmed md5 6868ebe295bd3edae01bff7afb8f61c0 matched comp/ninth.js before measuring. Probe: .claude/nnqc/sk15b-reach-probe.html; captures sk15b-live.png, sk15b-hollow-z4a.png, sk15b-hollow-z1.png.

CODE READ. ninth.js:1230-1237 GROUND.hollow loops `for (var v = 0; v <= f.gh; v++)` / `for (var u = 0; u <= f.gw; u++)` drawing via f.px/f.py. ninth.js:1325-1336 tile pass. ninth.js:1338-1354 clip polygon + the px/py helpers. ninth.js:58-59 isoXB/isoYB.

THE FINDING'S MECHANISM IS MIS-DERIVED. It asserts tile (x,y)'s diamond "covers world [x-0.5,x+0.5] x [y-0.5,y+0.5]". False. The four corners at :1329-1330 are (sx,sy),(sx+29,sy+14.5),(sx,sy+29),(sx-29,sy+14.5) with sx,sy=isoXB/isoYB(x,y); I checked each against isoXB/isoYB and they are exactly the projections of (x,y),(x+1,y),(x+1,y+1),(x,y+1), i.e. base world [x,x+1]x[y,y+1], edges on INTEGER world lines. Finding [19] states this correctly, so _recovered.txt contradicts itself. The real cause, which the finding never mentions, is ninth.js:1353 `py: function (u, v) { return isoYB(u, v) - box.y + TILE_H / 2; }` — a +14.5px drop with dx=0 solves to world (+0.5,+0.5), so a GROUND line at v=k lands on base world y=k+0.5 (the tile-row centre). Both errors are +0.5 in the same direction and cancel, so the 14.5px number, the conclusion and the proposed fix are all still correct.

PIXELS (my own probe, not round one's). Hollow, cut along world y at world x=3.0, well clear of the centre gradient: bright spikes at y=2.500 lum 45.2, 3.500 41.8, 4.500 46.8, 5.500 39.6 against a ~22 baseline; dark dips at integer y (3.000=16.4, 5.000=16.9). CONTROL mill 15x13 (same box, no ruled lattice), identical cut: no half-integer spikes, same dips at integer y (3.000=24.3, 5.000=24.9) — so the tile-edge darkening is at integer world y in both floors and only the hollow has anything at half-integer y. 200k-sample phase binning of world-y within its tile row, centre bowl and outer ring excluded: hollow 27.73 in [0.375,0.5) and 25.60 in [0.5,0.625) against 21.9-23.5 elsewhere; mill flat at 33.3-35.8 across all eight bins.

SECONDARY CLAIMS BOTH HOLD. v=13 line lands on base world y=13.5 while the clip polygon (:1338-1343, corners = world (0,0),(gw,0),(gw,gh),(0,gh)) ends at y=13: 12 points sampled along it read lum 6.8-8.1, which is the #07060a background fill (lum 6.75), i.e. nothing drawn; v=12 reads 20.6-22.7. North edge at world x=6 walking y 0 to 1: 10.8 at y=0, ~14.0-14.9 through y=0.1-0.4, 23.1 at y=0.5 — first frost line is half a tile in, so the half-tile strip is real (though it sits inside the `edge===0` 45%-black ring at :1334, so it is near-invisible).

REACH. PLACES.hollow at :5261 (floor 'hollow', dark:1), entered by the road exit {x:5.4,y:0.7,w:3,to:'hollow'} at :5255, and it is where the ending is staged. Normal play, no dev handle needed. Visible in .claude/nnqc/sk15b-hollow-z4a.png and in the brightened live-frame crops in sk15b-live.png.

SEVERITY. No gameplay, readability or performance consequence (one wasted stroke per axis, once per floor build). The frost is ~+90% contrast, the tile lattice ~-27%, so what a player sees is one pale grid at half the intended pitch with no reference that would tell them it is wrong. Same class as [18] (drawn ground overhangs the walkable area), which round one graded polish. Real defect, wrong grade.
     CORRECTION: Keep the finding, fix its mechanism and drop it to polish. Corrected statement: buildFloor's tile pass draws tile (x,y) as the diamond whose corners are the projections of world (x,y),(x+1,y),(x+1,y+1),(x,y+1), so a tile covers base world [x,x+1]x[y,y+1] and its edges lie on INTEGER world lines — NOT [x-0.5,x+0.5] as the finding says (finding [19] gets this right; the document contradicts itself). The half-tile error comes from ninth.js:1353, where buildFloor hands GROUND painters `py(u,v) = isoYB(u,v) - box.y + TILE_H/2`. That +14.5px with dx=0 is world (+0.5,+0.5), so GROUND coordinates are tile-CENTRE coordinates (consistent with the contract comment at :1064-1068) and a line drawn at v=k lands on base world y=k+0.5. GROUND.hollow's `for (var v = 0; v <= f.gh; v++)` therefore rules the tile centres, 14.5px below the tile lattice, when its own comment at :1226-1228 says it "picks every edge out in white". Measured amplitudes are as claimed. The proposed fix (`v = -0.5; v <= f.gh - 0.5` and the same for u) is correct under the corrected derivation too, since (k-0.5)+0.5 = k; one caveat the finding does not mention is that the outermost lines then sit exactly on the clip polygon edge, so a 1px stroke there renders at roughly half alpha. Severity polish, not bug: nothing functional changes and no player can perceive the grid as mis-registered.

=== [16] REFUTED  severityVotes=['polish']
  --correct-- refuted=True conf=high
     The finding's *data* checks out; its *causation* and its *fix* do not.

WHAT IS TRUE (re-derived from source, not taken on trust)
- comp/ninth.js:1134 is a fixed `for (var i = 0; i < 900; i++)` grit loop; comp/ninth.js:1185 (420) and :1189 (340) in GROUND.mill are the same shape. GROUND.town does scale, via `lattice(f, 0.3, 0.2, ...)` at :1101.
- Grit is `1 + fr()*2.2` (rx 1..3.2) by `0.7 + fr()*1.2` (ry 0.7..1.9): E[area] = pi*2.1*1.3 = 8.58 px^2, so 900*8.58 = 7,722 px^2 (the finding says ~7,300; it is 6% low). Road is 11x34 = 374 tiles at TILE_W*TILE_H/2 = 58*29/2 = 841 px^2 = 314,534 px^2, so coverage is 2.45%, not 2.3%. Immaterial either way.
- road 374 tiles really is the largest (square/a3sq 255, arena 289, lane 221, mill 195). Town cobbles really are ~44-46% at rx 3.4..6, ry = rx*0.52.
- The road really does read as a diamond checkerboard in play: .claude/nnqc/sk16-road-z2-mid.png (ndev=road&nat=5,26&hz=2&hw=5,26).

WHAT IS FALSE: the mechanism
I measured the parity amplitude on the real floor bitmaps rather than reasoning about it. Probe at C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk16-floorprobe.html — hooks CanvasRenderingContext2D.prototype.drawImage before ninth.js loads, grabs the canvas buildFloor produced, then averages a 13x7 patch at every interior tile centre (tile (x,y) centre = isoXB(x,y)-box.x, isoYB(x,y)-box.y+TILE_H/2; box.x = isoXB(0,gh)-64, box.y = isoYB(0,0)-64) and splits by (x+y)%2. Nothing under comp/ was touched (git status comp/ clean).

  place        tiles  grit coverage   parity contrast
  road 11x34    374      2.45%          21.0%   (finding measured 20%: confirmed)
  lane 13x17    221     19.2%          21.3%
  arena 17x17   289     14.6%          22.9%
  mill 15x13    195     21.7%          19.9%
  mark 13x11    143     29.6%          17.5%
  loft 13x13    169       -            24.2%
  hollow 15x13  195       -            17.0%
  square/village/a3sq  44% opaque      11.0%

The lane has EIGHT TIMES the road's ground coverage and a slightly WORSE checkerboard (21.3% vs 21.0%). The mill place has nine times the coverage and 19.9%. Twelve times the coverage (mark) buys 3.5 points. The road at 21.0% is unremarkable — the loft, arena and lane are all worse. So "2.3% coverage is why the road reads as a checkerboard" is falsified by three places running the same fixed-count code at normal density. Visual confirmation: .claude/nnqc/sk16c-lane-z2.png and .claude/nnqc/sk16b-mill-z2.png and sk16b-mark-z2.png all show the same alternating diamonds as the road.

The real cause is one line the finding never mentions: comp/ninth.js:1331 `g.fillStyle = shadeHex(pal[(x + y) % 2 ? 0 : 1], 0.92 + fr() * 0.2)` — a deliberate two-tone parity on every floor. For road pal ['#2b2822','#22201b'] the channel means are 39.0 and 31.0, times mean shade 1.02 = 39.8 / 31.6, base amplitude 8.16; measured 7.49 after pass two and the `edge===1` darkening. The town escapes it not by scaling but because its cobbles are OPAQUE (`stones.add(x, y, rx, ry, shadeHex(f.pal[2], k))`, :1110, no alpha) — and town's palette has the SAME base amplitude as mill's (42.67 vs 34.0 = 8.67*1.02 = 8.84); 44% opaque coverage predicts 8.84*0.56 = 4.95 and it measures 5.00. That model, amplitude *= (1 - coverage*alpha), is what the whole table fits.

WHAT IS FALSE: the fix
Road grit is drawn at alpha .3/.4 (`rgba(96,88,72,.3)` / `rgba(20,17,13,.4)`), so coverage enters as coverage*0.35. The proposed `gw*gh*6` = 2244 blobs takes coverage 2.45% -> 6.1%, i.e. amplitude *= (1 - 0.061*0.35) = 0.979: contrast 21.0% -> ~20.5%. Half a percentage point. To reach the town's 11% you need coverage*alpha ~ 0.44, which at alpha 0.35 is 126% coverage — roughly 52,000 grit blobs — or opaque fills. The fix as written is a measured no-op against the symptom it claims to cure. Worse, 6 blobs/tile is BELOW the 900/121 = 7.4 blobs/tile the same code gives an 11x11 place, so it does not even restore the unscaled density at a normal size.

SECONDARY CLAIM (arena): arithmetically right — arena 289 tiles vs mark 143 with the same 420+340, so 14.6% vs 29.6%, "half as dressed" is correct. But arena is `{ n: 'Dev — free arena', ..., endless: 1, props: [], exits: [] }` at :5290, reachable only via S.scene === 'arena' (:434), and :523/:670/:710 all special-case it as paying nothing. Not player-facing.

I looked for a guard elsewhere and there is none that helps or hurts: drawLights (:7830) lays `rgba(6,5,14,.5)` over the road (night:1, not indoor), which halves the absolute amplitude to ~3.7/255 but leaves the ~20% relative contrast intact; the lantern's `lighter` pass and drawVignette only modulate it further. The checkerboard survives all of it, which the captures show.
     CORRECTION: The road's ground IS visibly checkerboarded, but not for the reason given and not distinctively. Corrected statement: every non-town floor shows the deliberate tile-parity two-tone from comp/ninth.js:1331 at 17-24% contrast — loft 24.2%, arena 22.9%, lane 21.3%, road 21.0%, mill 19.9%, mark 17.5%, hollow 17.0% (hollow's is explicitly intended, see the GROUND.hollow comment at :1240). The road is mid-table, not an outlier. The lane, with eight times the road's ground coverage, measures marginally worse. Only town (11.0%) escapes, and it escapes by laying ~44% OPAQUE cobbles, not by scaling detail count with area. The fixed 900/420/340 counts are real code facts but attenuate the parity by coverage*alpha, and road grit's alpha is 0.3-0.4, so the proposed `gw*gh*6` rescale moves the measured contrast from 21.0% to ~20.5%. If anyone wants to act on the visual, the levers are the parity palette delta at :1331 or opacity of the scatter, not the loop counts; and it is a whole-game item, not a road item. The arena half of the finding is arithmetically correct but concerns the dev-only place (props: [], exits: [], reachable only via S.scene='arena').
  --repro-- refuted=True conf=high
     CODE CHECK (all true as written). comp/ninth.js:1134 `for (var i = 0; i < 900; i++)` grit; :1185 `i < 420`; :1189 `c < 340`; GROUND.town scatters via lattice() at :1104. road is 11x34 (:5236). So the literal code claim holds.

HARNESS NOTE: port 8492 is serving a DIFFERENT worktree (served /comp/ninth.js md5 ebeb8d93... vs my 6868ebe2..., 23,866 diff lines). I diffed the GROUND block and it is byte-identical, then re-served my own tree on 127.0.0.1:8593 and took every capture and measurement from that.

REPRO: it reproduces as a *picture*. .claude/nnqc/sk16-road-mid-z2.png (road 5,26) and sk16-road-n-z2.png (road 5,10) both show a two-tone diamond checkerboard. So does .claude/nnqc/sk16-lane-z2.png and .claude/nnqc/sk16-mark-z2.png. That is the problem with the finding.

MEASUREMENT 1 - .claude/nnqc/sk16-checker.html hooks drawImage to grab each real floor bitmap and measures tile-parity Weber contrast (9 sample points per tile, edge ring excluded). Road is FIFTH of thirteen: loft 25.6%, arena 23.2%, lane 21.9%, mill 21.2%, road 20.6%, hollow 19.9%, mark 19.2%, stage 18.0%, chandler 17.1%, bernhouse 16.1%, square 11.1%, village 9.5%. (Road 40.75/33.14/ampl 7.61 — the finding's own 39.81/32.33/7.47 confirmed.)

MEASUREMENT 2 - same probe, composited on RT.cx after 300 real frames, lit, camera settled: mark 27.7%, road@5,10 23.8%, road@5,26 18.4%, mill 16.4%, hollow 12.6%, bernhouse 9.1%, village 8.2%, lane 6.9%, stage 6.1%, loft 5.5%, square 5.4%. The MARK is the worst checkerboard a player sees, and the mark is the SMALLEST mill place (13x11=143) getting the same 420 blobs, each ~85px^2 against the road's ~8.6 — roughly ten times the road's ink per tile. If low coverage caused the checkerboard, the mark would be the cleanest ground in the game. It is the dirtiest-looking one.

MEASUREMENT 3, the decisive one - .claude/nnqc/sk16-fixtest.html replicates buildFloor's tile pass (:1325-1336) and GROUND.road verbatim with the grit count as a parameter, validated by reproducing the real bitmap's contrast at 900 (21.9% replica vs 20.6% real). Amplitude by grit count: 0 -> 7.80, 900 -> 7.71, 2244 (the finding's exact proposed fix) -> 7.91, 6000 -> 8.01, 20000 -> 8.01. The proposed fix does not move the checkerboard, and neither does 22x it. Mechanism: the grit is half light rgba(96,88,72,.3) and half dark rgba(20,17,13,.4), uniformly distributed, so it tints both parities identically — count cannot cancel a parity term. The reason GROUND.town has no checkerboard is not that lattice() scales; it is that its cobbles are OPAQUE (shadeHex returns a solid hex at :1110-1111) and large enough to replace the tile shade. lattice() scaling is incidental.

REACH: road is the only place with floor 'road' (:5236), so "gets the same 900 a 5x5 would" describes a place that does not exist — the constant was tuned for the one 11x34 it draws. The arena comparison needs a dev-menu warp (:884-885) to an explicitly-furniture place hidden from the map (:8023); among places a player actually walks, the mill-kind span is mark 143 to lane 221, 1.55x, not 2x. And the tile parity itself is authored on purpose: the comment at :1320-1324 says the tile pass is "what keeps the iso grid legible... Kept deliberately quiet".
     CORRECTION: The checkerboard is real and visible on the road, but the finding attributes it to the wrong thing and proposes a fix that measurably does nothing. Corrected statement: every floor kind except 'town' shows the tile-parity checkerboard, because pass two everywhere else is low-alpha tint rather than opaque cover; the road ranks 5th of 13 for it on the raw bitmap and is beaten on screen by the mark, which has ten times its ground ink. Scaling GROUND.road's 900 to 2244 changes the measured parity amplitude from 7.71 to 7.91 (i.e. not at all; 20000 blobs gives 8.01). What survives is only that GROUND.road has the lowest detail energy in the game (within-parity sd 5.39/4.84, lowest of 13) — a taste note about one place's dressing, which the house rules exclude as "could be improved". Recommended disposition: drop. If anyone does want to bury the iso grid on the road, the change is opaque grid-breaking marks in the style of GROUND.town's cobbles, not more of the same transparent grit.

=== [18] REFUTED  severityVotes=[]
  --correct-- refuted=True conf=high
     The premise is an iso misreading. buildFloor's tile loop (comp/ninth.js:1325-1331) emits moveTo(sx,sy) -> (sx+29,sy+14.5) -> (sx,sy+29) -> (sx-29,sy+14.5) with sx,sy = isoXB/isoYB(x,y). Inverting all four through screenToWorld's own maths (comp/ninth.js:62-66, cam 0: a=(px-ORX)/29, b=(py-ORY)/14.5, wx=(a+b)/2, wy=(b-a)/2) gives world (x,y), (x+1,y), (x+1,y+1), (x,y+1). Tile (x,y) therefore covers world [x,x+1] x [y,y+1], NOT [x-0.5,x+0.5] x [y-0.5,y+0.5] as the finding claims. The tile pass paints world [0,gw] x [0,gh]; moveActor (comp/ninth.js:6055) clamps to [0.5,W-0.5] x [0.5,H-0.5]. That is a symmetric half-tile margin on all four sides. There is no one-tile disagreement and no side-asymmetry. The pass-two clip polygon (comp/ninth.js:1338-1343) resolves to the same rectangle: isoXB(gw-1,0)+TILE_W/2 = isoXB(gw,0), isoYB(gw-1,gh-1)+TILE_H = isoYB(gw,gh), isoXB(0,gh-1)-TILE_W/2 = isoXB(0,gh). So pass one, pass two and the clamp all agree.

The finding also contradicts finding [19] in the same file, which derives the identical eight lines correctly ("Those four points are exactly the projections of world (x,y), (x+1,y), (x+1,y+1), (x,y+1)").

The screenshots are real but show [19]'s defect, not this one. drawActor (comp/ninth.js:2111) is isoY(RT.px,RT.py) + TILE_H/2, a 14.5px straight-down shift equal to world (+0.5,+0.5). In mark (w:13 h:11, comp/ninth.js:5218) at the SE clamp (12.5,10.5) the player draws at the projection of (13,11), which is exactly the floor's south corner, so the feet land on the rim (gnd9-mark-SE.png, and the interact ring visibly hangs over in coll-square-southrim.png). At the NW clamp (0.5,0.5) the player draws at the projection of (1,1), 29px inside the corner at (0,0) (gnd9-mark-NW.png). Without the offset both would be 14.5px clear of the rim. The whole 29px NW-vs-SE difference the repro captures is the actor offset, which [19] already reports with the same square 8.5,14.5 repro and the same explanation.

Detail claims are also false: "column 0 and row 0 are entirely unreachable" is wrong, the centre reaches 0.5 which is the middle of tile 0, and with r=0.3 the body covers [0.2,0.8] of it; on the far side the body reaches gw-0.2, symmetric.

The proposed fix would break correct code: extending the loop to x <= gw and pushing the shape corners out would add a genuine extra tile ring beyond an already-correct boundary (1.5 tiles of overhang past the walkable centre limit on all sides), and would move the west/east corners to 58px against FLOOR_PAD = 64 (comp/ninth.js:68-73), eating the margin placeBox reserves for overhanging props.
     CORRECTION: Drop [18]. The ground and the movement clamp already agree: buildFloor paints world [0,gw] x [0,gh] (tile (x,y) is the diamond [x,x+1] x [y,y+1]) and moveActor clamps the centre to [0.5,W-0.5] x [0.5,H-0.5], a symmetric half-tile margin on every side, and the pass-two clip polygon matches exactly. The visible symptom the repro screenshots capture (feet on the rim at the SE clamp, a tile of ground beyond the feet at the NW clamp) is entirely the `+ TILE_H / 2` actor offset already reported as [19], which uses the same square 8.5,14.5 capture. Nothing here needs its own fix and the suggested fix to buildFloor would introduce the very overhang [18] complains about.
  --repro-- refuted=True conf=high
     MECHANISM CHECK (comp/ninth.js:1325-1331, buildFloor's tile loop)

The tile is drawn as moveTo(sx,sy) -> (sx+29, sy+14.5) -> (sx, sy+29) -> (sx-29, sy+14.5) with sx,sy = isoXB/isoYB(x,y). I inverted all four vertices through the projection's inverse (a = (px-560)/29, b = (py-84)/14.5, world = ((a+b)/2, (b-a)/2)):

  tile (0,0)   -> world corners (0,0) (1,0) (1,1) (0,1)
  tile (3,5)   -> world corners (3,5) (4,5) (4,6) (3,6)
  tile (12,10) -> world corners (12,10) (13,10) (13,11) (12,11)

Tiles are CORNER-anchored: tile (x,y) spans world [x,x+1] x [y,y+1]. Finding [18]'s central claim, "Tile (x,y)'s diamond covers world [x-0.5,x+0.5] x [y-0.5,y+0.5]", is false. Its consequence, "the tile pass paints world [-0.5, gw-0.5] x [-0.5, gh-0.5]", is therefore also false: the pass paints [0,gw] x [0,gh].

Against comp/ninth.js:6055 `nx = clamp(nx, 0.5, W - 0.5); ny = clamp(ny, 0.5, H - 0.5);` that is a half-tile margin on all four sides. Symmetric. There is no disagreement to reconcile.

Note that finding [19], four blocks further down the same file, states the corner-anchoring correctly ("Those four points are exactly the projections of world (x,y), (x+1,y), (x+1,y+1), (x,y+1)"). [18] and [19] contradict each other and [19] is the one that is right.

I also inverted the `shape` clip polygon (ninth.js:1338-1343, built from gw-1/gh-1 plus half-tile offsets). It comes back as exactly world (0,0) (17,0) (17,17) (0,17) for a 17x17 place: it already agrees with the tile pass exactly, so the finding's "push the two far corners out by one tile" would break a polygon that is currently correct.

Subsidiary claim also false: "column 0 and row 0 are entirely unreachable (the player's centre cannot get below 0.5, which is that tile's far edge)". Column 0 spans world x in [0,1]; x=0.5 is its MIDPOINT, not its far edge. With r=0.3 the player's body covers x in [0.2,0.8] of that tile. Column gw-1 is reachable to exactly the same depth.

EMPIRICAL CHECK

First: the dev server on port 8492 is not serving this worktree. It serves C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/suspicious-driscoll-00fbd8, whose comp/ninth.js is modified and 872,028 bytes against this worktree's 475,774 (md5 ebeb8d93... vs 6868ebe2...). My first three captures were of another agent's in-progress code. I discarded them, started python -m http.server on 8571 rooted at this worktree, confirmed the served comp/ninth.js md5 matched byte for byte, and re-took everything. Worth passing on: any skeptic capturing against 8492 right now is measuring the wrong file.

Probe: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk18-probe.html
Result: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk18-probe-arena3.png

It hosts the game, runs ndev=arena (17x17, zero props, night=false, so nothing occludes either corner) for 300 frames, reads the composited .nn-cv back with getImageData, and scans canvas column x = isoX of the world diagonal (isoX(k,k) = 560 for all k, so the north and south corners share one column). cam read from window.__ninth.RT().world.cam = {x:0, y:71.35}.

  measured first painted pixel from the top : y = 13
  measured last painted pixel from the bottom: y = 505

  model A (corner-anchored, mine): north = world (0,0) -> y 12.6 ; south = world (17,17) -> y 505.6
  model B (finding [18]):          north = world (-.5,-.5) -> y -1.9 ; south = world (16.5,16.5) -> y 491.1

  north error: A 0.4px, B 14.9px -> A
  south error: A 0.6px, B 13.9px -> A

Both ends land on model A within a pixel and miss model B by half a tile. The two models are 14.5px apart, so this is not a marginal call.

  gap north (northmost standable -> floor edge) = 14.1px = 0.488 tiles
  gap south (floor edge -> southmost standable) = 13.9px = 0.478 tiles

Symmetric. The finding's "the two do not agree by exactly one tile, and only on one side" does not reproduce.

WHAT THE ROUND-ONE SCREENSHOTS ACTUALLY SHOW

gnd9-mark-SE.png and gnd9-mark-NW.png are real and the asymmetry in them is real, but they cannot discriminate between the two models, because both predict the same actor-relative gap. The probe separates them:

  at (0.5,0.5)     feet drawn at y 41.6  ->  0.988 tiles inside the north edge
  at (16.5,16.5)   feet drawn at y 505.6 -> -0.002 tiles inside the south edge

That is the +TILE_H/2 at ninth.js:2111 and the other ~13 actor/ring/light/particle sites, i.e. finding [19], and nothing else. Confirmed visually on my own server at .claude/nnqc/sk18-arena-NW.png and .claude/nnqc/sk18-arena-SE.png.

WHY THE FIX IS HARMFUL (algebra, verified by script)

  today          painted [0,17]x[0,17] vs walkable [0.5,16.5]x[0.5,16.5] -> 0.5 N/W, 0.5 S/E
  proposed fix   painted [0,18]x[0,18] vs the same walkable rect          -> 0.5 N/W, 1.5 S/E

plus 29px of the 64px FLOOR_PAD consumed on each of the left and right extremes of placeBox, and an outer ring of tiles falling outside the clip polygon used by GROUND pass two.

REACHABILITY: moot, since nothing is wrong, but for completeness the geometry is place-independent (the derivation uses only gw/gh), so if it were real it would be visible in all 13 places from the first screen of the game.

comp/ untouched: git status --porcelain comp/ returns 0 lines. Scratch files are confined to .claude/nnqc/ (sk18-*). The temporary server on 8571 has been stopped.
     CORRECTION: Strike [18]. Its premise, its stated defect, and its proposed fix are all wrong; the only real thing in it is a symptom already filed, correctly, as [19].

Corrected statement: buildFloor's tile pass and moveActor's clamp agree, symmetrically, to within a pixel. The drawn ground is world [0,gw] x [0,gh] and the walkable rect is [0.5,gw-0.5] x [0.5,gh-0.5]: a half-tile margin on ALL FOUR sides. Measured 0.488 tiles north/west and 0.478 tiles south/east.

The one-tile-versus-nothing asymmetry the repro screenshots genuinely show is not the ground. It is the actor: drawActor/drawNpc render at isoY(x,y) + TILE_H/2, which in this projection is world (+0.5,+0.5). At the north clamp (0.5,0.5) the drawn feet land 0.988 tiles inside the ground edge; at the south clamp they land -0.002 tiles inside it, i.e. exactly on the brink. That is finding [19], already filed at (bug) with the right mechanism and the right fix. Delete [19]'s constant and the actor sits ~0.48 tiles from each edge, symmetrically, with no change to buildFloor at all.

[18]'s fix must not be applied. Looping to x <= gw / y <= gh makes the painted rect [0,gw+1] x [0,gh+1], which turns today's symmetric 0.5/0.5 margin into 0.5 north/west and 1.5 south/east: it creates the exact one-tile asymmetry the finding claims to remove, in the opposite direction. It also pushes a ring of tiles outside the `shape` clip polygon (so ground-detail pass two would stop short of the new outer ring) and spends 29 of the 64px FLOOR_PAD on each side, which exists for prop overhang.

=== [19] survived 2/2  severityVotes=['bug', 'bug']
  --correct-- refuted=False conf=high
     Re-derived from source, not from the finding. comp/ninth.js:59-61: isoY(x,y)=ORY+(x+y)*14.5-cam.y, so a straight-down 14.5px shift is exactly world (+0.5,+0.5). comp/ninth.js:1326-1331: buildFloor draws tile (x,y) as moveTo(sx,sy)->(sx+29,sy+14.5)->(sx,sy+29)->(sx-29,sy+14.5), which are the projections of world (x,y),(x+1,y),(x+1,y+1),(x,y+1) — so isoY with no offset is the true ground point. comp/ninth.js:6623-6627: drawProp takes isoX/isoY of the four corners of b raw and blits so the sprite footprint centre lands on myc=(y0+y2)/2 = the projection of the box centre; no offset. comp/ninth.js:6048: blocked() tests the raw actor (x,y) against that same rect. comp/ninth.js:2111 (drawActor), :7956 (drawNpc), :3479 (drawFoe) all compute sy=isoY(..)+TILE_H/2, and blit's contract at :1863 ("y is the ground under its feet") plus the shadow ellipse at :2117 (translate(sx,sy-bob), ellipse at (0,bob)) confirm that is the drawn ground contact. So the actor's rendered footing is world (+0.5,+0.5) from its collision point while the box it collides with is not. Every cited line number is exact (91, 1353, 2111, 3479, 6627, 7317, 7956 all verified).

No consistent reading rescues it. If actor coords were tile indices drawn at tile centre, blocked() would have to test (px+0.5, py+0.5); it tests px. Two independent cross-checks pick the no-offset space as the file's real world space: screenToWorld (:62-66) is the exact inverse of isoX/isoY with no offset (so RT.mouse.wx/wy and RT.moveTo set at :1660 are in the prop convention while the marker ring at :3867 and the walker are drawn 14.5px below the cursor), and lightsOf (:7813-7827) derives light positions straight out of prop boxes — including the sill lamp whose comment says "the painter and the light have to agree about this ... one formula, read by both" — after which drawLights:7841 adds +TILE_H/2 to them.

Guard check: coversSomeone (:6672-6683) does fade a prop that covers the player, but its test `if (sy > y1 || sy - a.h < y0) continue` skips any prop whose paint box top (-(hgt+over)) is below the 44px figure's head. For the bernhouse table (hgt 20, over 14, paintedBox y0 = myc-34) the player's head at sy-44 = myc-45.45 is above myc-34, so it never fires and the table draws at full alpha. So the guard covers houses and walls but not tables, counters, carts, wells or crates.

Reproduced myself with fresh captures at the exact collision limits, same framing both shots (.claude/nnqc/sk19-table-N.png, sk19-table-S.png; bernhouse, table b [4.2,3.4,2.6,1.6], nat=5.5,3.1 and nat=5.5,5.3, both exactly 0.3 tiles clear of the wood). North: only the crown and forehead clear the tabletop. South: a clean strip of floor between the player and the front face. Worked numbers agree: at the north limit the drawn feet are at world (6.0,3.6), 5.8px of screen y inside the footprint edge, hiding 24px of the 44px figure where a correct render would hide 10px; at the south limit the drawn feet are 0.8 tiles clear instead of 0.3.
     CORRECTION: Real and correctly mechanised; three details are off. (1) "approach a prop from the north and you are drawn 0.5 tiles INSIDE its footprint" overstates the absolute figure: the 0.3 collision radius eats most of the 0.5, so the maximum real penetration is 0.2 tiles per axis (0.4 tiles of k, 5.8px of screen y). The accurate phrasing is "half a tile further into it than collision believes" — which is what the finding's own worked example (0.15 tiles at y=3.05) actually computes. (2) buildFloor's ground-detail py() at :1353 is not a local patch of this bug: it is the tile-index-to-tile-centre helper the GROUND painters use for decorative floor detail, it never meets collision or a prop box, and changing it would only slide the decoration half a tile. Only PAINT.fence :7317 is a genuine compensating patch. (3) The site count is 17, not ~14: comp/ninth.js:91, 1403, 1427, 2111, 2809, 2991, 3479, 3780, 3867, 3937, 3952, 6676, 7841, 7875, 7893, 7956, 8008. Two consequences the finding does not list, both of which strengthen it: RT.moveTo comes from screenToWorld at :1660 so in click-to-move the marker ring and the walker both land half a tile south-east of the tile you clicked; and lightsOf builds light positions out of prop boxes which drawLights :7841 then shifts, so the sill lamp lands off the house it was measured from (partly masked by the ad-hoc translate(sx, sy-10)). One scope note: for tall props (house, wall, mill) coversSomeone does fire and fades them to T_CUT 0.56, so the worst full-opacity burial is on the low props — table, counter, cart, well, crate, sack.
  --repro-- refuted=False conf=high
     GEOMETRY, DERIVED INDEPENDENTLY. comp/ninth.js:1325-1331 draws tile (x,y) as moveTo(sx,sy) -> (sx+29,sy+14.5) -> (sx,sy+29) -> (sx-29,sy+14.5) with sx,sy = isoXB/isoYB(x,y). Those four points are isoXB/isoYB of world (x,y), (x+1,y), (x+1,y+1), (x,y+1) exactly, so isoY(x,y) with NO offset is the true ground point of world (x,y). comp/ninth.js:6624-6626 drawProp sets myc=(y0+y2)/2 = isoY(b0+b2/2, b1+b3/2), no offset, and contactShadow (:6695) plus body() (:6716) paint off c.x0..c.y3, which are the footprint corners about that origin. So a prop's painted base quad IS the AABB blocked() (:6044-6051) tests. comp/ninth.js:2111 drawActor uses sy = isoY(px,py)+TILE_H/2, and its foot shadow is ellipse(0,bob) under translate(sx, sy-bob), i.e. ground contact is exactly sy. +14.5px straight down = world (+0.5,+0.5) in this projection. The mismatch is real.

RUNTIME PROOF. I could not use the round-one URLs directly: port 8492 does not serve this worktree's .claude/nnqc (404), so I served the worktree on 8497 and wrote .claude/nnqc/sk19-reg.html, which after devDemo paints, on a SEPARATE overlay canvas, magenta = isoX/isoY(RT.px,RT.py), cyan = the same +TILE_H/2, green = every prop b as drawProp draws it, orange = b grown by the player radius 0.3. (The overlay is necessary: the game's rAF DOES run under --virtual-time-budget, contrary to TESTING.md, and wipes marks drawn on .nn-cv.) The probe's own title readback confirmed my hand-derived camera exactly: bernhouse cam=(29,-46.5), noOff=(602.0,254.5) for (5.5,3.05).

sk19-reg-bern-whole-table.png and sk19-reg-bern-north2.png, ndev=bernhouse&nat=5.5,3.1 (exactly the blocked() limit north of table b [4.2,3.4,2.6,1.6]): magenta sits exactly ON the orange grown box (collision is correct), cyan sits clearly INSIDE the green footprint, and every prop body is painted exactly on its green quad (table, lamp, bed, crate). Measured cyan-magenta = 84 image px at 6x = 14.0 canvas px, against 14.5 by arithmetic. The player is painted over to the shoulders while standing 0.3 tiles clear of the wood.

sk19-reg-bern-south.png, nat=5.5,5.3 (south collision limit): feet drawn at the projection of (6.0,5.8), 0.8 tiles clear of the same table. So the drawn stand-off differs by a full half tile between the two sides of one prop.

sk19-reg-bern-southrim.png, nat=1.5,8.5: cyan lands at canvas (328,290), which is exactly isoX/isoY(2.0,9.0), the floor's y=H boundary, with the void immediately below the boots.

Square repro checked numerically too: cam=(29,61), foot point (357,371) = projection of (9.0,15.0) = the rim. I confirmed the harness/camera mapping by measuring the actual rim in sk19-square-southrim.png: at image column 600 the floor/void step is at image y=360, and the prediction from cam=(29,61) with hx=230,hy=300,hz=4 is 360.

REACH. Trivially reachable in normal play. Bern's house is on the main path, its table is the largest thing in the room, and the 'His script' look sits at (5.4,5.2), 0.2 tiles south of the table, so the player is pushed against it from both sides. The rim case is reachable too: bernhouse y=8.5 for x in about 0.9..4.0 is outside the (5.5,8.3,w2.4) exit band and outside the crate, so you can walk there and stand with your feet on the edge of the world.
     CORRECTION: The finding stands; four details are off and one instruction in it would cause damage if followed literally.

1. It is 17 call sites, not "~14": comp/ninth.js:91, 1403, 1427, 2111, 2809, 2991, 3479, 3780, 3867, 3937, 3952, 6676, 7841, 7875, 7893, 7956, 8008 (plus :7317, which is finding [20]).

2. "approach a prop from the north and you are drawn 0.5 tiles INSIDE its footprint" is loose. At the collision limit it is 0.2 tiles inside from the north and a 0.8-tile gap from the south. The invariant worth stating is that the drawn stand-off from one prop differs by a full 0.5 tile between its north and south faces.

3. The square repro (nat=8.5,14.5) is a devDemo-only pose, not a player state: devDemo sets RT.armed=false, and the square's lane exit (8.5,14.3,w3, default h 0.9) covers y 13.85..14.75, so a walking player is sent to the lane before they can stand there. The phenomenon is still reachable; use Bern's house at y=8.5, x 0.9..4.0 instead (sk19-reg-bern-southrim.png).

4. The chandler repro is wander-phase dependent, not a fixed frame. Re-running ndev=chandler&nfr=340 through a slightly different harness put her clear of the counter; the NPC-cover case is properly established by finding [23]'s ring script, not by that single capture.

5. The fix note calls comp/ninth.js:1353 (buildFloor's ground-detail py) "a local patch" of the same bug. It is not. It is a legitimate "centre of tile (u,v)" helper for GROUND scatter, whose (u,v) are floats over the whole place; stripping TILE_H/2 from it would shift every ground detail in the game half a tile north for no reason. Leave :1353 alone. Only the actor/ring/light/particle/camera sites and :7317 move.

=== [20] survived 2/2  severityVotes=['bug']
  --correct-- refuted=False conf=high
     Re-derived the projection from source rather than from the finding. spriteCtx (comp/ninth.js:6455-6476): sk=(bw-bh)*14.5, rrx=(bw+bh)*14.5, rry=(bw+bh)*7.25, lx(u,q)=(u-q)*29-sk, ly(u,q)=(u+q)*14.5-rry. lx(bw/2,bh/2)=0 and ly(bw/2,bh/2)=0, so local (0,0) is the footprint centre, and c.x0..c.x3 are exactly the four footprint corners. drawProp (:6627) blits with the local origin at mxc,myc = ((x0+x2)/2,(y0+y2)/2) = isoX/isoY of world (b0+bw/2, b1+bh/2), no offset — I checked buildFloor's tile loop (:1325-1331) to confirm isoY(x,y) with no offset is the true ground point of world (x,y). contactShadow (:6703) paints the c.x0..c.x3 quad. PAINT.fence's at() (:7315-7318) returns lx/ly of the footprint midline plus TILE_H/2; a straight-down 14.5px screen shift is world (+0.5,+0.5), and since the shift is along the run for the y half, the net effect is the run moving +0.5 tiles perpendicular. All nine fences are north-south (bw=0.5 < bh: lane :5156 x3, mill :5179 x2, mark :5221 x2, road :5239 x2), so all nine move +0.5 in world x, i.e. 0.25 tiles past the far edge of a 0.5-tile-wide footprint. A grep for lx(/ly( returns only :7317 and PAINT.hedge at :7501-7510, and :7317 is the only TILE_H/2 inside the PAINT block.

Checked for a guard elsewhere: there is none. propSprite's canvas has room for the shift (bottom margin ~rry+30 vs needed rry+14.5), so nothing clips, which is why it has stayed invisible to the audit.

Reproduced twice against the running game rather than trusting the maths:
1. Pixel readback (.claude/nnqc/sk20-probe.html, read back from .nn-cv immediately after devDemo, with RT.world.cam read in the same tick). Mill west fence: picket columns paint down to d=+14 relative to the footprint midline; the between-picket columns show the rails at exactly foot-24 and foot-12 as PAINT.fence draws them, and the contact-shadow darkening ending at d=+10.
2. Overlay (.claude/nnqc/skc20-overlay.html, my own canvas pinned over .nn-cv and redrawn every frame with the live cam — the game's rAF loop does run under a virtual-time budget and wipes anything drawn once into .nn-cv, which is worth knowing for future probes). Green = footprint quad, magenta = footprint midline, cyan = midline+14.5. Measured in .claude/nnqc/skc20-mill-fence-ov.png: at two clean columns the lowest fence pixel is 15.4 and 14.4 canvas px below magenta, i.e. on the cyan line. In .claude/nnqc/skc20-post-only.png the neighbouring post's paint bottom is 4 canvas px below its footprint centre (body() with ins 0.42 predicts 3.65), so the post is on its footprint and the fence is not. .claude/nnqc/skc20-road-join-ov.png shows both at once: the pickets stand on the cyan line while the green footprint crosses them at mid-shin.

Provenance: git log -S shows the term arrived with 527e3a7 (the local-space painter rewrite), and the code it replaced drew its pickets at isoY(...)+TILE_H/2 while drawing its rail along the unshifted footprint edge. So it is carried-over code from a convention that no longer exists, not a deliberate patch for the actor offset as the finding guesses.

Note on the harness: port 8492 serves a different (longer, 15587-line) build; I diffed PAINT.fence, spriteCtx, drawProp, contactShadow and the fence/post data and they are byte-identical to this worktree, and I ran my own server on 8497 over this worktree for the probes.
     CORRECTION: PAINT.fence's `+ TILE_H / 2` (comp/ninth.js:7317) is real and is exactly a world (+0.5,+0.5) displacement of the whole fence off its own footprint, shadow and collision box. Two things in the write-up are wrong or missing.

(1) The shadow separation is overstated ~3x. 14.5px is the CENTRE-to-centre offset, not the gap. contactShadow (:6695-6708) paints the footprint quad scaled 1.0/1.09/1.18 and translated (0,+2), so at a fixed screen column the darkening runs from about d=-6 to d=+10 relative to the footprint midline, and the picket feet are at d=+14.5. Measured on the mill west fence (b [0.6,2,0.5,8], a column between pickets): floor lum 13 -> rails at d=-10..-8 -> shadow 8..10 at d=-6..+9 -> lower rail at d=+2..+5 -> back to floor lum 16 at d=+11; picket columns paint down to d=+14. So the clear strip of undarkened floor between the bottom of the shadow and the bottom of the pickets is ~4-5px, not a full sliver-width, and the shadow band is not "clear of the fence" at all: it crosses the pickets' lower ~16px (it is painted first, so the pickets are drawn over it). What a screenshot shows is a fence standing just south of a dark strip, not a fence floating a sliver's width above one.

(2) "off its collision box" is true only in raw world coordinates and is the one part a player cannot see. blocked() (:6044-6051) tests the raw box, but drawActor draws the player at isoY+TILE_H/2 as well (finding [19]), so on screen the +14.5 is what makes the fence look like it stops you where it stops you; without it the fence would read as blocking you a full tile early. The finding's own fix note says as much; the title should not list the collision box as a visible symptom.

Two consequences the finding misses, both stronger evidence than the shadow:
- comp/ninth.js:5239-5240, the road: fence b [9.7,23.6,0.5,8.4] and post b [9.6,22.4,0.6,0.6]. PAINT.post (:7350) draws through body() on the unshifted footprint (verified: its lowest paint sits rry*ins = 3.65px below the footprint centre). The unshifted fence midline is x=9.95 and the post centre is x=9.90 — authored collinear to 0.05 tiles. The shift puts the drawn fence line at x=10.45, i.e. 0.55 tiles = 16 screen px below the post's ground point at a fixed column, and doubles the along-run gap from 0.6 to 1.1 tiles. That is "Where the fence ends" (:5249), the look the whole road is built around. Same story on the west pair, and the mill/road side fences, symmetric about the place centre in the data (0.85/14.15 in w=15; 1.05/9.95 in w=11), land a full tile asymmetric on screen.
- PAINT.hedge (:7493-7513) is the only other painter that uses c.lx/c.ly with the same "run along the long axis" loop, and it does not add the constant. The road has a hedge (b [8.4,20.2,1.6,1.2]) twenty tiles from the fence: the two agree about the footprint everywhere except that one term.

Minor adjacent fact, not part of the claim: paintedBox (:6510) declares the paint stops at rry+4, but a fence now paints to rry+14.5, so coversSomeone under-measures fence sprites by 10.5px.
  --repro-- refuted=False conf=high
     HARNESS NOTE FIRST: the dev server on port 8492 is NOT serving this worktree. /comp/ninth.js from it is 872,028 bytes and does not match this worktree's 475,774-byte file (it has a zoom-punch camera and a TUNE block full of punch/detonation entries that do not exist here); /.claude/nnqc/ 404s there. Every capture below is from my own python http.server on 127.0.0.1:8571 rooted at C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af, md5-verified against comp/ninth.js. Any other skeptic on 8492 is auditing someone else's build.

CODE. comp/ninth.js:7317 is exactly as quoted, inside PAINT.fence's at(): `return [c.lx(u, v), c.ly(u, v) + TILE_H / 2];`. spriteCtx (6455-6468) defines lx(u,q) = (u-q)*29 - sk and ly(u,q) = (u+q)*14.5 - rry with sk=(bw-bh)*14.5, rry=(bw+bh)*7.25; lx/ly(0,0) = (-sk,-rry) = (c.x0,c.y0) and lx/ly(bw,0) = (rrx,(bw-bh)*7.25) = (c.x1,c.y1), so lx/ly land the footprint corners exactly on the quad contactShadow paints (6695-6709) — the same quad blocked() tests (6047). drawProp (6627) puts local (0,0) at (isoX/isoY of the footprint centre in corner coords), so the convention is unambiguous. grep for TILE_H/2 confirms 7317 is the only PAINT.* hit.

The decisive internal evidence the finding did not use: PAINT.hedge (7501-7502) is the same construction, `long ? c.lx(c.bw*q, c.bh/2) : c.lx(c.bw/2, c.bh*q)` with `c.ly(...)` RAW. The fence is the only painter of that shape that adds the offset, so this is a local anomaly and not a convention.

MEASURED, not eyeballed. I built .claude/nnqc/sk20-overlay.html (a copy of canvas.html that hangs green/cyan hairlines from each post foot predicted with and without the offset, on a separate canvas laid over the game's — the game's own canvas is wiped every frame because headless=new DOES run rAF under --virtual-time-budget). Mill east fence b=[13.9,2,0.5,8], hz=8, ndev=mill&nat=12.6,6.5. Post columns isolated by subtracting the background columns either side (.claude/nnqc/sk20-mill-clean8.png vs sk20-mill-ticks.png, 16 image px per game px):
  postCol 853  foot-cyanTop  -1 img px   foot-greenTop +223
  postCol 1077 foot-cyanTop  -1          foot-greenTop +239
  postCol 1285 foot-cyanTop  -1          foot-greenTop +213
  postCol 1509 foot-cyanTop +15          foot-greenTop +117 (partly off frame)
Median foot - cyan = -0.06 game px; median foot - green = +13.3 game px. The post feet stand on the +TILE_H/2 line to within a pixel, 14.5px south of their own footprint centre line. Reproduced.

VISIBLE. Luminance profile across the footprint at post-free columns of sk20-mill-clean8.png: col 303 reads 45-50 north of the band, 25-28 through the 232-image-px band, back up to 30-31 south of it — the contact shadow is a real dark band on the floor, and the feet at band+232 are on the lighter floor below it. .claude/nnqc/sk20-road-end-z4-lift.png is the single best shot: the road's `post` prop and the road's `hedge` both have their contact shadow directly under them, and the fence four tiles away has its shadow up-screen with a clear gap at every post, all in one frame.

REACHABLE. 9 fence instances (5156 lane x3, 5179 mill x2, 5221 mark x2, 5239 road x2) — count confirmed by regex. Lane, mill, mark and road are all on the normal route (square -> lane -> mill / village -> mark -> road); the road pair frames the "Where the fence ends" look at 5249, which is the set piece of the place. Nothing here needs a save state or an input that does not exist.

WHERE THE FINDING'S OWN REPRO IS WEAK: .claude/nnqc/coll-lane-fenceshadow.png does not show what it claims. Everything north-west of the lane's west fence in that shot is black, so no dark band is distinguishable from the falloff. The finding is right; that particular screenshot does not demonstrate it. Use the mill or the road-end shots instead.
     CORRECTION: Two things in [20] are overstated, but the finding itself stands.

(1) "a full sliver-width clear of it" is wrong about the size of the gap. The sliver IS 14.5px thick measured vertically at a fixed screen x (my first instinct that it was 7.25 was wrong: the two long edges of the quad are offset by (14.5, 7.25) screen px, and walking that offset back along a line of slope -0.5 adds another 7.25, so the vertical separation is 14.5). Since the shift is also 14.5, the shadow's south edge and the post feet nearly meet rather than being a sliver apart: contactShadow's outer ring is scaled 1.18 about the origin (which preserves the centre line and multiplies the perpendicular offset) and then translated +2, so its south edge sits at centre+10.56 while the feet sit at centre+14.5 — a gap of about 4px, and about 5px to the darkest ring. "The shadow sits entirely north of the fence" is true; "a full sliver-width clear of it" is not.

(2) "off its ... collision box" is true in world coordinates but misleading about what a player sees. drawActor (comp/ninth.js:2111) draws the player at `isoY(RT.px, RT.py) + TILE_H / 2` — the same offset. For the mill's east fence b=[13.9,2,0.5,8], blocked() with r=0.3 stops RT.px at 13.6, the player is DRAWN at world 14.1, and the fence is drawn at 14.65: a gap of exactly 0.55 tiles = r + half the fence's width. That is the correct-looking result. Delete `+ TILE_H / 2` on its own and the player's drawn feet land on the drawn fence line instead. The finding already says to land it with the actor-layer fix; the title should not be read as "there is an invisible wall at the fence", because there is not one today. What a player can actually see today is the shadow and the floor phase, not the stopping distance.

=== [21] survived 1/2  severityVotes=[]
  --correct-- refuted=False conf=high
     Core mechanism verified against source. blocked() (comp/ninth.js:6044-6050) is `if (x + r > b[0] && x - r < b[0] + b[2] && y + r > b[1] && y - r < b[1] + b[3]) return true;` over the raw authored footprint; ins is never consulted, and there is no other guard (the only other blocked() call sites are 3022/3026/3241/3242 foes, 6056/6057 moveActor, 6083/6087 unstick, 6203/6204 stepNpcs, all passing the same raw radius). grep for \bins\b gives exactly three hits in the whole file: PROP.tree ins:0.34 (6393), PROP.post ins:0.42 (6405), and `var k = c.d.ins || 1;` in body() (6716). audit-geometry.js never reads it either. PAINT.tree (7181) is a bespoke painter that never calls body(), so tree's ins is dead data; PAINT.post (7350) opens with `var f = body(g, c, ['#5b5243','#3b352b','#6d6455']);` so post's ins IS live, and it insets the DRAWN quad only. Numbers re-derived with TILE_W=58, TILE_H=29 (ninth.js:46), isoX = ORX + (x-y)*29, isoY = ORY + (x+y)*14.5: lane tree b [2.2,6.4,1.4,1.4] (5157) with r=0.3 gives blocked x[1.9,3.9] y[6.1,8.1], 2.0x2.0 tiles, centre (2.9,7.1); along the constant-screen-y diagonal (1,-1) the exclusive limit is t=1.0, screen dx = 2.0*29 = 58.0px. Trunk half-width 6.5px from `var w = 13 - q * 5` (7186). Lamp b 0.5x0.5 -> 1.1x1.1 -> 63.8x31.9px diamond, post 4px from `px(g, -2, -hgt, 4, hgt + 3)` (7574). Seven lamps confirmed at 5097 (square x2), 5125 (bernhouse), 5143 (chandler x2), 5161 (lane), 5238 (road). Four things are wrong or overstated, none fatal. (a) "at dy = 0": drawActor (2111) draws at isoY + TILE_H/2, drawProp (6628, mxc/myc from the corner projections) has no such offset, so the player's feet sit 14.5px lower on screen, not level. (b) magnitude: 58.0 not 60.9, and 47px is 0.81 of TILE_W=58, not "a full tile-width"; measured off the finding's own coll-lane-tree1-z4.png the bare band is ~40px (0.65 tile), and I captured the square lamp at its closest standable point (5.41,4.89) in .claude/nnqc/sk21-square-lamp-z4.png where the bare band is ~23px (0.4 tile). (c) attribution: r=0.3 projects to a 34.8x17.4px diamond around a figure drawn 14px wide, so 17.4px of the 58px is the player's own radius and no ins change touches it; with a point player the limit would be rrx = (1.4+1.4)*58/4 = 40.6px, exactly the footprint diamond edge. (d) the floor is not unmarked: contactShadow (6695-6709) fills the full footprint diamond q=[c.x0..c.x3] at ring scales 1.0/1.09/1.18, reaching 1.18*40.6 = 47.9px of the tree's 58px and 17.1px of the lamp's 31.9px; at rgba(5,4,9,.15-.34) on a night floor it is not legible, so the visual claim survives, but it does mean the proposed fix would seat the player inside the tree's own painted shadow.
     CORRECTION: Keep at polish, but restate. blocked() (comp/ninth.js:6044-6050) collides against the authored footprint b[] grown by r, and `ins` is a paint-side inset read only by body() (6716), not a collision declaration. Two consequences, and they are different. First, PROP.tree.ins = 0.34 (6393) is dead: PAINT.tree (7181) is bespoke and never calls body(), so nothing in the game or the audit reads that number. PROP.post.ins = 0.42 (6405) is not dead - PAINT.post (7350) calls body(), which scales the drawn quad by k, so the post is drawn at 42% of a footprint it still collides at 100% of. Second, slender props therefore stop you short of what you can see. Lane tree b [2.2,6.4,1.4,1.4]: the exclusive collision limit along the constant-screen-y diagonal is 58.0px (= TILE_W) from the footprint centre, against a trunk drawn 13px wide, leaving about 40px (0.65 tile) of floor you can see and cannot cross - not 47px and not a full tile-width. A square lamp (b 0.5x0.5, 4px post) leaves about 23px (0.4 tile); see .claude/nnqc/sk21-square-lamp-z4.png. Note also that the player and the prop are not on the same screen row: drawActor (2111) adds TILE_H/2 and drawProp does not, so the feet sit 14.5px lower (that is finding [19]). Roughly 17.4px of the tree's 58px is the player's own r=0.3, which projects to a 34.8x17.4px diamond around a figure drawn 14px wide; shrinking the prop cannot recover that part. Finally, the ground is not blank out there: contactShadow (6695-6709) paints the full footprint diamond out to 1.18*rrx, i.e. 47.9px of the tree's 58px and 17.1px of the lamp's 31.9px, so the footprint IS marked, faintly. That also means the suggested fix has a cost: applying ins in blocked() would put the player standing inside the tree's own contact shadow, so the shadow would have to be inset with it.
  --repro-- refuted=True conf=high
     Three independent checks, two of which kill the finding outright.

1. "PAINT.tree never calls body() either, so tree's ins:0.34 is read by nobody at all - dead data" is FALSE. grep for `\bins\b` in comp/ninth.js gives exactly three hits: 6393 (tree data), 6405 (post data), 6716 (`var k = c.d.ins || 1;` in body()). The finding correctly notes PAINT.tree (7181) does not call body(). It missed comp/ninth.js:6634, in drawProp's pre-sprite fallback branch:
    if (!sp) { var c = spriteCtx(t, b[2], b[3], v); ... contactShadow(cx, c);
      if (c.d.body === 'round') roundBody(cx, c); else if (c.d.body === 'cyl') cylBody(cx, c); else body(cx, c); ... }
PROP.tree has no `body` field, so a tree takes the `else body(cx, c)` arm and body() reads c.d.ins = 0.34. That path is reached whenever mayBuild() is false (startBuildBudget at :3851, BUILD_MS 5 at :6618), i.e. on every cold entry to a place with several unbuilt props - _recovered.txt's own perf notes say the fallback lasts two frames on a cold entry to the square. So `ins` is live, and its semantics are unambiguous: body() multiplies the footprint quad corners x0..y3 by k, so it means "draw the plain extruded box narrower than its footprint" (which is why PAINT.post at :7350 uses it and PAINT.tree, which draws its own tapered trunk, has no need to). It never states a collision hull, so it cannot be "dead data that already states the correct answer".

2. "~47px of lit, empty, unwalkable floor" does not reproduce as empty. I re-derived the maths: blocked() with r=0.3 blocks 1.9<x<3.9 and 6.1<y<8.1 for the lane tree b [2.2,6.4,1.4,1.4], so the true closest point on the screen-horizontal is (3.9,6.1), not (3.95,6.05). Screen dx to the footprint centre (2.9,7.1) is ((3.9-6.1)-(2.9-7.1))*29 = 58px. That 58 decomposes exactly as rrx (footprint half-diagonal, (1.4+1.4)*58/4 = 40.6) + player radius (0.3*58 = 17.4). There is literally zero slack: blocked() is precisely "the player circle of radius 0.3 may not overlap the footprint rectangle".
And that 40.6px is painted. contactShadow (:6690-6709) fills the footprint diamond in three bands at alpha .34/.2/.15. Measured on a fresh capture with the player parked away so no lantern washes it (.claude/nnqc/sk21-lane-tree-nolight.png, ndev=lane&nat=8.5,2.5&hz=3&hw=2.9,7.1, 6 png px per canvas px, tree base at png 600,310): along the ground row the floor luminance is 12.1-14.2 out to 38 canvas px east, 17.1/20.0/20.2 at 40-46, and 22.5 from 48 on. The transition sits between rrx=40.6 and rrx*1.18=47.9. Rows +4/+8/+12 canvas px down transition at 31/26/15, matching 40.6*(1-dy/20.3) - the visible dark region IS the footprint diamond. So the unwalkable ground is shadowed at roughly a 40% luminance drop, not bare. The finding also compares against a 13px trunk while PAINT.tree :7186-7190 draws roots out to cos(a)*rrx*0.4 = +/-16px, so the drawn ground contact is ~32px wide.
The "lit" adjective is half fair: in .claude/nnqc/sk21-lane-tree-close.png (player at 3.9,6.1) the player's own lantern lifts that strip from ~38 to ~78 luminance and the shadow reads as part of the falloff, with a measurable step of ~9 at 48 canvas px. But that is the player lighting the tree's shadow, not slack in the collider.

3. The proposed fix is actively unsafe. The painter sort at :3887 keys props on the footprint centre (o.b[0]+o.b[2]/2 + o.b[1]+o.b[3]/2) and the player at :3889 on RT.px+RT.py. Insetting blocked() by ins would put the tree's solid at 0.476 tiles and let the player stand inside the un-inset footprint, on the tree's own contact shadow, at a sort key that ties with the tree's (both 10.0 on the diagonal used in the repro) - i.e. the player flipping in front of and behind the trunk.

Lamp side-claims check out as data but carry the same error: 7 lamps at 5097 (x2 square), 5125 (bernhouse), 5143 (x2 chandler), 5161 (lane), 5238 (road), all b 0.5x0.5, blocked box 1.1x1.1. Of that 1.1, 0.6 is the player's own diameter and 0.5 is the authored footprint; the lamp's contact shadow covers rrx*1.18 = 17.1px of the 31.9px closest-approach offset, leaving about 8 canvas px of genuinely bare floor at the ground row.
     CORRECTION: What survives is only this, and it is not a defect: blocked() (C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/comp/ninth.js:6044-6051) collides against the authored footprint b[] grown by the actor radius, and `ins` is a paint-space inset for body(), not a collision hull. A tree's 1.4x1.4 footprint is larger than its 13px trunk because the author said so, the contact shadow paints that footprint on the ground, and the depth sort keys off the same footprint centre. There is no invisible margin to remove: the player's closest standable point is exactly footprint edge + 0.3 player radius.

=== [22] survived 1/2  severityVotes=[]
  --correct-- refuted=False conf=high
     CODE, verified verbatim. comp/ninth.js:6052-6058 moveActor does clamp-then-test, so the final player position is always a tested position. comp/ninth.js:6200-6205 stepNpcs does test-then-clamp: `if (!blocked(nx,w.y,0.28)) w.x=nx; if (!blocked(w.x,ny,0.28)) w.y=ny; w.x=clamp(w.x,0.6,pw()-0.6); w.y=clamp(w.y,0.6,ph()-0.6);`. The two blocked() calls do verify the final pre-clamp point (the second runs at the already-updated w.x), and then the clamp moves it to an untested point. That asymmetry is real. unstick (6082-6090) reads and writes RT.px/RT.py only and is called from exactly two sites, gotoPlace:6147 and devDemo:8136, so the finding is right that NPCs have no unstick. Only stepNpcs writes w.x/w.y (grep world.npc gives 6146, 6176, 6179, 6180, 6185, 6216, 7955).

MECHANISM, REPRODUCED. C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk22-trap2.js drives the verbatim inner body against a synthetic 13-wide place with a fence at x 11.6..12.125, y 4..8, clamp line c = 12.4, r = 0.28. Actor at (12.40, 3.70), unblocked because y is 0.02 short of the band. Target (14.5, 4.6), speed 1.5, dt 1/60 so sp = 0.025. Frame 2: nx = 12.4231, left edge 12.1431 > 12.125 so clear, w.x accepted; ny = 3.7293 tested at that clear x so accepted; the clamp pulls x back to 12.40, left edge 12.12 < 12.125, inside the fence. Printed as preclampBlocked=false afterClampBlocked=true. So yes, the clamp can put an NPC in a prop.

WHAT THE FINDING GETS WRONG.
(1) The trigger. The window is not "an edge that has a prop on it". Solving the two inequalities (clamped point blocked, pre-clamp point not) gives: the prop face must land within sp of the clamp line minus the radius, i.e. b0+b2 in (c-0.28, c-0.28+sp] on the high side, a window sp = speed*dt wide: 0.012 to 0.032 tiles at 60fps for the three walkers that exist (speeds 0.7, 0.85, 1.9), at most 0.095 at the 0.05 s dt cap (frame:3804). I enumerated every prop in PLACES against all four clamp lines (.claude/nnqc/sk22-clamp.js part A). The three fences the finding names are all far outside it: lane fence b=[0.6,4,0.5,9] spans x 0.6..1.1 against clamp line 0.6, so the actor is clear of it only 0.28 past the line; mill fence b=[13.9,2,0.5,8] spans 13.9..14.4 against clamp line 14.4, again 0.28; road fence b=[9.7,23.6,0.5,8.4] spans 9.7..10.2 against clamp line 10.4, 0.08. Every other prop in the game is at 0.28 or 0.88. blocked() therefore stops a walker before it can ever occupy an unblocked out-of-band point at any of them. Authoring a walker beside the lane, mill, mark or road fences does not produce the bug at any frame rate.
(2) "Once inside, both axis tests fail for every step ... so they never leave" is false for half the target directions. Line 6204 tests the y move at the freshly written, still-unclamped w.x. On any frame whose target has an outward x component the x write lands at a point clear of the prop, so the y test passes there and the walker slides along y at full speed while nominally inside the prop: phase 2 of sk22-trap2.js moves 2.7 tiles in y in 360 frames. It walks out the moment its y clears the prop band. Total freeze happens only when the target is inward in x (phase 3: 0 frames of movement in 600), which for a wanderer lasts until the next target pick at :6207 and is permanent only for a path walker whose every path point is inward in x.
(3) "forever" is bounded by one visit. gotoPlace:6146 does RT.world.npc = {}, so leaving the place and coming back reseats every NPC at their def home. The finding does not mention this guard.

NOT LIVE TODAY, confirmed independently of the finding's own script. Only three NPCs move: child (square, path all inside x 7.4..10.6, y 9.8..12.6), shepherd (village, home 9.2,5.6 wander 1.7), chandler (chandler, home 5.8,3.2 wander 1.1). None can reach within 3 tiles of a clamp line. 120 faithful simulations of 6000 frames each (.claude/nnqc/sk22-clamp.js part B) produced zero clamp events of any kind, harmless or otherwise. Also checked that no code mutates place().props at runtime (no props.push / .props = / splice anywhere), so an NPC cannot be enclosed by a prop appearing around it, and RT.timeScale (which could triple sp) is set only from the dev menu at :1005.
     CORRECTION: Keep the fix (clamp nx/ny before the two blocked() tests, as moveActor does) and keep the severity at risk, but the urgency and permanence claims are wrong. It does NOT go live "the first time a walker is authored within wander of an edge that has a prop on it": the trap needs the prop's face to land in a window of width speed*dt (0.012 to 0.032 tiles at 60fps, 0.095 at the dt cap) just inside the clamp line. The nearest any prop in the game gets is the road fence at 0.08, and the lane, mill and mark fences the finding cites are at 0.28, so a walker authored beside any of them is stopped by blocked() well before it can occupy an out-of-band point at all. And a trapped NPC is not stuck forever: comp/ninth.js:6204 runs the y test at the un-clamped w.x, so on any frame with an outward-x target the walker slides freely along y and leaves as soon as its y clears the prop band (only a path walker whose every point is inward in x is fully frozen), and gotoPlace at comp/ninth.js:6146 wipes RT.world.npc, so the state ends the next time the player leaves the place.
  --repro-- refuted=True conf=high
     CODE. comp/ninth.js:6052-6057 moveActor clamps then tests; comp/ninth.js:6201-6205 stepNpcs tests then clamps. That much is accurate. dt is capped at 0.05 (comp/ninth.js:3804 and 3850). Only three NPCs move at all: child (path, speed 1.9, square), shepherd (wander 1.7, speed 0.85, lane), chandler (wander 1.1, speed 0.7, chandler).

REPRO 1, does it fire? I transcribed stepNpcs verbatim (.claude/nnqc/r2sk22-clamp.js) and ran 200,000 frames per NPC per place at dt=1/60 and dt=0.05. Clamps fired: 0, at both rates, for all three. Geometry says why: the shepherd's reach in a 13x17 place is x in [7.5,10.9], y in [3.9,7.3] against clamp lines 0.6/12.4 and 0.6/16.4; the chandler's is x in [4.7,6.9] against 0.6/11.4; the child's four path points are all interior. No overshoot is possible either, since the arrival test fires at d<0.16 and the largest sp in the game is 1.9*0.05 = 0.095. Live confirmation: .claude/nnqc/r2sk22-lane-shep-a.png (900 frames) and .claude/nnqc/r2sk22-lane-shep-b.png (2400 frames) both show the shepherd mid-field, nowhere near either fence.

REPRO 2, is "forever" true? No. comp/ninth.js:6146, inside gotoPlace: `RT.world.npc = {};                       // everybody back to their own doorstep`. npcRT (comp/ninth.js:6175-6177) lazily rebuilds each record from n.x/n.y. Every call site that enters a place goes through gotoPlace (6078 stepTravel, 6251 doInteract, 1576 boot, 5480 script, 5891 revive, 8135 devDemo). An NPC only steps while place() is his place. So the ceiling on any stick is one visit, cleared by walking out and back.

REPRO 3, is the trigger right? .claude/nnqc/r2sk22-clamp2.js sweeps every walker home within 1.7 tiles of any edge of all 12 prop-bearing places, at speeds 0.7/0.85/1.5/1.9 and dt 1/60, 1/30, 0.05, 40k frames each. 64,286 homes tried at the shepherd's numbers: 53,779 fired the clamp, 0 ended inside a prop. The only row in the entire sweep where a clamp landed a walker inside a prop is road at speed 1.9 with dt=0.05: 630 homes clamped in, 0 permanently stuck. The three fences the finding names as the ones that will make it go live (lane 0.6/11.6, mill 0.6/13.9, road 0.8/9.7) are all x faces, and a prop that near the boundary stops the walker 0.28 short of it, which is what prevents the clamp rather than what triggers it.

REPRO 4, why it self-heals. Entering the trap requires the pre-clamp x = c-sp to be clear of the prop, i.e. (c-sp)+R <= b0, since that is what let the y-move through. The escape step tests that same coordinate: the next frame's westward x-move goes to c-sp, is not blocked by the identical inequality, the y-move is then evaluated at that clear x, and the walker slides along the clamp line until he is out of the prop's y band. .claude/nnqc/r2sk22-worstcase.js part B, at the road's west fence: from the trapped point (0.60, 28), 130 of 720 headings produce net movement at speed 1.9/dt 0.05, and 0 of 720 at any speed that cannot reach the trap in the first place. Targets are redrawn every 0.6 to 3.4 s by the existing "stuck against a wall: give up on this leg" line at comp/ninth.js:6207, so the worst observable is a walker standing still for a few seconds by an edge, indistinguishable from behaviour the game already produces against every wall.

REPRO 5, the y-axis version. By my maths that one would genuinely trap, because the x-move runs first at the clamped y, but it is structurally unreachable (.claude/nnqc/r2sk22-yaxis.js). A prop with b1 < 0.88 blocks the whole legal strip at y=0.6 inside its x band, and the walker can never change x while below the boundary, because the x-move always runs at an already-clamped y. Zero y-clamp-into-prop events in any run. The nearest y faces in the game are mark's fence and a3sq's stagewip at b1=1, both needing sp > 0.120 against a hard ceiling of 0.095.

Probes written, all under .claude/nnqc/ with comp/ untouched: r2sk22-clamp.js, r2sk22-clamp2.js, r2sk22-worstcase.js, r2sk22-yaxis.js, r2sk22-lane-shep-a.png, r2sk22-lane-shep-b.png.
     CORRECTION: The code asymmetry is real: moveActor (comp/ninth.js:6052-6057) clamps then tests, stepNpcs (comp/ninth.js:6203-6205) tests then clamps. Everything built on top of that is wrong. (a) The clamp never fires in shipped data; the finding concedes this. (b) "stuck there forever" is false: gotoPlace does `RT.world.npc = {};` at comp/ninth.js:6146 ("everybody back to their own doorstep"), gotoPlace is the only way into a place, and stepNpcs only advances NPCs in the place you are standing in, so the longest any stick could last is the remainder of one visit and it clears the moment you walk through any exit. The finding looked for unstick() and missed the stronger guard 60 lines above the code it quotes. (c) The stated trigger, "a walker authored within wander of an edge that has a prop on it, which is most edges", is wrong: a prop on an edge protects the walker, because blocked() stops him 0.28 short of it long before he can leave the bounds at that y. The real condition is that the prop's near face lands in a band of width sp = speed*dt starting at 0.88-sp, which is 0.014 tiles wide at the shepherd's speed at 60fps and 0.095 at the fastest shipped mover at the dt cap. (d) Even when forced, it does not stick, because the entry inequality and the escape inequality are the same one.

=== [23] survived 2/2  severityVotes=[]
  --correct-- refuted=False conf=high
     Re-derived the projection from source rather than trusting the finding. spriteCtx (comp/ninth.js:6455-6461) gives footprint corner0 = (-sk,-rry) = (-52.2,-43.5) for the counter [3.4,4.4,4.8,1.2], which is exactly the screen offset of world (3.4,4.4) from the footprint centre (5.8,5.0); so sprite-local (0,0) is the projection of the footprint centre on the ground, and drawProp (6622-6628) blits so that myc = (y0+y2)/2 = isoY(cxw,cyw). drawNpc (comp/ninth.js:7956) is sy = isoY(w2.x,w2.y) + TILE_H/2, and the game's own coversSomeone (comp/ninth.js:6674) uses the identical +TILE_H/2. audit-geometry.js:178 is dy = ((n.x+n.y)-(cxw+cyw))*14.5 with no +14.5, so the audit's actor model is 14.5px above the game's, verified. The binding clause is line 182 `dy - 40 >= top`, which a LARGER dy makes easier to satisfy, so the omission under-reports, as claimed; the companion clause `dy <= rry+4` is never binding for a prop that sorts later (true dy <= 14.5, rry+4 >= 18.5 for bw+bh >= 2). The cover loop at 168-187 samples only n.x,n.y; the path/wander loop at 218-230 tests standable/reachable only. RT.hide (comp/ninth.js:3882) is a one-element array holding the player, written nowhere else, and the comment at 3875-3881 explicitly nominates the geometry audit as the guard for NPCs, so no guard exists elsewhere. Chandler data confirmed at comp/ninth.js:5140 (counter) and 5427-5428 (x 5.8, y 3.2, wander 1.1); PROP.counter h 24 over 16 at 6410; blocked() at 6044-6050 is AABB expanded by r, so at radius 0.28 her southernmost y is 4.4-0.28 = 4.12; k 9.92 vs 10.8; drawn point (6.3,4.62), 0.22 inside the footprint. All as stated. Two numbers are wrong or soft. (1) The finding's own script .claude/nnqc/coll-cover.js prints 20 chandler ring hits, not 22. (2) I swept 256 ring directions against the counter's REAL silhouette (ground diamond raised by hgt=24) instead of the bounding box, in .claude/nnqc/sk23-silhouette.js: she is never fully hidden; the worst reachable point (6.35,4.12) leaves 9.6px of her 40px figure showing, and without the half-tile draw offset it would be 24.1px. The bounding-box predicate fires at 73 of 199 covered samples with real visible heights from 9.6px to 38.6px, so most firings are eave-tip false positives. coll-chandler-lamp.png matches the 9.6px figure: head, neck, shoulders and part of an arm above the counter, which reads as a shopkeeper standing behind her counter rather than as a break. The audit hole itself is real and unguarded, so (risk) is the right severity.
     CORRECTION: The audit hole is real exactly as described: audit-geometry.js:178 models the NPC at isoY(n.x,n.y) while the game draws them at isoY(n.x,n.y) + TILE_H/2 (comp/ninth.js:7956, and coversSomeone at 6674 uses the same +TILE_H/2), and the cover loop at audit-geometry.js:168-187 samples only the authored home tile while path and wander are checked at 218-230 for standable/reachable only. Correct two things in the write-up. First, the count: the finding's own coll-cover.js prints 20 of 64 ring directions for the chandler, not 22. Second, the impact: the chandler is never actually invisible. Re-deriving the counter's real silhouette rather than the bounding box, the worst reachable point (6.35, 4.12) still leaves 9.6px of her 40px figure showing, and with a correct projection it would be 24.1px, so the half-tile offset costs her about 14.5px of visible body and takes her from visible-to-the-hips to visible-to-the-shoulders. Of the 73 firings my 256-direction sweep produces, the real visible height ranges 9.6px to 38.6px, so most are the eave-tip false positives the finding's own fix note anticipates. The value of the finding is the latent hole, not the chandler: fix the dy and sample path/wander, and tighten the x test to rrx so the corrected predicate does not drown in false positives.
  --repro-- refuted=False conf=high
     Read the claim at .claude/nnqc/_recovered.txt:304-308, then the code rather than the summary.

1. Projection. TILE_W 58, TILE_H 29 (comp/ninth.js:46); isoY at :61. drawProp (:6621-6627) sets myc = (y0+y2)/2, which is isoY of the box centre, no offset. drawNpc (:7956) is isoY + TILE_H/2, and blit (:1864) hangs the sprite bottom on that point, so the feet really are 14.5px lower. paintedBox (:6508-6510) and propSprite (:6477-6496) put the sprite box in local coords about (mxc,myc). coversSomeone (:6669-6681) compares isoY(a.x,a.y)+TILE_H/2 against that box. audit-geometry.js:178 does not add it. dy_true = dy_audit + 14.5, confirmed. RT.hide is the player only (:3882), so no cutaway for NPCs, as claimed.

2. Figure height. ADULT_TOP is 16 rows plus 4 leg rows (:1873-1896) at PXS 2 (:1821), so 40px, matching the audit's 40 for adults.

3. Ran node .claude/nnqc/coll-cover.js. Reproduces the silent/fires contrast, but 20 chandler lines, not 22.

4. Data checked directly: chandler place at comp/ninth.js:5135-5150 (counter b [3.4,4.4,4.8,1.2]), NPCS.chandler at :5427-5428 (x 5.8, y 3.2, wander 1.1), PROP.counter at :6410 (h 24, over 16), PAINT.counter at :7619-7645 (the "over" band holds only a 14px set of scales at 0.24 along and a flat ledger at 0.68). stepNpcs (:6181-6207) picks r in [0.6, wander] and slides on blocked(0.28), so y 4.12 is reachable, roughly one target in twenty-five, so once or twice a minute.

5. The dev server on 8492 is NOT serving this worktree: its /comp/ninth.js is 15587 lines against this tree's 8279 (md5 differs). I served the worktree myself on 8571 and captured against that, then stopped it. Anyone taking screenshots for this worktree from 8492 is shooting a different build.

6. Captures: .claude/nnqc/sk23-b-y3.2.png (control, chandler at her authored home, whole figure clear of the counter) and .claude/nnqc/sk23-b-y4.12.png (pinned at the southernmost standable point, hair and one eye above the counter, the rest occluded). Harness: .claude/nnqc/sk23-pin.html. Earlier attempts sk23-chan-y3.2/3.7/4.12.png had the "E - look at The ledger" prompt sitting on her, which is why the player was moved to 2.2,7.6.

7. Cross-read [7] (line 63-65) and [19] (line 280-284) in _recovered.txt: [7] already reports the path-point half of this hole and the child burial; [19] already reports the actor offset and already cites coll-chandler-lamp.png. Nothing in comp/ was touched.
     CORRECTION: The audit-hole mechanism is real; the chandler evidence offered for it is not, and one half of the title duplicates [7].

TRUE, verified line by line:
- `.claude/ninth-night/tools/audit-geometry.js:178` is `var dy = ((n.x + n.y) - (cxw + cyw)) * 14.5;`. `comp/ninth.js:6627` anchors a prop sprite at `myc = (y0 + y2) / 2`, i.e. isoY of the footprint centre with no offset, but `comp/ninth.js:7956` draws a person at `isoY(w2.x, w2.y) + TILE_H / 2`, and blit (`comp/ninth.js:1864-1865`) puts the feet on that point. The game's own runtime version of this same test, `coversSomeone` at `comp/ninth.js:6676`, does add it: `sy = isoY(a.x, a.y) + TILE_H / 2`. So the audit's dy is 14.5px short of the game's, and the miss band is true dy in [top+40, top+54.5), the far-back band where a roof buries somebody.
- The cover loop at audit-geometry.js:168-187 reads only `n.x, n.y`; path and wander are visited separately at 211-234 and only for standable/reachable.

WRONG OR OVERSTATED:
- "fires for the chandler at 22 of 64 ring directions": `node .claude/nnqc/coll-cover.js` gives 20, not 22, and every one of the 20 is a false positive. I pinned her runtime position with a scratch harness (`.claude/nnqc/sk23-pin.html`, canvas.html plus hnpc/hnx/hny then `__ninth.tick`) at her southernmost standable point (5.8, 4.12): `.claude/nnqc/sk23-b-y4.12.png` against the control `.claude/nnqc/sk23-b-y3.2.png` shows the top of her hair and one eye still clearly above the counter. The check exists to flag "nothing showing"; something shows. The maths agrees: at her column (local dx 25.5) the counter's topmost paint is its top-face back edge, 24px above the footprint edge at local y -4.65, so -28.65, while her head is at -38.3. The predicate's -(h + over) = -40 is measured from the footprint CENTRE and ignores the iso rhombus, which is what produces the hit. Same for the 5 "shepherd fully hidden by a rail fence" hits (fence h 22 + over 18).
- The fix as written therefore makes things worse, not better: run over the wander ring with +14.5 it turns "geometry clean" into ~25 alarms of which one is real. Tightening the x test to rrx removes only 2 of the 20 (max |dx| 89.7 and 87.1 against rrx 87). The predicate needs the prop's top edge at the NPC's own column, not a bounding box.
- "only tests the authored home tile" is already reported: [7] says "Second hole in the same check: the audit burial-tests only n.x/n.y, never n.path" and works the child's path points. Only the wander ring is new here, and the wander ring produced nothing but false positives.
- The visual repro is [19]'s: `.claude/nnqc/coll-chandler-lamp.png` is cited by [19] as its own third screenshot ("the chandler sunk into her own counter to the armpits"), for the actor +TILE_H/2 offset. [23] is re-using it as if it were a burial.

WHAT THE FINDING IS ACTUALLY WORTH: exactly one true positive today, and it is not the one cited. With the +14.5 the child's HOME tile flips: dy goes -113.1 to -98.6 against top+40 = -112, so the corrected check catches the square child behind the SE house that [7] reports as broken and that the audit currently misses by 1.1px. That is the case to argue from. The finding stands as "the audit's projection no longer matches the game's own coversSomeone", severity risk, not as "the chandler is buried and nothing sees it".

=== [24] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     Code check, every claim in the block against the real lines.

1. Location and shape. C:/Users/isaac/IsaacUre.github.io/comp/ninth.js:7857-7869 is drawVignette, called from draw() at :3893, and grep shows those are the ONLY two occurrences of the symbol in the file. Body is exactly as quoted: `var ind = place().indoor;` then createRadialGradient(VW/2, VH/2-30, ind?300:250, VW/2, VH/2, 680), three literal stops (rgba(4,3,8,0) / .2-or-.3 / .6-or-.8), then fillStyle=vg; fillRect(0,0,VW,VH). Every input except `ind` is a literal. Confirmed.

2. The pixel count. VW=1120, VH=580 at :45. The canvas is created at :758 with width/height attributes = VW/VH, and CSS (comp.css:2919) only stretches it, so the backing store is 1120x580 = 649,600 px. My probe printed "canvas backing store 1120x580 css 1120x580 dpr 1". Confirmed.

3. "Only place().indoor can change." grep for `indoor` gives :5119 and :5137 only (bernhouse, chandler) - a static literal field on the place, no writers. So drawVignette is a two-valued function of which place you stand in, and consecutive frames in one place are byte-identical. Confirmed.

4. Guard elsewhere? None. frame() at :3802 calls draw(real) unconditionally every rAF; draw() calls drawVignette unconditionally at :3893 with no S.opts / low-fx / dirty-flag test anywhere. There is no existing bake of it.

5. Re-measured independently with my own probe, not theirs: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk24-vig.html, same headless --disable-gpu. I added the linearity test the original did not do (1, 2 and 4 extra copies), because the delta of adding a second vignette only tells you the cost of the first if the cost is linear. It is: arena per-extra 4.19 ms at n=2 and 3.96 ms at n=4; square 2.95 at n=2 and 3.13 at n=4. Baked blit marginal cost is at the noise floor (arena 2 blits = 1.76 ms total delta, square 2 blits = 0.39 ms), and a flat non-gradient full-screen rgba fill over the same 649,600 px costs about the same as a blit (arena delta 1.57 ms), which independently confirms their claim that the cost is per-pixel gradient evaluation and not the gradient object (my "gradient object, no fill" delta was 0.61 ms on arena). The mechanism reproduces on a second implementation.

Two things I would trim, neither of which touches the mechanism:

(a) "the arena's entire world frame is 2.74 ms, i.e. the vignette is essentially the whole frame" is internally inconsistent as stated: the baseline frame ALREADY contains one vignette, so a 2.74 ms baseline cannot also contain a 2.72 ms vignette plus the floor blit plus the ents pass. My run shows why - this box drifts (arena baseline 2.73 ms at the start of the sweep, 4.08 ms when I re-ran the identical baseline at the end). The square figure is the one that survives the drift.

(b) "On a GPU-backed canvas the absolute number is smaller, but ... the same shape of comparison" is asserted, not measured, and is the weakest sentence in the block. The probe's own flush() is getImageData(0,0,1,1) per iteration, which is exactly the readback pattern that makes Chrome abandon GPU acceleration for a 2D canvas, so the methodology guarantees software raster on top of --disable-gpu. On an accelerated canvas both the live gradient fill and the baked blit are one full-screen textured quad (gradient ramp lookup versus texture sample), so the win there is plausibly near zero.

Also worth stating plainly: against a 16.7 ms budget, a 4.43 ms square tick (which includes step() and updateHud, not just the world draw, since __ninth.tick at :1582 is step+draw) is not dropping frames on anything. So (polish) is right; the block never claims a player-visible symptom and I could not manufacture one.

The proposed fix is sound as written: fillRect(0,0,VW,VH) and drawImage(baked,0,0) sit under the same shake translate at :3853 and are geometrically identical, so shake behaviour is unchanged. One nit the block does not mention: after the fix, cx.fillStyle is no longer left holding the gradient on exit. Every subsequent consumer in draw() (drawCalls :3945-3946, the flash/hurt fills :3900-3901, drawPrompt) sets fillStyle before drawing, so nothing depends on the leftover.
     CORRECTION: Real and correctly diagnosed; the mechanism is exactly what the code does, and I reproduced it with an independent probe including the linearity check the original omitted (per-extra live vignette 2.95-4.19 ms, baked blit at the noise floor). Two overstatements to fix. First, "the arena's entire world frame is 2.74 ms, i.e. the vignette is essentially the whole frame" cannot hold as stated, because that baseline already contains one vignette; my re-run shows the baseline drifting 2.73 -> 4.08 ms across a single sweep on this machine, so drop the arena line and keep the square's 62%, which is robust to the drift. Second, "on a GPU-backed canvas ... the same shape of comparison" is unmeasured and probably wrong: the probe's per-iteration getImageData flush is the readback pattern that makes Chrome drop 2D canvas GPU acceleration, so every number in the block is software raster, and on an accelerated canvas a gradient fillRect and a drawImage are both a single full-screen textured quad. State it as a software-raster finding. Severity stays polish: the whole square tick is 4.43 ms against a 16.7 ms budget, so there is no player-visible symptom today.
  --repro-- refuted=False conf=high
     CODE CHECK. comp/ninth.js:7857-7869 drawVignette is exactly as described: `var ind = place().indoor;` is the only varying input, then createRadialGradient(VW/2, VH/2-30, ind?300:250, VW/2, VH/2, 680) with three literal stops and cx.fillRect(0,0,VW,VH). VW=1120, VH=580 (comp/ninth.js:45) and the canvas is created at those exact backing-store dimensions with no devicePixelRatio scaling (comp/ninth.js:758), so it really is 649,600 pixels. Single call site, comp/ninth.js:3893, inside draw(), unconditional; draw() itself is called from frame() at comp/ninth.js:3811 on every requestAnimationFrame with no skip, no throttle, no visibility gate. Grep confirms drawVignette appears only at 7857 (def), 3893 (call) and 1364 (a comment).

REACH. Trivially universal. There is no state, place, save, difficulty or input that turns it off. Every frame a player ever sees pays for it, in all 13 places. Nothing to reach for.

REPRO 1, the finding's own probe, byte-for-byte, .claude/nnqc/vignette-probe.html under --headless=new --disable-gpu. My three runs versus the finding's numbers (delta = one extra live vignette / one extra baked blit):
  arena     finding 2.72/0.20   mine 2.74/-0.02, 4.79/0.67
  square    finding 2.76/-0.02  mine 3.12/0.60,  3.78/0.33
  road      finding 2.26/0.12   mine 2.46/0.06,  3.12/0.08
  bernhouse finding 2.32/0.36   mine 4.68/1.52,  3.44/-0.36
Same magnitude, same sign, same conclusion: the live gradient costs 2.3-4.8 ms, the equivalent blit costs roughly nothing.

REPRO 2, my own probe .claude/nnqc/sk24-flushmode.html, which isolates the two ops over a synthetic busy frame and tests both flush strategies, to rule out the possibility that the getImageData(0,0,1,1) flush is what is actually being measured. Under --disable-gpu, three rounds: live gradient 2.26/3.36/3.55 ms per-iteration-flush and 2.23/3.57/2.85 ms flush-once-per-block; baked blit 0.079/0.126/0.132 and 0.076/0.127/0.076. The flush strategy changes nothing, so the cost is the raster, not the readback. Confirmed.

REPRO 3, .claude/nnqc/sk24-split.html, to test whether the finding's denominator is fair. It calls __ninth.tick(1) and __ninth.tick(5); since tick(n) is `for(i<n) step(); draw()` (comp/ninth.js:1582), (t5-t1)/4 isolates one step. Step costs 0.13-0.51 ms across square/arena/road/bernhouse/stage/hollow, i.e. the "frame" the finding divides by is 88-100% draw. So calling 2.76/4.43 "62% of the world draw" is fair rather than inflated by step time.

WHAT I TRIED THAT COULD HAVE REFUTED IT AND DID NOT. (a) Is the flush the real cost? No, see repro 2. (b) Is it software rasterisation only? I ran both probes again with --disable-gpu removed. The live gradient is unchanged, 2.28/3.35/3.46 ms per-iter and 2.99/2.88/2.94 ms per-block, so the gradient is the same price with the GPU flag off. The one thing that does change is the blit, which rises from 0.08 ms to 1.15-1.61 ms. That weakens the fix, not the finding. (c) Is anything else in draw() bigger? No: with step subtracted out, the marginal vignette is on the order of the whole remaining draw in arena/bernhouse/stage and about half of it in road/hollow.

SEVERITY. Polish is right and I would not move it. There is no player-visible symptom today: full step+draw measures 3.6-4.5 ms in software raster, well inside a 16.7 ms 60 Hz budget. It only starts to bite at the 144 Hz the file itself cares about (comp/ninth.js:3849 comment), on a machine where Chrome has fallen back to software canvas. The finding never claims a dropped frame; it claims a proportion, and the proportion holds.

Scratch files I added, both under .claude/nnqc/ as permitted: sk24-flushmode.html, sk24-split.html, sk24-vig-dom.txt, sk24-vig-gpu-dom.txt.
     CORRECTION: The finding stands. Two small numbers in it should be tightened, neither of which changes the verdict or the severity.

1. The fix's expected saving is config-dependent, and the finding's hedge ("on a GPU-backed canvas the absolute number is smaller, but ... the same shape of comparison") is optimistic in the wrong direction. With --disable-gpu removed, the live gradient does NOT get cheaper (2.3-3.5 ms, unchanged), but the baked blit gets more than ten times MORE expensive (0.08 ms -> 1.15-1.61 ms), because the drawImage then crosses the accelerated/unaccelerated canvas boundary. So the saving is about 30x under forced software raster but only about 2x once the GPU flag is off. Baking is still the right call, but whoever does it should measure the blit on a real accelerated context rather than assuming the 0.08 ms number transfers.

2. Strictly, "nothing about the result differs between two consecutive frames in the same place" is true only while the screen is still. drawVignette is called at comp/ninth.js:3893, inside the cx.save()/shake translate opened at comp/ninth.js:3852-3853, so during a shake the fill lands at a jittered offset and the pixels do differ frame to frame. This does not affect the fix, which the finding already places at the same point in the same transform, so the shake is preserved either way.

=== [25] survived 2/2  severityVotes=['risk']
  --correct-- refuted=False conf=high
     I could not refute the core claim. Every citation and every number checks out, and I reproduced both statically and live without using the round-one scripts.

CODE, re-read at the cited lines (C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/comp/ninth.js):
- 6434: `var SPRITES = {}, SPRITE_LRU = [], SPRITE_BUDGET = 10 << 20;` = 10485760 bytes exactly. Correct.
- 6482-6493 in propSprite: `rrx=(bw+bh)*TILE_W/4`, `rry=(bw+bh)*TILE_H/4`, `over=d.over||0`, `w=Math.ceil(rrx*2)+SPRITE_PAD*2`, `h=Math.ceil(rry*2+hgt+over)+SPRITE_PAD*2`, `bytes: w*h*4`. TILE_W=58, TILE_H=29 (line 46), SPRITE_PAD=30 (6435). The finding quotes this maths verbatim and correctly.
- 6522 trimSprites: evicts LRU-first while over budget. Its ONLY call site is 6495, inside propSprite after a build. Confirmed by grep: freeSprites (6529) is called from exactly one place, close() at 8243. There is no per-place flush, so cross-place accumulation is real and there is no guard elsewhere.
- Props are immutable at runtime: grep for props across the file finds only reads (3885 draw, 6045 blocked, 7814 lights). So the key set per place is fixed and "no within-a-place churn" holds.
- .claude/ninth-night/tools/audit-geometry.js contains no reference to SPRITE_BUDGET, SPRITE_PAD or sprite bytes. The "nothing checks this" claim is right.

MY OWN STATIC DERIVATION (.claude/nnqc/sk-25-recheck.js, vm-evals the PROP and PLACES literals, re-implements hash2/propVar/propSprite sizing from scratch): 64 distinct sprite keys, 12657332 bytes = 12.071 MB, budget 10.000 MB, headroom -2.071 MB. Identical to the finding to three decimals, including the per-place figures (stage 1.283, square 2.149 max, a3sq 1.766) and the top entries (curtain|13|1.4 0.738, wall|12|0.6 0.505, mill|5|4.4 0.481, foot|13|0.5 0.479, a house 0.346).

LIVE (my own probe .claude/nnqc/sk25-live2.html, 12-place tour with __ninth.place + __ninth.tick, reading __ninth.gfx() after each): peaks at 9.98 MB with 56 of 64 keys, and trims. That matches the finding's "9.99 MB, 56-58 keys".

Two things I initially thought were refutations and were not: my first probe reported two square house sprites missing, but that was my own propVar guess being wrong (house|4|3.4|4, not |3), not eviction. And the port-8492 dev server does NOT serve this worktree, it serves the suspicious-driscoll-00fbd8 worktree (15587 lines vs this branch's 8279), so I served this tree myself on 127.0.0.1:8497. Worth passing on to other round-two agents doing live captures.

Where the finding IS wrong is its narrative about when and what gets evicted, corrected below. That does not make the capacity claim false, so not refuted, and "risk" is still the right bucket: the mechanism is real, latent, and unchecked, with no player-visible effect today.
     CORRECTION: The headline is correct as written: 64 keys, 12.071 MB against a 10.000 MB SPRITE_BUDGET, nothing checks it. Three parts of the "what" are wrong and should be replaced.

1. "so the cache runs permanently at its ceiling" / "from roughly mid-game onwards the LRU is at its cap" is wrong. Measured over a full tour, the cache never trims once until the ELEVENTH place: after mark it holds 46 keys at 9.26 MB with zero evictions ever; the first eviction happens on entering road, and the cap is only really pressed by a3sq at the very end. It is "the last two places of a complete tour", not "mid-game onwards".

2. "the 2.071 MB that has to go ... is the prologue stage (1.283 MB) and a3sq (1.766 MB)" is wrong, and a3sq cannot be it: a3sq's sprites are built last, so they are the most recently used entries at the moment they force the trim. The measured victims on entering a3sq are the rest of the prologue stage (crate|1.6|1.2, foot|13|0.5, after curtain|13|1.4 went at road) plus FIVE of Bern's eight sprites: wall|11|0.6|0, table|2.6|1.6|0, shelf|2.4|0.8|0, hearth|2.2|1|0, crate|1.2|1.2|1.

3. Therefore "the true margin is 0.978 MB ... three more houses anywhere in the game and the eviction starts landing on places the player walks in and out of" is wrong in both directions. Eviction ALREADY lands on a re-enterable interior today, no extra houses needed. But the consequence is far smaller than implied: re-entering bernhouse after that eviction rebuilt all five missing sprites inside a single tick, so the plain-solid fallback is on screen for about one frame, which is exactly what the engine does by design on any first entry (see the BUILD_MS comment at 6611-6617). The 0.978 MB number is still meaningful, but only as "the warm set, everything except stage and a3sq, is 9.021 MB and fits with 0.98 MB to spare".

The fix as proposed is fine. The audit check is the better half of it, and it should be stated as "the whole game's distinct sprite keys must fit in SPRITE_BUDGET", not as a claim about mid-game thrash.
  --repro-- refuted=False conf=high
     REPRODUCED, twice, independently.

Code is as described. comp/ninth.js:6434 `var SPRITES = {}, SPRITE_LRU = [], SPRITE_BUDGET = 10 << 20;`. propSprite's sizing at comp/ninth.js:6483-6493 is exactly rrx=(bw+bh)*TILE_W/4, rry=(bw+bh)*TILE_H/4, w=ceil(rrx*2)+SPRITE_PAD*2, h=ceil(rry*2+hgt+over)+SPRITE_PAD*2, bytes=w*h*4. trimSprites at 6522-6528 is a byte-driven LRU shift. drawProp (6629) is the only caller.

Node: `node .claude/nnqc/sprite-budget.js` prints 64 keys / 12.071 MB / budget 10.000 / headroom -2.071. I did not trust that script, so I re-derived independently in C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk25-keysets.js from the vm-evalled PROP and PLACES literals: same 64 keys, same 12.071 MB.

LIVE: the port-8492 dev server is serving a DIFFERENT ninth.js (15587 lines, not this worktree's 8279) and returns 404 for .claude/nnqc, so I served this worktree on 127.0.0.1:8571 and ran .claude/nnqc/sk25-evict-probe.html and .claude/nnqc/sk25-epilogue-probe.html headless. The union of every key the game ever builds is exactly 64, matching the node list key for key. Peak resident 9.98 MB with 56-58 keys, matching the finding's 9.99 MB / 56-58. Eviction is real and starts before act 3: after `road` the union is 55 but only 54 are resident.

REACH: reachable in an ordinary playthrough, and in a way the finding did not spot. a3sq has `exits: []` (comp/ninth.js:5288), so I first thought the over-budget state was terminal, but a3Home at comp/ninth.js:5889-5891 does `S.place='square'; gotoPlace('square', false)` after the ending, and the `wick` script at 5482 has an explicit "it is the morning after" branch. So the player walks Wick again with a3sq's 1.766 MB in the cache. Measured first post-ending re-entry: bernhouse rebuilds 5 sprites, chandler 4, mill 3, loft 2, road 9, hollow 3.

Still only a risk: the rebuild costs one frame at the BUILD_MS=5 budget (frame0 5.1ms for epilogue bernhouse vs 5.9ms for the first-ever bernhouse), i.e. one or two frames of the plain-solid fallback, which is the designed first-entry behaviour. Acts 1-2 have zero rebuilds, confirming "harmless today".

Audit hole confirmed: `node .claude/ninth-night/tools/audit-geometry.js` says "geometry clean" and grep finds no SPRITE_BUDGET or byte accounting anywhere in it.
     CORRECTION: The headline number and the mechanism are exactly right. Three details need fixing.

1. "Today this is harmless" is too generous. There IS a reachable rebuild path the finding missed: a3Home (comp/ninth.js:5889-5891) walks the player back into the square after the ending, and the first post-ending re-entry to each Wick place rebuilds sprites that a3sq's 1.766 MB evicted. Measured with .claude/nnqc/sk25-epilogue-probe.html: bernhouse 5 keys (incl. wall|11|0.6 at 0.450 MB), chandler 4, mill 3 (incl. mill|5|4.4 at 0.481 MB), loft 2, road 9, hollow 3. It stays a risk and not a bug only because the cost is one ~5ms frame, indistinguishable from a first visit.

2. "nothing is built while you walk around a place you are already in" is false. drawProp culls off-camera props at comp/ninth.js:6625-6626, so sprites build lazily as the camera sweeps. Measured in .claude/nnqc/sk25-lazy-probe.html: road arrives with 10 keys and builds 3 more (tree|1.4|1.4|3, cairn|1.4|1.4|0, tree|1.4|1.4|0) while you walk its length; square and hollow build 0 late. The conclusion that sentence supports still holds, because road's whole set is 0.814 MB against a 10 MB cache.

3. The margin arithmetic gets the right answer by loose reasoning. a3sq is never an eviction victim while you stand in it; it is the thing doing the evicting. The clean statement, same number: acts 1-2 want 10.305 MB of the 12.071, of which the prologue stage's 1.283 MB is dead once the prologue ends (nothing has `to: 'stage'`; only the dev panel at comp/ninth.js:835 goes back), leaving 9.021 MB hot against 10.000 MB, i.e. 0.979 MB spare. That is where "three more houses at 0.346 MB" comes from, and it is correct.

Minor: the "after mark: 9.96 MB, after road: 9.28 MB" drop is order-dependent and did not reproduce on my route (9.26 then 9.28). spriteMB does drop elsewhere in my trace (epilogue mill 10.00 -> loft 9.74), so the underlying claim that trimSprites overshoots downward is fine.

Severity stays (risk). The proposed fix (add the byte sum to audit-geometry.js) is the right one.

=== [26] survived 2/2  severityVotes=['risk', 'risk']
  --repro-- refuted=False conf=high
     I could not refute this. Every code claim checks out and I reproduced the numbers live against this worktree's build.

CODE, verified line by line:
- comp/ninth.js:1053 `var FLOORS = {}, FLOOR_LRU = [], FLOOR_MAX = 7;` and comp/ninth.js:1373-1378 `function trimFloors() { while (FLOOR_LRU.length > FLOOR_MAX) {...} }` — the eviction test is on array length only. There is no byte counter anywhere for FLOORS; grep for a FLOOR_BYTES analogue returns nothing.
- comp/ninth.js:6434 `var SPRITES = {}, SPRITE_LRU = [], SPRITE_BUDGET = 10 << 20;   // bytes of backing store, not entries` with `SPRITE_BYTES` at 6435 and comp/ninth.js:6522-6528 `while (SPRITE_BYTES > SPRITE_BUDGET && SPRITE_LRU.length > 1)`. So the asymmetry the finding describes is exactly as stated, and the sprite side even carries the comment explaining why bytes were chosen.
- comp/ninth.js:1311 key is `kind + gw + 'x' + gh`, comp/ninth.js:1314-1315 sizes the canvas to `placeBox(gw, gh)`. I re-derived placeBox (comp/ninth.js:69-73) myself: w = 29*(gw+gh) + 128, h = ceil(14.5*(gw+gh) + 157). room11x9 -> 708x447 -> 1.207 MiB; road11x34 -> 1433x810 -> 4.428 MiB. Both match the finding to three decimals, and the 3.7x spread is real.
- comp/ninth.js:8242 `freeFloors();` inside close(), and freeFloors at 1379-1382 zeroes every backing store. The finding's secondary point that PARALLEL.md is stale is correct: .claude/ninth-night/PARALLEL.md:161-163 still says "unbounded cache ... about 2.6 MB each, and close() never frees it. Eight entries exist." All four of those are now wrong (it is bounded, sizes run 1.2-4.4 MiB, close() does free it, and there are 12 distinct keys against FLOOR_MAX=7).

REPRO:
- `node .claude/nnqc/sprite-budget.js` runs and prints "distinct floor keys: 12   sum of all: 25.368 MB / worst 7 resident (FLOOR_MAX): 17.891 MB". Reproduced verbatim.
- Live: the port-8492 server is serving a DIFFERENT worktree (872 KB ninth.js with a zoom-punch feature vs this worktree's 475 KB, and /.claude/nnqc/ 404s there), so I stood up my own server on this worktree root and wrote .claude/nnqc/sk26-floormem.html, driven through __ninth.place + __ninth.tick with ?dev= so the handle exists. Output at .claude/nnqc/sk26-floormem3.png:
  A. all 13 places twice -> peak floorMB = 17.00, peak spriteMB = 9.99. That is the finding's "17 MB peak" exactly.
  B. a plausible player route that NEVER enters the dev arena (stage, square, bernhouse, square, chandler, square, lane, mill, loft, mill, lane, square, lane, village, mark, road, hollow, road, mark, square, a3sq) -> peak floorMB = 16.79, peak spriteMB = 9.99.
  C. late-game stretch square/lane/mill/loft/village/mark/road/hollow/a3sq -> peak 16.72.
  D. the ordinary six-place Wick loop three times -> settles at 13.1 MB resident.
  Display canvas measured at 1120x580 with no devicePixelRatio scaling (comp/ninth.js:758 hardcodes width/height), = 2.478 MiB. So the "roughly 30 MB" total is real: 16.8 + 10.0 + 2.5 = 29.3 MiB in normal play.

REACH: this is the part I expected to kill it and it survived. The 17.9 headline needs the dev-only arena floor, but stripping the arena out only takes the player-reachable worst-7 to 16.927 MiB, and I measured 16.79 MiB live on a route with no dev place in it. It is not a save-state or a hypothetical: it is where the cache sits for the whole back half of the game.

AND THE HAZARD IS NOT ALREADY GUARDED: I checked the audit's floor-memory test, which the brief lists as already covered. .claude/ninth-night/tools/audit-geometry.js:125-126 is `var mb = px*py*4/1048576; if (mb > 8) bad(...)`. That is a PER-PLACE cap of 8 MiB with no aggregate at all. The finding's hypothetical 17x40 place bakes 1781x984 = 6.686 MiB, so it would pass that gate with 1.3 MiB to spare, and seven such places resident would be 47 MiB while audit-geometry.js still prints "geometry clean". The per-entry guard exists; the aggregate one the finding asks for does not.

Nothing observable breaks today - no dropped frame, no artefact, no OOM - which is why (risk) is the right tier and not (bug). It is a measured structural asymmetry with a live number on it, filed at the correct severity.
     CORRECTION: Two numeric corrections, neither of which changes the conclusion.

1. The 17.891 MB worst-7 figure includes `mill17x17` (2.762 MiB), which is the dev-only arena's floor. A player who never opens the dev place cannot hold it. The worst-7 a real player can reach is road11x34 4.428 + town17x15 2.502 + mill13x17 2.254 + mill15x13 2.019 + hollow15x13 2.019 + town15x12 1.908 + loft13x13 1.797 = 16.927 MiB, and I measured 16.79 MiB live on an arena-free route. So the headline should read "up to 16.9 MB in normal play (17.9 MB if the dev arena is entered), 1.7x the sprite budget" rather than 17.9 / 1.8x.

2. Units drift once. Everything in the finding is MiB (road = 4,642,920 B / 1048576 = 4.428) except the "17x40 place would be 7.01 MB" line, which is decimal; in the same MiB the finding uses elsewhere it is 6.686 MiB. Worth fixing because 6.686 is the number that matters: it is under the audit's `mb > 8` gate at .claude/ninth-night/tools/audit-geometry.js:126, so such a place would be added in complete silence.

One sharpening rather than a correction: "the larger half of it is the one with no number on it" is slightly unfair. FLOORS is bounded, and there is a declared per-place number (8 MiB, audit-geometry.js:126). The actual defect is that the two numbers never meet: 7 entries times an 8 MiB per-entry ceiling is a 56 MiB sanctioned bound, 5.6x the sprite budget, and nothing in the game or the audit ever evaluates that product.
  --correct-- refuted=False conf=high
     MECHANISM, re-derived from source rather than taken from the finding.

1. The asymmetry is exactly as claimed. comp/ninth.js:1053 `var FLOORS = {}, FLOOR_LRU = [], FLOOR_MAX = 7;` and comp/ninth.js:1373-1378 `function trimFloors() { while (FLOOR_LRU.length > FLOOR_MAX) {...} }` — a pure entry count, no byte term. Against that, comp/ninth.js:6434 `var SPRITES = {}, SPRITE_LRU = [], SPRITE_BUDGET = 10 << 20;   // bytes of backing store, not entries`, comp/ninth.js:6494 `SPRITES[key] = sp; SPRITE_BYTES += sp.bytes;` and comp/ninth.js:6522-6528 `while (SPRITE_BYTES > SPRITE_BUDGET && SPRITE_LRU.length > 1) { ... SPRITE_BYTES -= s.bytes; ... }`. Grep for FLOOR_BYTES: nothing exists. Every line number cited in the finding is exact.

2. No guard elsewhere. `freeFloors` (comp/ninth.js:1379) has exactly one caller, comp/ninth.js:8242 inside close(). `trimFloors` has exactly one caller, comp/ninth.js:1366, right after the insert in buildFloor. buildFloor is called from draw() (3855) and from gotoPlace (6156), destination only, so the resident set is simply the last 7 distinct floor keys. Nothing shrinks it on a place change.

3. I re-derived placeBox myself. With ORX=560, ORY=84, TILE_W=58, TILE_H=29, FLOOR_PAD=64, placeBox(gw,gh) gives w = 29*(gw+gh) + 128 and h = ceil(14.5*(gw+gh) + 157). road 11x34: w = 29*45+128 = 1433, h = ceil(652.5+157) = 810, bytes = 1433*810*4 = 4,642,920 = 4.428 MiB. bernhouse 11x9: 708x447 = 1,265,904 = 1.207 MiB. Ratio 3.67x. Both match the finding to three decimals.

4. I enumerated all 13 PLACES floor kinds and sizes from comp/ninth.js:5077-5295 and folded them into keys (square and a3sq share town17x15, so 12 distinct keys). Total 26,600,796 bytes = 25.368 MiB; worst seven = 18,760,004 bytes = 17.891 MiB = 1.789x SPRITE_BUDGET. Identical to the finding. The Wick-loop figure in its fix section (town17x15 + room11x9 + room12x9 + mill13x17 + mill15x13 + loft13x13) is 11,615,080 bytes = 11.077 MiB, also as stated.

5. Live confirmation independent of round one. I wrote C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/cq26-floorbytes.html (result screenshot cq26-floorbytes2.png) and drove it through __ninth.place, which is gotoPlace(id, true) at comp/ninth.js:1592, the real travel path. Route made only of real exits, no dev arena and no gated a3sq: mill > lane > square > lane > village > mark > road > hollow. Per step: floors=2/3/4/4/5/6/7/7, floorMB=3.61/5.86/8.36/8.36/10.27/11.86/16.28/16.72. Held at end: 7 floors, 16.72 MB, exactly the 16.716 MiB my LRU simulation predicted. Five rounds of all 13 places: peak floorMB=17, peak floors=7, peak spriteMB=9.99 against budget 10. Main canvas 1120x580x4 = 2.478 MiB. Peak canvas backing store ~29.5 MB, and the floor half is the unmeasured half. The claim reproduces.

6. The PARALLEL.md quotes are real: line 106 "Cap `w` and `h` at 17 until job 3 lands a camera." and lines 161-163 "FLOORS is an unbounded cache ... about 2.6 MB each, and close() never frees it. Eight entries exist." All three staleness claims hold.

WHAT I TRIED TO BREAK IT WITH AND COULD NOT: the dev-arena objection (worst-7 without arena is still 16.925 MiB, and the measured real-exit route is 16.72 MB, so dropping the dev place moves the number by 1 MB); a devicePixelRatio objection (the canvas at comp/ninth.js:758 is a plain width=1120 height=580, no DPR scaling anywhere in the file); and the sprite-spread parenthetical, where I computed every distinct prop sprite shape from PROP and PLACES and got curtain 13x1.4 at 478x405 = 0.738 MiB, matching the finding's upper bound exactly.

Not a bug: nothing misbehaves today, the cap is enforced, close() frees. It is a real, reproducible, unmeasured allocation with a named future hazard. "risk" is the right shelf.
     CORRECTION: Three numbers in the block are off, none of them load-bearing.

(a) The headline 17.891 MB worst-seven includes mill17x17 (2.762 MiB), which is only the dev arena. The worst seven a player can reach is 16.925 MiB (1.69x SPRITE_BUDGET, not 1.79x), and an ordinary route through Wick and north measured 16.72 MB live. The accurate form is "seven floors is up to 16.9 MB in play, 17.9 MB if you count the dev arena".

(b) Units are mixed. The core figures (1.207, 4.428, 17.891, 25.368) are MiB, but "7.01 MB" for the hypothetical 17x40 floor and "2.6 MB" for the display canvas are decimal MB. In MiB those are 1781x984x4 = 6.685 MiB and 1120x580x4 = 2.478 MiB, so the hypothetical is 5% smaller than stated.

(c) The sprite-spread parenthetical "(0.005 MB to 0.738 MB)" is right at the top and wrong at the bottom. SPRITE_PAD = 30 (comp/ninth.js:6435) puts a 60px floor under both sprite dimensions (comp/ninth.js:6485-6486), so the smallest sprite in the game is post 0.6x0.6 at 95x118 = 0.043 MiB. The real spread is 17.3x, not 148x. That still supports the point it was cited for: sprite entries vary far more than floor entries (17.3x against 3.7x), which is why sprites got a byte budget.

Everything else stands, including the 3.7x floor spread, the 12 distinct keys, the 25.368 MiB total, the 11.08 MiB Wick loop in the fix, and all three PARALLEL.md staleness claims.

=== [27] survived 2/2  severityVotes=['bug']
  --correct-- refuted=False conf=high
     Every number in [27] reproduces exactly.

exitDir (comp/ninth.js:8031-8038) and buildMap (8039-8064) read as the finding says. MAP_SEED (8030) = {square:[2,3], stage:[1,4], arena:[0,0]}, and `var MAP_POS = buildMap();` (8065) runs once at load.

Hand derivation, then confirmed by a node dump of the real literals (.claude/nnqc/skeptic27-mapdump.js, my own copy of the audit's `grab` + verbatim exitDir/buildMap):
- square->lane, data at 5105 `{x:8.5, y:14.3, w:3, to:'lane', n:'the lane, north'}`, W=17 H=15. dx: 8.5 is neither <4.25 nor >12.75, so 0. dy: 14.3 > 11.25, so +1. d=[0,+1] -> lane [2,4], one cell BELOW Wick.
- square->bernhouse [1,3], square->chandler [3,3], square->a3sq (middle door, !dx&&!dy fallback) [2,2].
- lane->mill at y=0.7 in H=17: dy=-1, wants [2,3] = the square itself. d[0]==0 so the slip loop walks the x axis off pos[lane]: slip1 -> [3,3] chandler, slip2 -> [1,3] bernhouse, slip3 -> [4,3] free. mill = [4,3], on Wick's own row, two east.
- mark->road at y=0.7 in H=11: wants [4,3] = the mill now, 1 slip -> road [5,3].
- Final visible cells, byte-identical to the finding: row2 loft[4,2] hollow[5,2]; row3 bernhouse[1,3] square[2,3] chandler[3,3] mill[4,3] road[5,3]; row4 lane[2,4] village[3,4] mark[4,4]. stage/a3sq/arena are MAP_HIDE.

The "one exception" claim holds: I walked all 18 exits. Every compass-named exit is -y=north, +y=south, +x=east, -x=west (lane->mill 0.7, mark->road 0.7, road->hollow 0.7, road->mark 33.3 "back south", lane->square 16.3 "back to Wick", village->mark 14.3 "east", village->lane 0.7 "west", mark->village 0.7 "back west"). Only square->lane is named north while sitting at +y. Note the mutual pair square<->lane both derive to [0,+1], so each place puts the other to its south — the direct contradiction.

No guard elsewhere. drawMap plots straight from MAP_POS with y increasing downward (8107 `var y = oy + m[1]*cell`), no rotation, no compass. The link and node loops apply only visited/MAP_HIDE filters, never a direction sanity check. Prompt text confirmed at 6225: `label: open ? 'go to ' + e.n : ...`, i.e. "go to the lane, north".

Screenshot confirms it end to end: .claude/nnqc/map-full-zoom.png shows the row reading "Bern's house | Wick | The chandler's shop | The mill | The road north", the lane hanging below Wick, the lane-to-mill link as an up-right diagonal, and the live exit prompt "E — go to the lane, north" rendered at the left edge behind the overlay in the same frame.

Also checked whether the world itself agrees: gotoPlace (6135-6144) walks you in at the back exit, so leaving the square at y=14.3 puts you in the lane at y=16.3-1.2=15.1 and you walk toward -y to reach the mill. So the fiction and the walking direction are both "lane north of Wick"; the exit band's placement is the sole dissenter, exactly as the finding says.
     CORRECTION: The finding stands as written. One small error, in its fix paragraph only, not in the claim: it says setting `dir:[0,-1]` on the square's lane exit "moves the lane to [2,2]... (a3sq holds it)". a3sq does not hold it at that moment — the lane is PLACES.square.exits[0] and a3sq is exits[3], so the lane takes [2,2] first and it is a3sq that slips (to [3,2]). The proposed fix therefore works better than its own note suggests: lane [2,2], mill [2,1] with no slip at all, i.e. Wick, the lane above it, the mill above that.
  --repro-- refuted=False conf=high
     Reproduced both by derivation and by screenshot.

DERIVATION: wrote .claude/nnqc/skep27-mappos.js, a verbatim copy of exitDir (comp/ninth.js:8031-8038) and buildMap (comp/ninth.js:8039-8064) run over the real PLACES literal parsed out of the source. Output matches the finding exactly: "square -> lane at (8.5,14.3) dir=0,1 slips=0 cell=2,4", "lane -> mill at (6.2,0.7) dir=0,-1 slips=3 cell=4,3", "mark -> road at (6.6,0.7) dir=0,-1 slips=1 cell=5,3". Final cells: bernhouse[1,3] square[2,3] chandler[3,3] mill[4,3] road[5,3] / lane[2,4] village[3,4] mark[4,4] / loft[4,2] hollow[5,2]. Hand-checked the exitDir arithmetic on square.exits[0] (comp/ninth.js:5105, x:8.5 y:14.3, place w:17 h:15 at comp/ninth.js:5088): 14.3 > 15*0.75 = 11.25 so dy=+1, and 8.5 is inside 4.25..12.75 so dx=0. Confirmed.

SCREEN: .claude/nnqc/skep27-map-all.png, captured against a profile built by ten successive headless loads sharing --user-data-dir (nwipe once, then ndev=each place, since gotoPlace writes S.seen['been_'+id] at comp/ninth.js:6119 and sSaves). Row 3 reads left-to-right "Bern's house | Wick | The chandler's shop | The mill | The road north"; the lane hangs below Wick; the lane->mill link is a long up-right diagonal. .claude/nnqc/skep27-map-min.png is the minimal player-real state (square + lane only) and already shows the lane drawn directly BELOW Wick on a vertical link.

REACH: M is an unconditional toggle, comp/ninth.js:1695 `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }`. Both captures drove it through nkey=m, i.e. real keydown events, not the dev handle. The lane is the second place in the game, so a player hits this within minutes. No save state, no unreachable input.

DIRECTION CONVENTION: checked every compass-named exit in the file. lane->village east x=12.3 (w13), village->lane west x=0.7, village->mark east x=14.3 (w15), mark->village west x=0.7, mark->road north y=0.7 (h11), road->mark south y=33.3 (h34), road->hollow north y=0.7, hollow->road y=12.3. north = -y, east = +x, without exception apart from square.exits[0]. The finding's "one exception" claim holds.

DUPLICATE: grepped the finding titles in _recovered.txt. [8] "(bug) The map draws the mill next door to the chandler's shop, because the square and the lane both put their shared door on their south wall" is the same defect at the same line with the same numbers and the same fixes. That is the main correction.
     CORRECTION: The defect is real and correctly described, but [27] is a DUPLICATE of [8] in the same _recovered.txt: [8] already cites PLACES.square.exits[0] at comp/ninth.js:5105, the same [0,+1] exitDir reading, the same 3-slip mill to [4,3] and 1-slip road to [5,3], the same "every other north door is on -y" survey, the same audit blind spot, and the same two fixes. [27] adds only the full cell table and the row-3 label reading. Merge them; do not count as two defects.

Two errors inside [27]'s fix note (not in the defect itself), both confirmed by running the counterfactual in .claude/nnqc/skep27-what-if.js:
1. "That alone moves the lane to [2,2]... (a3sq holds it)" is backwards. square.exits is ordered [lane, bernhouse, chandler, a3sq], so BFS places lane FIRST at [2,2] and a3sq is the one that slips, to [3,2]. That then pushes lane->village into 2 slips and village to [3,1]. So `if (e.dir) return e.dir;` plus dir:[0,-1] on square.exits[0] is still not sufficient on its own, just for a different reason than stated.
2. "y=0.7 on the square's north wall is behind the houses ... so there is no clean band there" is overstated. The square's north houses are at x 0-4, 5.2-8.8 and 13-17 (all y<3.6), leaving a clear 4.2-tile gap at x 8.8-13. `{ x: 10.9, y: 0.7, w: 3 }` spans 9.4-12.4, is unburied, and still reads dir=[0,-1] (10.9 < 17*0.75 = 12.75, so dx=0). A pure data fix with no exitDir change does exist.

=== [28] survived 2/2  severityVotes=['bug', 'bug']
  --repro-- refuted=False conf=high
     REPRODUCES, with the maths exactly right and two overstatements.

Code checked. comp/ninth.js:8023 MAP_HIDE = {arena,stage,a3sq}. 8075-8082 bounds pass is gated on `!MAP_HIDE[id] && !S.seen['been_'+id]` -> return, so on a fresh save whose only visited place is stage, `any` stays false and 8082 sets lo=hi=[0,0]. 8083-8086 then give spanX=spanY=0, cell=Math.min(100,100)=100, ox=1120/2-0=560, oy=150+130-0=280 (VW/VH=1120/580 at ninth.js:45). The node loop at 8104-8113 is gated only on `!m || MAP_HIDE[id]`, so it draws every non-hidden place at its true cell.

I re-derived MAP_POS from the audit's byte-identical buildMap copy (scratch script .claude/nnqc/rc28-mappos.js, built from .claude/ninth-night/tools/audit-geometry.js:55-85) and substituted ox/oy/cell. Result: bernhouse[1,3]=660,580; square[2,3]=760,580; chandler[3,3]=860,580; mill[4,3]=960,580; road[5,3]=1060,580 (all exactly on the bottom edge, labels at 606, off-canvas); lane[2,4], village[3,4], mark[4,4] at y=680, fully off-canvas; only loft[4,2]=960,480 and hollow[5,2]=1060,480 fully visible, both as '?'. a3sq[2,2] and stage[1,4] are hidden.

Screenshots taken, not assumed. .claude/nnqc/rc28-prologue-map.png (nwipe=1&ndev=stage&nfr=240&nkey=m&hz=1, driven through the real keydown chain): title 'WHERE YOU HAVE BEEN' and two faint dots to the right, nothing else. .claude/nnqc/rc28-prologue-clipz4.png (same URL at hz=4&hx=700&hy=435): three node domes sliced exactly by the canvas bottom edge at x approx 760/860/960, plus loft's full '?' node above them. So the clipping is real and visible.

Reach checked, and this is where the finding is overstated. (a) M is reachable during the prologue: 1695 `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }` and the prologue uses say()/beat() toasts, not a dialog, so the guard is open. Fresh save lands on stage (437), and resetGame (835) also drops back into it. (b) But M is documented nowhere: the HUD button row at 797-803 is B/K/I/V/` only, no map button; grep across comp/ninth.js, comp/comp.css and comp/index.html finds the letter M in a player-facing string exactly once, at 8116 ('M to close', i.e. only on the map itself). (c) The window is 13.4 seconds: SCRIPTS.prologue (5475-5481) is pure beat() timers, step() runs stepScene unconditionally (no mapOpen gate), so nothing the player does pauses it, and beat(13.4) calls gotoPlace('square'). gotoPlace (6116-6126) sets been_square and clears RT.beats/RT.dialog but NOT RT.mapOpen, so a map left open repaints correctly centred the moment the prologue ends. The bad state therefore heals itself.

Also miscounted: the finding says 'four half-circles clipped by the bottom edge'. It is five - bernhouse, square, chandler, mill and road are all on row 3, and road at x=1060 is still inside VW=1120.

Severity: real visual failure, but transient, self-healing, no state damage, confined to a 13.4s window at the very start behind a key the game never mentions. 'bug', not 'broken'.
     CORRECTION: Real, and the arithmetic is exact (cell=100, ox=560, oy=280; row 3 at y=580, row 4 at y=680, labels at y+26 off-canvas), but two claims need correcting. (1) FIVE nodes are clipped by the bottom edge, not four: bernhouse[1,3], square[2,3], chandler[3,3], mill[4,3] and road[5,3] all land at y=580 (road at x=1060 is still inside VW=1120). (2) "This is the first map the game will ever show anyone" does not hold. M is documented nowhere in the game - the HUD button row at comp/ninth.js:797-803 is B/K/I/V/` only, and the only player-facing mention of the key anywhere in comp/ is the map's own "M to close" at 8116 - so a player has to guess the key inside the prologue's fixed 13.4s of beat() timers (SCRIPTS.prologue, 5475-5481, which nothing pauses: step() calls stepScene regardless of RT.mapOpen). And because gotoPlace (6116-6126) sets been_square but never clears RT.mapOpen, a map left open at 13.4s repaints correctly the instant the square is entered. So this is a transient, self-healing overlay glitch in a narrow opening window behind an undiscoverable key, not the map every player meets first.
  --correct-- refuted=False conf=high
     Re-derived everything from source rather than trusting the write-up.

Mechanism, line by line:
- comp/ninth.js:8077 accumulates lo/hi only over `!MAP_HIDE[id] && S.seen['been_'+id]`. comp/ninth.js:455 `S.seen = S.seen || {}` means a cold save has no been_ flags at all; comp/ninth.js:437 `S.place = S.place || 'stage'` plus comp/ninth.js:1576 `gotoPlace(S.place, false)` boot you into the stage, and comp/ninth.js:8023 `MAP_HIDE = { arena:1, stage:1, a3sq:1 }` hides it. So `any` is false. Confirmed.
- comp/ninth.js:8082 fallback lo=hi=[0,0] -> spanX=spanY=0 -> comp/ninth.js:8084 `spanX ? 620/spanX : 100` takes the 100 branch on both axes, cell = max(46, min(100,100,100)) = 100. comp/ninth.js:8086 ox = 1120/2 - 0 = 560, oy = 150+130 - 0 = 280 (VW/VH are 1120/580 at comp/ninth.js:45). All three numbers in the finding are right.
- comp/ninth.js:8104-8113 node loop is gated on `!m || MAP_HIDE[id]` only, no been_ test, and `here` is false everywhere because RT.place === 'stage' is hidden, so every node is r=9.
- I ran buildMap()/exitDir() standalone against the real PLACES literal (scratch file C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/f28-check.js, same grab() trick as audit-geometry.js). MAP_POS: square[2,3] bernhouse[1,3] chandler[3,3] lane[2,4] mill[4,3] loft[4,2] village[3,4] mark[4,4] road[5,3] hollow[5,2] — columns 1..5, rows 2..4, exactly as claimed. Screen positions with ox=560/oy=280/cell=100: bernhouse(660,580) square(760,580) chandler(860,580) mill(960,580) road(1060,580) all sit exactly on y=VH; lane(760,680) village(860,680) mark(960,680) are fully off; loft(960,480) and hollow(1060,480) are the only two fully visible.
- drawMap is called unconditionally from draw() at comp/ninth.js:3921, and M is gated only on `!RT.dialog` at comp/ninth.js:1695, so nothing else prevents it.

Reproduced on screen, driving M through the real keydown chain, not nmap:
- .claude/nnqc/sk28-prologue-map-z1.png (ndev=stage&nfr=300&nkey=m&hz=1): the veil, the title, and two '?' dots near the right edge. Nothing else.
- .claude/nnqc/sk28-prologue-map-edge4.png (hz=4&hx=610&hy=445): three node domes sitting on the canvas bottom edge with only their top halves drawn, labels gone. Clipping proved.

I looked for a guard elsewhere and found none: gotoPlace (comp/ninth.js:6116) never clears RT.mapOpen (only resetGame at :820 and the keydown handler at :1677/:1695 write it), and no autoscale saves it because cell is already at its 100 ceiling.

Why I downgrade broken -> bug: the window is bounded and self-healing. SCRIPTS.prologue (comp/ninth.js:5480) queues `beat(13.4, gotoPlace('square', true))`, stepScene (comp/ninth.js:5994-5997) runs beats every frame with no mapOpen gate, and gotoPlace sets been_square, so within at most 13.4 s the map re-lays itself onto lo=hi=[2,3] -> ox=360, oy=-20, square back at dead centre (560,280), even if the player leaves the map open across the transition. On top of that M appears nowhere in the HUD (render() at comp/ninth.js:797-803 lists B/K/I/V/` and no M) and 'map' appears in no player-facing string, so the screen is undiscoverable. And every node that lands off-canvas is a node finding [29] says should not be drawn at all, so this is the same root cause seen from a second angle rather than an independent break.
     CORRECTION: The maths and the repro are correct. Three details are off. (1) Five nodes are clipped by the bottom edge, not four: bernhouse(660,580), square(760,580), chandler(860,580), mill(960,580) and road(1060,580) all have x within 0..1120 and all show only their top halves. (2) "Every label sits at y+26 = 606, also off-canvas" is true only of that y=580 row; loft and hollow sit at y=480 and their '?' labels at y=506 are on canvas, which the finding's own screenshot description concedes. (3) "M to close" is drawn at comp/ninth.js:8116 `cx.fillText('M to close', VW/2, VH-40)`, i.e. x=560, dead centre, not "sideways of centre"; what is off-centre is the map content, not the hint. Finally the state is time-boxed and self-correcting: SCRIPTS.prologue (comp/ninth.js:5480) fires gotoPlace('square', true) at t=13.4 s regardless of what the player does, which sets been_square and re-centres the map on the spot, even with the map still open.

=== [29] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     Re-derived everything from source rather than trusting the finding.

CODE, comp/ninth.js:8104-8114. Node loop guard is `var m = MAP_POS[id]; if (!m || MAP_HIDE[id]) return;` — no `S.seen` test, confirmed verbatim at 8105. Compare the link loop at 8090, `if (!MAP_POS[id] || !S.seen['been_' + id] || MAP_HIDE[id]) return;`, and the bounds loop at 8077, `if (!m || MAP_HIDE[id] || !S.seen['been_' + id]) return;`. Both of those gate on visited; the node loop is the only one of the three that does not. So the claim is literally what the code does.

NO GUARD ELSEWHERE. `S.seen['been_' + id]` has exactly one writer, comp/ninth.js:6119 inside gotoPlace. The map key is ungated apart from dialogue: comp/ninth.js:1695, `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }`. drawMap's only early return is `if (!RT.mapOpen) return;` at 8068. Nothing upstream filters MAP_POS by knowledge; buildMap (8039-8065) assigns a cell to every non-MAP_HIDE place unconditionally, including the unreachable-fallback pass at 8059-8063.

DERIVED MAP_POS INDEPENDENTLY. I re-implemented exitDir/buildMap in a scratch script (.claude/nnqc/skep29-mappos.js, evaluating the real PLACES/MAP_SEED/MAP_HIDE literals out of comp/ninth.js) and got: square [2,3], bernhouse [1,3], chandler [3,3], lane [2,4], mill [4,3], loft [4,2], village [3,4], mark [4,4], road [5,3], hollow [5,2]; stage [1,4], a3sq [2,2], arena [0,0] all MAP_HIDE. Ten visible cells spanning columns 1..5 and rows 2..4, i.e. 5 by 3, exactly as the finding states.

SIMULATED THE COLD SAVE. seen = {stage, square}, here = square. Bounds loop sees only square, so lo = hi = [2,3], spanX = spanY = 0, cell = min(100, 100) = 100, ox = 560 - 2*100 = 360, oy = 280 - 3*100 = -20. Nodes land at (560,280) Wick gold, (460,280) (660,280) (760,280) (860,280) (760,180) (860,180) (560,380) (660,380) (760,380). All ten inside 1120x580. Link pass emits exactly three edges, all dashed: square->lane, square->bernhouse, square->chandler. Untouched by any edge: mill, loft, village, mark, road, hollow — the six the finding names, exactly.

SCREENSHOT. Fresh capture, not the round-one one: .claude/nnqc/sk29-cold-map-z1.png (hz=1) and .claude/nnqc/sk29-cold-map-z2.png (hz=2, hx=420 hy=140), driven with nwipe=1&ndev=square&nfr=300&nkey=m through the real keydown chain. Ten circles are plainly visible against the dimmed scene, one gold labelled "Wick", nine dark with '?' beneath. The zoom shows three dashed spokes off Wick and six circles with no line touching them. Counts match the simulation exactly.

The hollow being the ending is confirmed by the source comment at comp/ninth.js:5259-5260, "the hollow. The one place in the game that should feel wrong. Job 1 puts the ending here" — but as noted in the correction, its node is an indistinguishable '?'.
     CORRECTION: Real and the mechanism is exactly as described, but the framing overstates what the player actually learns, and the severity is presentational rather than a behavioural bug.

Two corrections:

1. "The hollow in particular is the game's last location and it is a dot on the map before the player has left Wick" is overstated. Unseen nodes render as an anonymous '?' (comp/ninth.js:8113, `cx.fillText(seen ? PLACES[id].n.split('—')[0].trim() : '?', x, y + 26)`) in flat colours identical across all nine. Nothing distinguishes the hollow's dot from the loft's. What actually leaks is the cardinality and the arrangement: "there are ten places, laid out 5 columns by 3 rows, and Wick is not near the edge". The hollow is not identifiable, so the ending is not spoiled; the world's size is.

2. Severity is polish, not bug. The unseen state is deliberately authored code, not an omission: line 8108 has a three-way fill (`here ? '#c9a94a' : seen ? '#3d3350' : '#1a1620'`), 8110 a three-way stroke, 8111 a three-way label colour, 8113 the '?' fallback. Somebody wrote an "unvisited node" look on purpose. The only genuinely inconsistent part is that the link loop gates on knowledge (8090 source must be visited, 8094 far end dashed if not) while the node loop gates on nothing, so the map draws six nodes the player has not even heard of. That is an information-design inconsistency on a screen that still functions correctly, renders correctly and orients correctly. Nothing is wrong, misplaced or unreadable.

Note also that the more damaging consequence of this same ungated loop, the prologue map drawing off the bottom of the canvas, is already finding [28] and is already rated broken. [29] standing alone is the residual in-game case.

Suggested rewrite of the title: "(polish) The map's node loop has no knowledge gate, so a cold save shows the world's full extent as nine anonymous '?' dots, six of them attached to nothing".
  --repro-- refuted=False conf=high
     Reproduced fully. comp/ninth.js:8103-8105 node loop guard is verbatim `var m = MAP_POS[id]; if (!m || MAP_HIDE[id]) return;` with no visited test, while the bounds pass (8077) and the link pass (8090) both carry `!S.seen['been_' + id]`. I re-derived MAP_POS with my own copy of buildMap/exitDir (.claude/nnqc/f29-mappos.js): 10 non-hidden nodes, columns 1..5, rows 2..4; cold save gives cell=100, ox=360, oy=-20; square's map-visible exits are lane, bernhouse, chandler only (a3sq is MAP_HIDE), so mill, loft, village, mark, road, hollow have no link at all. Capture .claude/nnqc/f29-cold-map-full.png (nwipe=1&ndev=square&nfr=300&nkey=m) shows 1 gold disc and 9 dim discs; pixel-sampled at the predicted centres every one of the 9 reads fill (26,22,32)=#1a1620 and ring (36,31,46)=#241f2e over a background of ~(8,7,12), i.e. the unseen branch at its true cell. .claude/nnqc/f29-cold-map-z4.png shows the '?' glyphs. Reach is fine: comp/ninth.js:1695 `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }` is ungated from the first frame, driven here through the real keydown chain via nkey=m. So the mechanism, the counts and the reachability all hold; I could not refute it. What does not hold is the 'bug' severity, see correction.
     CORRECTION: Real, reachable and numerically exact, but it is a design/spoiler judgement rather than a defect, so polish not bug. (1) The unvisited node style is deliberately authored, not a missing guard: comp/ninth.js:8108-8113 carries three separate `seen ? ... : ...` ternaries for fill, stroke and label plus the literal '?' glyph. It is also load-bearing: on the cold save the three heard-of destinations at the far ends of the dashed roads (bernhouse, chandler, lane) MUST render as '?' nodes for the dashed-road idiom at 8094 ('one you have only heard of is dashed') to point at anything. So the questionable set is the 6 orphan discs, not the 9 the finding counts as the problem. (2) The leak is smaller than stated. Measured off the capture, the orphan discs are #1a1620 fill with a #241f2e ring on a ~(8,7,12) background: a WCAG contrast ratio of about 1.12:1 for the fill and 1.25:1 for the ring, and the label is a bare '?' at #3a3446. A player learns the world has 10 cells in a 5x3 lattice; they learn no name, no content and no route. 'The hollow in particular is the game's last location and it is a dot on the map' is true only in the sense that an anonymous dim dot exists at that cell, indistinguishable from the other five. (3) Novelty is partly shared: the same missing guard at 8104 is already logged as the root cause of [28] (broken, the prologue map drawn off the bottom of the canvas), and [28]'s fix note already says fixing this loop removes it. [29]'s independent contribution is only the spoiler argument. The proposed fix (a `known` set built in the link pass) is the right shape and would leave 4 nodes on the cold map.

=== [30] survived 2/2  severityVotes=['risk', 'risk']
  --repro-- refuted=False conf=high
     Ran the repro rather than reading it. `node .claude/ninth-night/tools/audit-geometry.js` prints "13 places, 7 people, 19 exits, 16 things to look at / geometry clean" today, confirmed.

Instrumented copy of the audit's own buildMap (written to .claude/nnqc/skeptic30-slip.js, parses PLACES/MAP_SEED/MAP_HIDE straight out of comp/ninth.js the same way the audit does) reproduces the claimed numbers character for character:
  SLIP lane -[the mill]-> mill dir=0,-1 want=2,3 (held by square) slips=3 got=4,3
  SLIP mark -[north, the way she went]-> road dir=0,-1 want=4,3 (held by mill) slips=1 got=5,3
Every other edge is slips=0. Final cells: bernhouse[1,3] square[2,3] chandler[3,3] mill[4,3] road[5,3] / lane[2,4] village[3,4] mark[4,4] / a3sq[2,2] loft[4,2] hollow[5,2], matching [27].

Code claims check out at the stated lines. audit-geometry.js:63-87 is buildMap; `slip` is declared at line 71 and read only at 72-75, never returned, never asserted on — buildMap returns `pos` alone (line 86). Grepping the whole 300-line audit for MAP_POS/exitDir/slip returns only lines 46,54,55,65,71-75,88,256,264,266: there is no direction assertion anywhere in the file.

The mutual-pair claim is also true and is stronger than the finding lets on. My pair test (every A->B with a matching B->A, requiring exitDir(A,e) === -exitDir(B,back)) flags exactly three pairs: bernhouse<->square, chandler<->square (both interior doors, exactly the whitelist the fix predicts) and lane<->square, both [0,+1]. So the proposed two-line test isolates [27]'s root cause and nothing else in the current world.

The proposed fix's mechanism also holds: the slip loop never writes to the requested cell, so after it runs `taken[want.join(',')]` still names the blocker — 'square' for the mill, 'mill' for the road, i.e. the exact message text the fix prints.

Reach: this is a tooling risk, not a player-facing one, and the brief explicitly sanctions audit holes as findings. The audit's own header says "Exit code 1 if anything is wrong, so it can gate a commit", so the gate is hit by every maintainer run and it is green while the map is wrong. Not refutable on reach.

Three small imprecisions, none of them load-bearing (see correction).
     CORRECTION: Real and correctly diagnosed; keep at risk. Three corrections to the wording:

1. "263-269 (the only map check)" — there are two map assertions, not one. Line 256-258 also fires if a place got no cell at all ("the map derivation gave it no cell"). It is just as dead as the duplicate check: the spare loop at 79-85 hands a cell to everything not MAP_HIDE, and the only MAP_HIDE place not in MAP_SEED (a3sq) is reached by BFS from square, so it gets [2,2]. Neither assertion says anything about position, which is the finding's actual point and survives.

2. "A slip>0 ... is the only signal available" is too strong, and the finding contradicts it two sentences later. The root lie in [27] — the lane placed SOUTH of Wick from square.exits[0] at y=14.3 — has slip=0 and got exactly the cell it asked for. A slip check alone would flag the mill and the road (the downstream displacement) but would never point at the lane, which is the cause. The mutual-pair check is the one that finds the cause; the slip check finds the symptom. Both are worth adding, but the fix should lead with the pair check.

3. "byte-equivalent to the game's" is loose: the audit's spare loop iterates Object.keys(PLACES) where the game iterates PLACE_IDS. Since PLACE_IDS is `Object.keys(PLACES)` (comp/ninth.js:5295) the two derivations are output-identical, which is the claim that matters, but the text is not byte-identical.

Also, implementing the fix as written needs `var want = c.slice()`, not `var want = c`: the slip loop mutates `c` in place, so an alias would make `want` and `got` print the same cell and the check would never fire.
  --correct-- refuted=False conf=high
     Verified every claim against source. (1) The audit's only result-side map assertions are audit-geometry.js:256 (`if (!MAP_POS[id])`) and 263-268 (duplicate visible cell); `slip` appears only inside the copied buildMap at lines 71-76 and is never read after the while loop. grep for MAP_POS|MAP_HIDE|exitDir|buildMap|slip returns nothing else, and no other file in .claude/ninth-night/tools/ touches the map. (2) The audit's exitDir (55-61) and buildMap (63-87) are identical to the game's at comp/ninth.js:8031-8065; the only textual difference is the spare loop using Object.keys(PLACES) where the game uses PLACE_IDS, and comp/ninth.js:5295 is `var PLACE_IDS = Object.keys(PLACES);`, so they are the same list in the same order. (3) I instrumented a copy (.claude/nnqc/skept30-slip.js) to capture want/heldBy/slip and got exactly the finding's numbers: `lane -[the mill]-> mill dir=0,-1 want=2,3 held=square slip=3 got=4,3` and `mark -[north, the way she went]-> road dir=0,-1 want=4,3 held=mill slip=1 got=5,3`. Hand-derived independently: lane is w13 h17 (comp/ninth.js:5154) with its mill exit at (6.2,0.7) (5167), 0.7 < 17*0.25=4.25 so dy=-1, and 3.25 < 6.2 < 9.75 so dx=0; lane sits at [2,4] so want=[2,3], which MAP_SEED pins to square (comp/ninth.js:8029). Since d[0]==0 the slide runs the x axis: slip1 -> [3,3] chandler, slip2 -> [1,3] bernhouse, slip3 -> [4,3] free. mark is w13 h11 with its road exit at (6.6,0.7) -> dir [0,-1], mark at [4,4], want [4,3] now held by mill, slip1 -> [5,3]. (4) The rendered map confirms it: .claude/nnqc/map-full-zoom.png row 3 reads Bern's house, Wick, The chandler's shop, The mill, The road north, with the mill drawn due east of a shop interior even though it is reached by walking north out of the lane. (5) The mutual-pair claim holds: no such check exists, and square->lane (exit 8.5,14.3 of 17x15) and lane->square (exit 6.2,16.3 of 13x17) both derive [0,+1]; 6 of 19 exits fail an opposite-direction test today (4 of them interior doors, as the finding predicts). `node .claude/ninth-night/tools/audit-geometry.js` prints "13 places, 7 people, 19 exits, 16 things to look at / geometry clean" with all of this in place. Mechanism, line numbers, worked numbers and repro are all correct.
     CORRECTION: Real, with three small overstatements to trim. (a) "no two visible places share a cell, which the slip loop guarantees by construction" is not literally true: the slide bails at `slip < 12` (13 candidate cells on one row) and then assigns the colliding cell regardless, and the spare loop bails at `spare < 40`, so the duplicate check is a genuine exhaustion backstop, merely unreachable with 13 places. (b) audit-geometry.js:263-269 is not "the only map check"; line 256 (`if (!MAP_POS[id])`) is also one, though it is equally blind to direction since the spare loop hands every non-hidden place a cell. (c) "A slip>0 ... is the only signal available" is contradicted by the finding's own next sentence: square<->lane misplaces the lane with slip=0, so slip>0 catches one class of lie and the mutual-pair test catches another. The substantive claim survives all three: the audit's derivation agrees with the game's byte for byte, yet asserts nothing about whether a place ended up where its exit pointed, and two of the ten graph placements (mill slip=3, road slip=1) had their cell chosen by collision rather than direction while the tool reports "geometry clean".

=== [31] survived 2/2  severityVotes=['polish', 'polish']
  --correct-- refuted=False conf=high
     Re-derived from source, not from the finding. comp/ninth.js:8023 `var MAP_HIDE = { arena: 1, stage: 1, a3sq: 1 };`. drawMap's node loop is comp/ninth.js:8104-8114; line 8105 is `var m = MAP_POS[id]; if (!m || MAP_HIDE[id]) return;` and 8106 is `var seen = S.seen['been_' + id], here = RT.place === id;`. When RT.place === 'a3sq', the only id for which `here` could be true is returned out of one line before it is tested, so `here` is false for every drawn node and `cx.fillStyle = here ? '#c9a94a' : seen ? '#3d3350' : '#1a1620'` (8108), the stroke (8110) and the label colour (8111) never take the gold branch. Mechanism confirmed exactly as claimed.

Searched for a guard elsewhere and found none. The map key is comp/ninth.js:1695, `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }` — the only block is an open dialog, and Act 3 narration goes through say() at comp/ninth.js:1509, which appends to the .nn-say DOM ticker and never touches RT.dialog (the only writers of RT.dialog are 6126, 6275, 6277, 6313, none in the a3 path). drawMap is called unconditionally from draw() at comp/ninth.js:3921. There is no pin/lockout mechanism (the only `unpin` is comp/ninth.js:1477 and it filters RT.lines, not input).

Reachability in real play confirmed: comp/ninth.js:5108 is the square exit `{ x: 8.7, y: 6.6, w: 2.4, to: 'a3sq', needs: 'a3ready', over: 1 }` and `S.seen.a3ready = 1` is granted at comp/ninth.js:5318 once fragCount() === 3. a3sq (5279-5288) has oneway: 1 and exits: [], and a3Resume (5641) puts a reload back there, so the player is in a3sq for the whole act and across reloads.

Reproduced empirically. Built a persistent-profile save by loading square (nwipe=1) then lane, mill, loft, village, mark, road, hollow, bernhouse, chandler with --user-data-dir, then took two final captures from that same profile with nkey=m: .claude/nnqc/skep31-full-square.png shows the identical node layout with Wick's node gold (#c9a94a fill, #ffe66e stroke and label, radius 12), and .claude/nnqc/skep31-full-a3sq.png shows the same layout with every node purple-grey and no gold pixel anywhere. A cold-profile pair (.claude/nnqc/skep31-a3sq-map.png) also shows no gold and confirms the unvisited-node correction above.

Not covered by the static audit: audit-geometry checks "place missing from the map" and "two places on one map cell", neither of which touches the here-highlight, and a3sq is exempt from the map checks by MAP_HIDE anyway. Not a duplicate of any other block in _recovered.txt: [32], [33], [34] and [35] are the other map findings and none of them concerns RT.place highlighting.

Severity: the mechanism is real and verified, so not refuted. But it is a missing highlight on an optional informational overlay, in the one place in the game with no exits and no travel, with no incorrect information shown and no progression impact. That matches the polish band that [32] and [33] occupy, not bug.
     CORRECTION: Real, but the severity and the framing are overstated. Two corrections. (1) "Every node draws in the flat 'seen' colours" only holds on a fully-visited save; on a partial save the unvisited nodes still draw in the dark #1a1620/#241f2e/'?' state, because the node loop at 8104-8106 filters on MAP_HIDE only, not on seen. (2) The impact framing "a screen whose one job is orientation" does not apply in a3sq. a3sq is oneway with exits: [] (5279-5288) and the square door back is permanently shut once the ending commits (`if (e.over && S.a3.ending) return false`, 6102), so there is nowhere to orient toward during the act. The overlay's own stated job, "WHERE YOU HAVE BEEN", is still discharged correctly and completely; what is missing is the secondary "you are here" highlight. Nothing false is drawn, nothing is blocked, no save state is affected. Corrected statement: "During Act 3 the map overlay draws no gold current-location node, because RT.place is 'a3sq' and a3sq is in MAP_HIDE, so the `here` branch at 8106 is unreachable for every node that survives the 8105 filter. Visited nodes render in the ordinary seen colours and unvisited ones in the unseen colours. The map still records the journey accurately; only the you-are-here marker is absent, on a screen that has no navigational function in a place with no exits." The proposed fix (MAP_STANDIN = { a3sq: 'square' }) is correct and would light the node already labelled 'Wick'.
  --repro-- refuted=False conf=high
     MECHANISM, CHECKED AT THE LINES. comp/ninth.js:8023 `var MAP_HIDE = { arena: 1, stage: 1, a3sq: 1 };`. comp/ninth.js:8104-8106 the node loop: `PLACE_IDS.forEach(function (id) { var m = MAP_POS[id]; if (!m || MAP_HIDE[id]) return; var seen = S.seen['been_' + id], here = RT.place === id;`. The early return fires for a3sq before `here` is evaluated, and `here` is only ever true for the id equal to RT.place, so while RT.place === 'a3sq' no node can take the here branch. Exactly as claimed.

REPRODUCED ON REAL PIXELS, NOT ON THE DEV HANDLE. The shared server on 8492 does NOT serve this worktree: it serves .claude/worktrees/suspicious-driscoll-00fbd8 (a 15587-line comp/ninth.js, md5 ebeb8d93... vs this worktree's 8279-line 6868ebe2...). I served this worktree myself on 127.0.0.1:8593, rebuilt a fully-visited profile there (10 loads under one --user-data-dir), then ran a probe that presses M through the REAL keydown chain (root.dispatchEvent KeyboardEvent 'm', the comp/ninth.js:1695 path) and scans the canvas ImageData for drawMap's here colours #c9a94a (fill) and #ffe66e (stroke/label).
  ndev=square : place=square mapOpen=true dialog=false, hereFill_c9a94a=349 px in a 22x22 box at (449,269)-(470,290), hereStroke_ffe66e=102 px
  ndev=a3sq   : place=a3sq   mapOpen=true dialog=false, hereFill_c9a94a=0, hereStroke_ffe66e=0, goldBox=null
seenFill_3d3350 rises 1621 -> 1800 and seenStroke_6a5f82 509 -> 569 across the two, which is Wick's node moving from gold into the flat seen palette. Screenshots agree: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk31-map-square.png has a gold, larger 'Wick' node with a gold label; C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk31-map-a3sq.png is the same layout with Wick indistinguishable from Grelling and Bern's house. Probe page: C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/sk31-probe.html. DOM dumps: .claude/nnqc/_sk31dom-square.txt and .claude/nnqc/_sk31dom-a3sq.txt.

REACH. a3sq is on the main story path: comp/ninth.js:5108 the square's fourth exit `{ x: 8.7, y: 6.6, w: 2.4, to: 'a3sq', n: 'up the steps, onto the stage', needs: 'a3ready', over: 1 }`, and S.seen.a3ready is set by Bern at comp/ninth.js:5318 once fragCount() === 3. M is live there: comp/ninth.js:1695 `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }`, and the probe confirms dialog=false with mapOpen going false->true in a3sq. The act 3 watcher already treats an open map as a real state, comp/ninth.js:5705 `var mute = RT.devOpen || RT.dialog || RT.mapOpen || RT.panel;`, so the authors expect a player to be able to do this. Reachable with no save state or input that does not exist.

WHERE IT IS OVERSTATED. (1) 'for the whole of the final act' is wrong. I probed every na3 beat: crowd/cue/verse all report place=a3sq gold=0, but hal/true/silence all report place=square gold=349. a3Home (comp/ninth.js:5889-5891) does `S.place = 'square'; sSave(); gotoPlace('square', false);` once the ending resolves, so the marker comes back. The window is the performance itself, not the act. (2) 'Every node draws in the flat seen colours' holds only for visited places; unvisited ones still draw #1a1620 with a '?' (comp/ninth.js:8108-8113). (3) Severity: nothing is misdrawn or misinformative, a highlight is simply absent, on an optional overlay in the one place with `exits: []` (comp/ninth.js:5288), so the map has no navigational job there. That puts it beside [32] and [33] (both polish), not beside [29] (bug: nodes for places never heard of, real information leak) or [28] (broken: panel drawn off the canvas). The proposed MAP_STANDIN fix is sound and cheap; what it fixes is cosmetic.
     CORRECTION: Real and reproduces exactly, but two details are loose and the severity is one step too high. It is not 'the whole of the final act': a3Home at comp/ninth.js:5889-5891 sends the player back to the square once the ending resolves and the gold marker returns (verified: na3=hal/true/silence all report RT.place === 'square' with 349 px of #c9a94a on the canvas, while na3=crowd/cue/verse report a3sq with 0). The affected window is the performance itself. Also, only VISITED nodes drop to the flat seen palette; unvisited ones still draw #1a1620 with a '?'. Corrected statement: 'While the player is in a3sq during the act 3 performance, opening the map shows no you-are-here node anywhere, because a3sq is in MAP_HIDE and drawMap returns at comp/ninth.js:8105 before evaluating here = RT.place === id. Wick draws in the same flat seen colours as Grelling. The marker returns when a3Home puts the player back in the square after the ending.' Severity polish, not bug: nothing is misdrawn, only a highlight is missing, on an overlay with no navigational function in a place that has no exits.

=== [32] survived 2/2  severityVotes=['polish']
  --correct-- refuted=False conf=high
     Re-derived from source, not from the finding. comp/ninth.js:8031-8038 exitDir, 8039-8064 buildMap, 8023 MAP_HIDE = {arena, stage, a3sq}, node draw 8104-8114. square.exits at 5104-5115: bernhouse door x=2.1 y=3.6, chandler door x=15 y=3.8, with W=17 H=15 (5090). indoor:1 is at 5119 and 5137, exactly as cited.

Bernhouse: 2.1 < 17*0.25 = 4.25 so dx=-1; 3.6 < 15*0.25 = 3.75 so dy=-1; both set, tie-break |2.1-8.5|/17 = 0.3765 > |3.6-7.5|/15 = 0.26, so dy=0. WEST, no slip, cell [1,3]. Correct as stated. Chandler: 15 > 12.75 so dx=1; 3.8 < 3.75 is false so dy=0. EAST, cell [3,3]. Correct, and 3.8 is indeed 0.05 past the 3.75 line.

I evaluated the real PLACES literal (lines 5077-5294) and the real MAP_SEED/exitDir/buildMap source (8023-8065) in node rather than trusting the finding. Result: bernhouse [1,3], square [2,3], chandler [3,3], mill [4,3], road [5,3], lane [2,4], village [3,4], mark [4,4], loft [4,2], hollow [5,2]. Row 3 is exactly "Bern's house | Wick | The chandler's shop | The mill | The road north". The mill's [4,3] is the slip loop stepping over [2,3], [3,3] and [1,3] from lane [2,4], so the two interiors do push it two cells, as claimed.

No guard anywhere. grep for MAP_HIDE/MAP_POS shows only 8023, 8060, 8066, 8077, 8090, 8092, 8098-8099, 8105 - no indoor test. grep for "indoor" shows it is read only at 7834 and 7863, both in world drawing, never in the map. Node draw at 8108-8113 is unconditional on kind: radius 9 (12 when current), same stroke, same 10px "Pixelify Sans" label from PLACES[id].n.split. Roads at 8090-8100 are solid whenever both ends are visited, so square-bernhouse and square-chandler are solid horizontal segments.

Screenshot .claude/nnqc/map-full-zoom.png shows precisely this: five identical circles on one row, two of them rooms.

Robustness claim also checks out and is if anything understated: even if the chandler door slipped to y <= 3.75 and dy became -1, the tie-break |15-8.5|/17 = 0.382 > |3.7-7.5|/15 = 0.253 still zeroes dy and keeps it EAST.

Nothing to refute. Every number and every line reference is right.
     CORRECTION: Two framing details are imprecise, though the substance stands. (1) "in the middle of the Wick-to-mill line": there is no Wick-to-mill road. The mill's only drawn links are the diagonal to the lane [2,4] and the vertical to the loft [4,2]. The chandler sits between Wick and the mill in row order and reading order, not on a drawn line. (2) "the overland map": the code's own doc comment at 8020-8022 frames this as a map "of where you have been", derived from the exit graph, so an interior having a node is by design. The actual defect is narrower and still real: indoor places get no visual distinction at all (same radius, same fill/stroke palette, same label font, same solid road), so on a grid-derived layout a room reads as a peer settlement. Restated: "The map draws indoor places with the same node and label treatment as outdoor places, so Bern's front room [1,3] and the chandler's shop [3,3] sit on row 3 as peers of Wick, the mill and the road north."
  --repro-- refuted=False conf=high
     I re-derived the whole thing rather than trusting the text.

exitDir maths, by hand against ninth.js:8031-8038 with square w=17 h=15 (W*0.25=4.25, W*0.75=12.75, H*0.25=3.75, H*0.75=11.25):
- exits[1] bernhouse (ninth.js:5107) x=2.1 y=3.6: dx=-1 (2.1<4.25) and dy=-1 (3.6<3.75), both set, so the tie-break at 8035 runs: |2.1-8.5|/17 = 0.3765 > |3.6-7.5|/15 = 0.26, therefore dy=0. Direction WEST.
- exits[2] chandler (ninth.js:5108) x=15 y=3.8: dx=1 (15>12.75); 3.8 is 0.05 past the 3.75 line so dy=0 outright. Direction EAST.
Both match the finding, including its note that no slip is needed.

I then ran buildMap verbatim over the real literals (scratch script at C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/.claude/nnqc/f32-mappos.js, loader copied from the audit tool). MAP_POS comes out square [2,3], bernhouse [1,3], chandler [3,3], mill [4,3], road [5,3], lane [2,4], village [3,4], mark [4,4], loft [4,2], hollow [5,2]. taken[] is never hit for either interior, so the finding is right that no slip is involved. It is also right that the mill's slip has to hop [3,3] and [1,3]: lane [2,4] aims at [2,3] (occupied by square), slips to [3,3] (chandler), [1,3] (bernhouse), then lands [4,3].

Row 3 is therefore bernhouse | square | chandler | mill | road, exactly as claimed.

Draw path, ninth.js:8104-8113: no branch on indoor, on floor, or on anything else. arc radius 9 (12 if you are standing there), fill #3d3350 / stroke #6a5f82 when seen, font '10px "Pixelify Sans"', label PLACES[id].n.split(em dash)[0].trim(). Identical for bernhouse, chandler and Grelling. Confirmed.

Screenshots: the cited .claude/nnqc/map-full.png shows the row, and comp/ninth.js has not been touched since (last change 69ca8b6 at 10:29, that shot at 11:08). I did not rely on it - I re-captured from scratch with a fresh persistent Chrome profile, six loads (nwipe=1&ndev=square, then bernhouse, chandler, mill, road, then ndev=square&nmap=1), producing .claude/nnqc/_sk32-map.png and the hz=2 crop .claude/nnqc/_sk32-maprow.png. The crop reads "Bern's house  Wick  The chandler's shop  The mill  The road north" across one line at identical node size and label weight. Reproduces.

Reach: square exits[1] and exits[2] carry no `needs` (ninth.js:5107-5108), so both interiors are two keypresses from the opening screen. M toggles the map unconditionally outside dialogue (ninth.js:1695, `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }`). No save state, no dev flag, no unreachable input. Any player who goes through Bern's door and the chandler's door and presses M sees it. Note also that unvisited places are drawn too (8104-8105 has no `seen` gate, only a '?' label), so even a cold player sees the row's shape.

Not refuted. The mechanism, the maths, the screenshot and the reachability all hold; only the "overland map" framing and the "two doors past" line are overstated, and the suggested `indoor` test misses the loft.
     CORRECTION: Real and reproducible, but two of its claims need trimming.

(a) The finding calls this "the overland map". The panel is titled 'WHERE YOU HAVE BEEN' (ninth.js:8072) and the comment above MAP_HIDE (ninth.js:8020-8023) states the exclusion rule explicitly: "neither is somewhere you can walk, so neither belongs on a map of where you have been". By the file's own stated criterion the two interiors DO belong on it - they are somewhere you can walk. So the "alternatively add them to MAP_HIDE" half of the proposed fix contradicts the design rule written into the file. Only the "draw indoor nodes differently" half is consistent with it.

(b) The punchiest sentence, "A player looking for the mill finds it two doors past a shop interior", overstates it. No road is drawn between the chandler and the mill, and none between Bern's house and anything but Wick. Exit graph: bernhouse->square, chandler->square, mill->lane,loft. drawMap draws one line per exit (ninth.js:8091-8101), so the solid line from Wick dies at each interior node and the mill hangs off the lane below. Confirmed in .claude/nnqc/_sk32-maprow.png. The five cells share a row by layout coincidence, and the drawn roads already say so. What actually survives is the narrower claim: two interiors are styled identically to settlements (same radius 9, same fill/stroke palette, same 10px Pixelify Sans label, ninth.js:8107-8113), with nothing on the node saying "this is a room inside the place next to it".

(c) The proposed `indoor: 1` test is incomplete. `indoor` is a lighting flag, read only at ninth.js:7834 and 7863. The grain loft is also an interior, reached only through the mill, and carries no `indoor` (ninth.js:5192 is `floor: 'loft', w: 13, h: 13, night: 1, dark: 1`). Testing `indoor` would restyle two of the three interiors and miss the loft at [4,2].

Everything else in the finding is exactly right, including the parenthetical that refutes the brief's collision guess.

=== [33] survived 2/2  severityVotes=['polish', 'polish']
  --repro-- refuted=False conf=high
     Code checked, all three cited lines are exact. comp/ninth.js:8069 `cx.fillStyle='rgba(6,4,10,.9)'; cx.fillRect(0,0,VW,VH)` dims the canvas only. comp/ninth.js:769 emits `<div class="nn-hud">` as a DOM sibling of `.nn-cv`, and comp/comp.css:2939 gives it `position:absolute; bottom:10px`. Nothing anywhere hides or dims it while the map is up: `grep -n "nn-hud" comp/ninth.js` returns only line 769, and every one of the 17 `mapOpen` sites is logic (movement, input, sfx mute), never a class or style. comp/ninth.js:8116 draws the hint at VH-40 = y 540, inside that band.

Reachability is trivial: comp/ninth.js:1695, `else if (k === 'm') { if (!RT.dialog) RT.mapOpen = !RT.mapOpen; }`. Press M anywhere outside dialog.

Reproduced twice. NOTE: the finding's repro URL is on port 8492, which today serves a DIFFERENT build (872 KB ninth.js vs this worktree's 475 KB, md5 mismatch). Ports 8493/8497 serve this worktree. On 8493 the harness repro is exact: .claude/nnqc/sk33-map-hint-z4.png shows 'M to close' pinched between the head word chip and the '3 ERD' rhyme key, and .claude/nnqc/sk33-map-full.png shows the whole HUD (breath bar, chips, 1..5 keys, stanza buttons, B/K/I/V) at full brightness over the dimmed map.

Because `.nn-cv` is `width:100%;height:100%;object-fit:contain` (comp/comp.css:2919), the harness's 1:1 host is not the shipped geometry, so I measured the real one. Probe .claude/nnqc/sk33-realwin.html mounts the game in the real `.win` box (comp/comp.js:8559 `w:1160,h:720`; 40px `.win-bar` + 1px borders). Measured: `.nn` 1158x678, bitmap scale 1.0339, letterboxed to y 80.2..679.8, so canvas y=540 lands at DOM y=638.5 while `.nn-hud` spans y 627..709 and `.nn-hand` spans 627..673. `elementFromPoint` across the full 44px text run returns `.nn-hand` at every sample. Visual at .claude/nnqc/sk33-realwin-hudzoom.png: the 'M' is half swallowed by the head chip's lit border and glow (`.nn-lw.head` is `background: rgba(14,11,22,.92)` plus a 12px box-shadow, comp/comp.css:3143), the rest sits in the gap above the SIGHT chip. Same defect, different overlap.

Severity stays polish: Escape also closes the map (comp/ninth.js:1677) and the player just pressed M to open it, so nothing is lost but the affordance reads as HUD furniture.
     CORRECTION: Real, but the described collision is harness-specific and one sub-claim is wrong. Correct statement: 'M to close' at comp/ninth.js:8116 is drawn at canvas y 540, which falls inside the undimmable DOM `.nn-hud` band at every window size at or near the shipped 1160x720. WHERE inside that band it lands moves with the window, because `.nn-cv` is object-fit:contain: at the harness's 1:1 1120x580 host it sits in the gap between the word chips and the rhyme keys, but in the real fake-desktop window it lands at DOM y 638.5, 11px inside `.nn-hand` (627..673), with the 'M' half under the lit head chip's border and glow. The rhyme keys are then at y 679..709, i.e. 40px BELOW the hint, so 'on a run where the third rhyme key is lit it is directly behind text' does not hold at real geometry, and does not hold at 1:1 either (the baseline sits on the key's top border, above its label). The collision that survives every geometry is with the word-chip row, not the rhyme keys. Also worth noting the proposed y=96 is only just clear: with a fully explored map, cell shrinks to fit 330px of vertical span centred on oy=280, putting the topmost node edge near y=103.
  --correct-- refuted=False conf=high
     Mechanism verified from source and from layout measurement, not from a screenshot alone.

CODE (my worktree, C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/serene-albattani-aae1af/comp/ninth.js):
- 8067-8069 drawMap: `if (!RT.mapOpen) return; cx.fillStyle='rgba(6,4,10,.9)'; cx.fillRect(0,0,VW,VH);` -- the dim is canvas-only.
- 8116: `cx.fillText('M to close', VW / 2, VH - 40);` with VW=1120, VH=580 (line 45), so canvas (560, 540). Cited line is exact.
- 769: `'<div class="nn-hud">'` is emitted AFTER `'<canvas class="nn-cv" ...>'` (762) inside `.nn`, both `position:absolute` with no z-index (comp.css 2919 `.nn-cv{position:absolute;inset:0;width:100%;height:100%;object-fit:contain}`, 2939 `.nn-hud{position:absolute;left:0;right:0;bottom:10px;...}`), so the HUD paints over the canvas by document order. Cited lines are exact.
- No guard anywhere: the only `mapOpen` reads are 1676/1677 (escape closes), 1695 (toggle), and the input/audio mutes at 1739/1756/2377/2404/2551/2737/2887/2935/5705/6074/6239. Nothing toggles a class on `.nn`, and `updateHud` is called unconditionally from draw() at 3845. The HUD keeps updating and stays at full brightness.

MEASURED (headless Chrome, .claude/nnqc/cor33-measure.html, iframe of the real /comp/?dev=ninth desktop shell, --dump-dom): window 1160x720 -> `.nn` = 1158x678; object-fit contain is width-limited (1158/1120=1.0339 < 678/580=1.169), so the canvas renders 1158x599.7 with 39.15px letterbox top and bottom. Canvas (560,540) maps to page (668, 664.5). `.nn-hud` box is page y 654..735. So the hint baseline sits 10.5px INSIDE the HUD band, and `d.elementFromPoint(668, 664.5)` returns `nn-hand` -- the DOM word-chip row, above the canvas. The bottom 29px of the HUD (the rhyme row, 706..735) is below the rendered canvas entirely and can never be dimmed. Claim holds.

SCREENSHOTS (all against MY build, see harness note below): .claude/nnqc/cor33-mybuild-z4.png (1120x580 harness, hz=4) and .claude/nnqc/cor33-realbox-z1.png / cor33-realbox-z3.png (hosted at 1158x678, the real content box, via .claude/nnqc/cor33-real.html). Both show the breath bar, word chips, rhyme keys, stanza buttons and B/K/I/V buttons at full brightness over the 90%-dimmed map, with 'M to close' tucked into the chip block.

WHAT IS OVERSTATED: the visual description in [33] is the 1120x580 QC harness, not the shipped window. In the harness the hint (glyph box y~532..540) lands in the 6px gap between the chip row (bottom 535) and the rhyme row (top 541); in the real 1160x720 window the 1.0339x scale plus 39px letterbox lift it to page 664.5, i.e. level with the top of the head chip (661.9..700) and just to its right, beside the glow rather than under it. And 'on a run where the third rhyme key is lit it is directly behind text' is wrong in both geometries: the rhyme row top is 541 (harness) / 706 (real) and the hint never reaches it, and the count pip does not change the row height. Nothing ever occludes the glyphs; the defect is purely that a canvas affordance is drawn inside an undimmed DOM widget block.

HARNESS WARNING for whoever reads this: the dev server on port 8492 is NOT serving this worktree. Its /comp/ninth.js is 872028 bytes / 15587 lines and its .claude listing matches C:/Users/isaac/IsaacUre.github.io/.claude/worktrees/suspicious-driscoll-00fbd8 (the audited file here is 475774 bytes / 8279 lines). Every capture taken through localhost:8492 is of that other in-progress build. I re-ran everything against my own server on 127.0.0.1:8493 rooted at this worktree (now stopped). The `M to close` line happens to be identical in all three worktrees, so [33] survives regardless.
     CORRECTION: Real, and correctly attributed to comp/ninth.js:8116 + :769 + comp.css:2939. Two details need fixing. (1) The 'wedged between the slotted-word chips and the 1..5 rhyme keys, half under the glow' picture is the 1120x580 QC harness only. In the shipped 1160x720 window .nn is 1158x678, object-fit:contain scales the canvas by 1.0339 and letterboxes it by 39.15px, so canvas (560,540) lands at page (668, 664.5): level with the top of the head word chip and immediately right of it, 10.5px inside the .nn-hud box (page y 654..735). (2) 'on a run where the third rhyme key is lit it is directly behind text' is false -- the rhyme row starts at page y 706 (real) / canvas y 541 (harness) and the hint never overlaps it; nothing occludes the glyphs in either geometry. The load-bearing part is unchanged: the canvas dim cannot reach the DOM HUD, so the map's own close affordance is drawn inside a full-brightness widget block and reads as HUD furniture. Note also that the unusable band is bigger than the finding implies: the HUD box starts at canvas y 489 in the harness and canvas y 530 in the real window, and its bottom 29px sit below the rendered canvas altogether.

=== [34] REFUTED  severityVotes=['polish']
  --correct-- refuted=True conf=high
     Read comp/ninth.js:8018-8118 (MAP_HIDE, MAP_SEED, exitDir, buildMap, MAP_POS, drawMap) in this worktree; the finding's line numbers 8084-8085 match exactly.

1. Re-derived MAP_POS by copying buildMap/exitDir verbatim into .claude/nnqc/skeptic34-map.js and running it against the real PLACES literal. Result: square[2,3] bernhouse[1,3] chandler[3,3] lane[2,4] mill[4,3] loft[4,2] village[3,4] mark[4,4] road[5,3] hollow[5,2]; stage[1,4], a3sq[2,2], arena[0,0] are all in MAP_HIDE and are excluded from the bounding box at 8077. So spanX=4, spanY=2 all-seen, giving 620/4=155 and 330/2=165, both above the 100 cap. I brute-forced all 2^10 subsets of the visible places through drawMap's exact box/cell code: cell is 100 in every one. The finding's "autoscale is dead code today" is correct, and the 46 floor is likewise never reached. ox=260, oy=-20, nodes span x 360..760 and y 180..380 (a 400x200 box), title baseline at y=70, i.e. 110px above the top row - all as stated.

2. The hazard is where it breaks. drawMap sets cx.textAlign='center' at 8070 and only restores 'left' at 8117, so every node label at 8113 is centred on the node. Overlap therefore requires (w1+w2)/2 > cell, not w > cell. Round one's own measure page (.claude/nnqc/measure-map-labels.html) computes centred half-width gaps correctly at cell=100, but the repro line then compares a full 96.0px label against an 88px pitch and reports "overlap by 8px", and 96.0 against 46 as "overlap by 50px". Both are the wrong test.

3. Re-measured with the real Pixelify Sans at 10px in headless Chrome (.claude/nnqc/sk34-measure.html, --dump-dom, font loaded: true). Widths reproduce round one's: lane 96.01, chandler 95.70, Grelling 35.26; and its cell=100 gaps reproduce (lane -> Grelling 34.37px, matching its "34.4"). At cell=88: loft->hollow 30.59, square->chandler 29.55, chandler->mill 23.45, lane->Grelling 22.37 - every gap positive, no overlap anywhere. At cell=46: overlaps do appear but the worst is lane->Grelling 19.63px and chandler->mill 18.55px, nowhere near 50px, and bernhouse->square and Grelling->mark are still clear.

4. Reachability of the failing cell: floor(620/spanX) <= 65 needs spanX >= 10 (11 columns); floor(330/spanY) <= 65 needs spanY >= 6 (7 rows). cell=46 needs spanX >= 14 or spanY >= 8. Today's world is 5 columns by 3 rows, and PARALLEL.md:103-105 only says Act 3 places get an a3 prefix appended - a3sq itself is in MAP_HIDE and contributes nothing to the box, so it is not even established that new a3 places widen it.

5. Confirmed against the real render: .claude/nnqc/map-full.png (round one's persistent-profile full-map capture) shows all ten labels with clear air between them, matching the derived 5x3 layout.

So the only surviving claim is "the autoscale branch never runs today and cell is always 100", which the finding itself calls fine, with no player-visible or screenshot-visible consequence. The predicted failure does not occur at the trigger the finding names, and the two overlap figures in the repro are both wrong.
     CORRECTION: Accurate version: at comp/ninth.js:8084-8085 `cell` evaluates to 100 for every reachable save state (verified by brute force over all 2^10 subsets of the ten map-visible places), so the `620/spanX` / `330/spanY` terms and the `Math.max(46, ...)` floor are both currently inert. That part is true and is the whole of the finding. The label-overlap hazard attached to it is not real at the thresholds claimed: labels are drawn centred (`cx.textAlign = 'center'` at 8070, restored to 'left' only at 8117), so an adjacent pair collides when (w1 + w2)/2 > cell, not when a single label is wider than cell. Worst adjacent pair is 'The lane out of Wick' (96.01px) / 'Grelling' (35.26px), which needs cell >= 65.63; second worst is 'The chandler's shop' / 'The mill' at 64.55. So the first overlap appears at cell <= 65, which needs spanX >= 10 (an 11-column map) or spanY >= 6 (a 7-row map) - roughly double today's world in either axis - not the 8 columns the finding names. At the finding's own hypothetical cell=88 every gap is still positive (worst 22.37px, lane -> Grelling), and at the 46px floor the worst overlap is 19.63px, not 50px.
  --repro-- refuted=True conf=high
     Read comp/ninth.js:8020-8118 (MAP_HIDE, MAP_SEED, exitDir, buildMap, drawMap) and VW/VH at line 45.

1) Re-derived MAP_POS from the shipped buildMap/exitDir against the real PLACES literal (.claude/nnqc/_rr34-mappos.js, output .claude/nnqc/_rr34-data.json). Visible (non-MAP_HIDE) cells: bernhouse[1,3] square[2,3] chandler[3,3] lane[2,4] village[3,4] mill[4,3] loft[4,2] mark[4,4] road[5,3] hollow[5,2]. Full-map bounding box lo=[1,2] hi=[5,4], spanX=4, spanY=2. Line 8084: cell = min(100, floor(min(620/4, 330/2))) = min(100,155) = 100; line 8085: max(46,100) = 100. cell drops below 100 only at spanX>=7 (620/7=88.6) or spanY>=4 (330/4=82.5). Even the pathological branch at 8058-8063, where a place the exit graph never reached is parked at x=-1, only takes spanX to 6, and 620/6=103.3 is still >=100. So cell is 100 in every save state: prologue, cold, or complete. The finding's own premise is right and it is unreachable, which under this lens is a refutation.

2) Re-ran the finding's measurement repro with the real font (.claude/nnqc/_rr34-measure.html, headless Chrome --dump-dom, "font check: true"). Widths reproduce exactly: "The lane out of Wick" 96.0, "The chandler's shop" 95.7, "Grelling" 35.3, "The road north" 74.3, "The grain loft" 66.5. But my page computes gaps against the ACTUAL row adjacency from MAP_POS rather than against the two widest strings. cell=100 worst gap +34.4 (lane->Grelling); cell=96 +30.4; cell=88 +22.4, i.e. no overlap at all, contra the finding; cell=66 +0.4; cell=46 worst overlap 19.6px. The finding's cell=88 "overlap by 8px" and cell=46 "overlap by 50px" are exactly (96.0+95.7)/2 - 88 = 7.85 and (96.0+95.7)/2 - 46 = 49.85: the lane/chandler pair, which is on different rows.

3) Screenshot repro. The QC page on port 8492 turned out to be serving a DIFFERENT worktree (.claude/worktrees/suspicious-driscoll-00fbd8, comp/ninth.js 15587 lines vs this worktree's 8279; md5 confirmed identical to that worktree's file), so I stood up my own static server for this worktree (.claude/nnqc/_rr34-serve.js, port 8493) and made a font-loading copy of the harness (.claude/nnqc/_rr34-canvas.html) that awaits document.fonts.ready before NINTH.init, so labels render in Pixelify Sans rather than the fallback the stock canvas.html gives. Seeded all ten visible places into one persistent Chrome profile (ndev per load, nwipe only on the first) and opened the map with nkey=m. .claude/nnqc/_rr34-map-full.png and .claude/nnqc/_rr34-map-row4-z2.png show the finished map: ten lit nodes, every label clearly clear of its neighbours, the tightest pair being lane -> Grelling exactly where the maths says. No pixel on screen differs from a build where 8084-8085 read "var cell = 100;".

4) Reachability. No input, no save state and no dev handle produces a cell other than 100. The finding itself concedes "That is fine today" and "Nothing needs doing before a new place lands". PARALLEL.md:103-104 does tell job 1 to append a3-prefixed Act 3 places, but buildMap slips a colliding cell along the perpendicular axis, so a new place adds a row as readily as a column, and three more columns would only take cell to 88, where every gap is still >=22px.
     CORRECTION: The verifiable half of [34] is that at comp/ninth.js:8084-8085 the autoscale is inert. With today's world the visible map spans x 1..5 and y 2..4, so spanX=4, spanY=2, and cell = min(100, floor(min(620/4=155, 330/2=165))) = 100, then max(46,100) = 100. Both the cap and the floor are dead in every reachable save. Nothing else in the finding holds. The overlap arithmetic is wrong: at cell=88 there is no overlap anywhere on the map (worst neighbour gap +22.4px, lane -> Grelling), and at cell=46 the worst overlap is 19.6px, not 50px. The finding's 8px and 50px come from pairing "The lane out of Wick" (96.0px) with "The chandler's shop" (95.7px), which sit at map cells [2,4] and [3,3]: different rows, 100px of vertical pitch apart, so they can never collide. Overlap for the real worst neighbour pair (lane 96.0 / Grelling 35.3) needs cell < 65.6, i.e. spanX >= 10 or spanY >= 6, which is eleven columns or seven rows of visited places, not the "eight columns" the finding names. The residue is a polish-grade note that two guards are inert; I would drop the entry rather than keep it at risk.

=== [35] REFUTED  severityVotes=[]
  --repro-- refuted=True conf=high
     MECHANISM REPRODUCES, HARM DOES NOT.

Verified true: `been_*` is written only at comp/ninth.js:6119 inside gotoPlace (grep shows the other five hits are reads at 8077/8090/8094/8106 plus one predicate at 5452). devDemo calls gotoPlace once, comp/ninth.js:8135. nfrag/nitems/nfoes/na3/npoem/npanel/nscroll never touch S.seen. I also chased the alternate route the finding did not consider: nkey=` toggles the dev panel and its WORLD tab has a per-place `gotoPlace(id, true)` button (comp/ninth.js:885), but those rows are DOM click handlers (comp/ninth.js:5016, 5049) with no keyboard activation, so nkey cannot fire them. Captured the single-URL result: .claude/nnqc/rr35-coldmap-single.png (?nwipe=1&ndev=mill&nfr=240&nmap=1) shows the mill gold and twelve dark `?` nodes. So "one lit node from one URL" is real.

Refuted: the claim that the map "is uncapturable in its real state through the harness" and that "any map screenshot anyone has taken shows exactly the cold state". I ran the persistent-profile route today, three loads sharing --user-data-dir (nwipe=1&ndev=square, then ndev=mill, then ndev=lane&nmap=1): .claude/nnqc/rr35-step3-map.png shows Wick, the mill and the lane lit with solid walked links and the rest dashed. Zero code change. Because gotoPlace writes the same flag whether you jumped or walked, that capture is state-identical to a played one. The finding's own repro paragraph concedes this route works, which contradicts its `what` paragraph. Empirically it is contradicted too: the same .claude/nnqc directory already holds map-full.png, map-full-zoom.png, travel-map-all.png, travel-map-all-z2.png, lensmap-mapall.png, audithole-map-all.png, skep27-map-all.png, sk33-map-full.png and cor33-map-full-z1.png, all real multi-place maps. Round one additionally left .claude/nnqc/travel-lens-map.html, a 20-line scratch host page that seeds S.seen['been_'+id] for ten places and calls N.tick(60) - a genuine ONE-load full-map capture built entirely inside the scratch-file allowance the brief grants (I could not re-serve it only because the running dev server on 8492 is rooted at a different worktree: /.claude/nnqc/* 404s while /.claude/ninth-night/tools/canvas.html returns 200).

REACH: comp/ninth.js:1625 is `if (/ndev=/.test(location.search)) devDemo();`. devDemo runs only when the URL carries ndev=. No player at isaacure.com/comp/ can reach any of this. The sole consumer is a QC agent, who already has a working documented route printed in the brief's own "harness limits" note. The proposed fix adds a dev-only param to the shipped 8279-line game file to save that agent two extra chrome invocations on one screen.
     CORRECTION: The code fact is right and the severity label "risk" is the right shape, but the stated harm is wrong. Correct statement: a single devDemo URL can mark only one place visited, so a one-load map capture shows one lit node and twelve `?` nodes; the real multi-place map is still capturable with no code change, either by repeating the load against a shared --user-data-dir (verified today, .claude/nnqc/rr35-step3-map.png, three places lit) or by a scratch host page under .claude/nnqc that seeds S.seen (round one already wrote one, travel-lens-map.html, and it produced travel-map-all.png). The assertion that "any map screenshot anyone has taken shows exactly the cold state" is false: at least nine full-map captures sit in .claude/nnqc today. Nothing player-facing is affected, since devDemo is gated on ndev= at comp/ninth.js:1625. An nseen param would be a convenience for the harness, not a defect in the game; it does not warrant a change to comp/ninth.js.
  --correct-- refuted=True conf=high
     Checked the code, not the claim.

The one true part: `S.seen['been_' + id] = 1` is written in exactly one place, comp/ninth.js:6119 inside gotoPlace, and devDemo has no parameter that seeds it. Verified by grep: the only other been_ sites are readers (5452, 8077, 8090, 8094, 8106).

But the finding's stated mechanism is "devDemo calls gotoPlace exactly once (line 8135)", and that is only true of devDemo's own source text. gotoPlace is reachable twice more from inside a single devDemo run, and the finding missed both:

1. Exits are interactables. comp/ninth.js:6224 pushes every exit into interactables() as `{k:'exit', ...}`, nearestInteract picks anything within 1.9 tiles (6231), and doInteract's exit branch at 6249-6251 calls `gotoPlace(o.e.to, false)`. The keydown chain binds this at 1688 (`if (k === 'e') doInteract();`), and nkey (8148) dispatches real keydown/keyup through exactly that chain. So `nat=<within 1.9 of an exit>&nfr=300&nkey=e` travels, and marks a second been_ flag.

2. Place scripts travel on their own during the nfr loop. SCRIPTS.prologue at comp/ninth.js:5480 is `beat(13.4, function () { gotoPlace('square', true); })`, and beats are stepped by step(), so any ndev=stage run with nfr >= 810 marks been_stage AND been_square without any input at all.

Reproduced both, single page load each, screenshots in .claude/nnqc/:
- skep35-mill-lane-map.png, from `...&nwipe=1&ndev=mill&nat=6.8,10.8&nfr=300&nkey=e&nmap=1`: the map shows The mill lit as visited and The lane out of Wick lit as current, with the solid road drawn between them. Two nodes, one URL, no persistent profile.
- skep35-stage-square-lane-map.png, from `...&nwipe=1&ndev=stage&nfr=900&nkey=e&nmap=1`: three places visited (stage, square, lane; stage is suppressed by MAP_HIDE at 8023), two lit nodes and the road. The E lands because gotoPlace's no-back-exit arrival puts you at (W/2, H-2.2) = (8.5, 12.8) in the square, 1.5 tiles from the lane exit at (8.5, 14.3), inside nearestInteract's 1.9 radius but outside exitAt's band (y 13.85..14.75), so it prompts rather than auto-travelling.

My first attempt (skep35-two-nodes.png, nat=8.5,13 in the square) failed only because the wandering child was nearer than the exit and E opened her dialogue instead. That is a targeting problem, not a capability limit.

Chaining stops at about three: gotoPlace does not clear RT.prompt (it is only recomputed in stepPlayer at 1750), and the nkey loop only draw()s between keys, so a second `e` re-fires the stale exit object and re-enters the same place.

Separately, the finding contradicts itself. The "what" says the map "is uncapturable in its real state through the harness"; the "repro" then documents the --user-data-dir workaround and names three files it already produced (map-full.png, map-full-zoom.png, map-in-a3sq.png). So it is not uncapturable through the harness, it is uncapturable in one page load.

Nothing here touches shipped behaviour: devDemo is dev-only code behind ?dev=, no player reaches it, and no screenshot of the game shows a defect.
     CORRECTION: What survives is much smaller than the finding claims, and is a harness convenience request rather than a defect. Accurate statement: "devDemo has no parameter that seeds been_ flags directly (the only writer is comp/ninth.js:6119 in gotoPlace), so a full map still needs either a chain of travels or a persistent Chrome profile across loads. A single URL can already reach two or three places, via nkey=e onto an exit (interactables 6224 -> doInteract 6249-6251 -> keydown 1688 -> nkey 8148) or via a script beat that travels during the nfr loop (SCRIPTS.prologue 5480)." The headline "cannot produce a map with more than one place visited" is false, and "uncapturable in its real state through the harness" is contradicted by the finding's own repro, which cites three full-map captures it took with the documented --user-data-dir workaround. The suggested nseen param would be a nice ergonomic addition, but "could be improved" in dev-only tooling with two working routes is not a defect.
