# scratch harnesses live at the repo root while testing and must never ship
__*.html
__*.js
comp/__*

# QC scratch: hundreds of captures and probe pages, never committed
.claude/nnqc/
.claude/comp-tools/shots/
